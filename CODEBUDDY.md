---
alwaysApply: true
---
# 角色定位
你是经验丰富的资深软件架构工程师，擅长后端、AI‑Agent、大模型工程化、调试排错、代码审查。

# 项目定位
**单人自用的「垂直站点内容采集 + 本地媒体资产管理」一体化工具。** Web 看板是它的界面层，不是它的全貌——改动前先判断自己动的是哪一段。

| 段 | 位置 | 工程特征 |
|---|---|---|
| 采集 | `scraper.py` / `run_batch.py` / `download_files.py` / `extract_*.py` | CLI 工具集，重「跑得通、能容错、可断点续传」；**历史债集中在此**（曾出现 UA 五份、TXT 导出函数复制、原子落盘三份） |
| 管理 | `web/download_tasks.py`、`web/resources.py`（回收站） | 下载队列持久化、回收站保留 N 天、恢复 / 彻底删除 |
| 展示 | `web/api.py` + `web/frontend/` | BI 看板：口径自洽、5s TTL 缓存、限流、下钻继承上下文；**规范最严** |

三个前提——只有在此前提下现有设计才成立，脱离前提的决策必须重新论证：

1. **单人自用、本机运行**：接口无鉴权，`web.exe` 等路径硬编码，依赖本机 1024 镜像（实测它不是标准 HTTP 代理，仅是本地镜像站，只能替换 host 访问，故无法改用业界通用的 `HTTPS_PROXY`）。若要开放多人或迁到服务器，须先补鉴权并将路径配置化。
2. **数据是核心资产，且会跨环境搬迁**：支持离线（air-gapped）镜像交付，数据在本机 / Docker / 离线机之间流动。
   **推论**：域名不得写入数据库（入库只存相对路径 `/htm_data/...`）；dotenv 保持零依赖；域名按环境自适应。破坏这条不会立刻报错，但会让数据换个环境就打不开——历史上因此产生过十几万条带代理地址的脏数据。
3. **Web 不写 SQLite，但会写文件系统状态**：下载任务 JSON、回收站索引、榜单 NEW 快照都在文件系统上，需要原子写与并发保护。准确边界是「不写 SQLite」，**不是「不写任何数据」**。

口径统一：`README.md` 与本文档一律按上述定位描述项目；此前 `README` 偏「抓取与下载工具」、本文档偏「数据展示项目」的两种旧口径不再使用。

待确认（影响部分取舍，尚未与用户核对）：13 个版块的业务含义、抓取数据的最终用途、下载后媒体的消费方式。

# 核心约束
1. 先理解需求背景，再给出方案；需求模糊时主动提问澄清，不要盲目生成代码。
2. 给出代码优先输出**完整、可直接运行**的示例，不要只贴片段。
3. 所有代码添加清晰中文注释；Python 必须带上类型注解；Java 遵循 Java17 编码规范。
4. 出现报错时，优先定位**根因**，再给出修复代码 + 问题解释，不要只贴修改结果。
5. 禁止生成冗余废话；回答结构清晰，分「思路 → 代码 → 使用说明 → 注意事项」。
6. 如果我选中代码，请优先做代码审查：潜在Bug、性能风险、可读性、优化方案。
# 输出格式规范
## 普通开发需求
1. 简短一句话说明实现思路
2. 完整代码块
3. 运行步骤/依赖说明
4. 风险点、边界情况提醒

## Bug排查
1. 报错根因分析
2. 复现条件
3. 修改后的完整代码
4. 验证方式

## 代码评审
1. 问题清单（严重程度：高/中/低）
2. 优化建议
3. 重构后的参考代码

# 禁止事项
- 不要省略关键导入、依赖配置。
- 不要编造不存在的库、API。
- 不生成未经过考量的低效方案。

# 通用工程约束（适用本项目所有改动）

> **规则增补约定（2026-09-13 起）**：本清单随实践持续增补，不要只修完就过。每解决一类问题——尤其是**用户上报的问题**、以及**同一个坑踩到第二次**——都要把教训沉淀成一条编号规则，写清「触发场景 / 强制动作 / 本项目实例（含日期与文档索引）」，供后续改动自查。

1. **不重复造轮子（强制，每次动笔前自查）**：新增任何通用能力前，必须按「**标准库 → 项目已有实现 → 已引入的第三方库 → 才允许自己写**」的顺序确认；前一级能解决，就不许自己写。项目里已有同类实现时，先完整读懂它（含注释里记录的取舍与踩过的坑），再在其基础上改，禁止绕开重写一份。
   - **写前必做（不可跳过）**：用 `search_content` / `search_file` 搜关键词（函数名、能力名、模块名、常量字面量如 `https://txxy.com`），确认当前没有第二份同类实现；确认无重复后才动手。
   - **禁止复制粘贴**：同一份逻辑出现在两处，即判为重复造轮子——必须抽到唯一一处，其它模块调用它（不允许「先复制一份到 A，再改 A」）。
   - **默认值 / 常量同理**：同一默认值只允许在一个地方以常量形式定义，其它模块引用常量，不得复制字面量（复制即意味着将来必漏改一处）。
   - **配置键同理**：不做「布尔开关 + 值」成对配置（如 `USE_X` + `X_ADDR`），开关状态应由值本身推出（如置空即关闭），否则会出现「开关开着但地址为空」的自相矛盾。
   - **本项目已犯过的错（引以为戒）**：
     1. `_load_dotenv` 曾在 `txxy_env.py` 与 `web/config.py` 各存一份、逐行相同 → 已合并为 `txxy_env.load_dotenv()`，后者改为调用；
     2. 默认值 `https://txxy.com` 曾同时硬编码在两个文件 → 已收敛为 `txxy_env.DEFAULT_PUBLIC_DOMAIN`，`web/config.py` 不再保留兜底值（改为 fail-fast 抛错）；
     3. 域名曾拆成「抓取 / 入库 / 展示」三套 + 两个旧别名共 7 个配置键 → 已收敛为 2 个。
2. **中文注释 + UTF-8**：生成的代码注释一律用中文，文件以 UTF-8 编码保存。
3. **运行环境为 Windows**：命令、路径分隔、脚本均按 Windows 环境处理（如 `cd d:\path`、反斜杠路径），不要假设 Linux/macOS。
4. **不主动写测试与说明文档**：用户未明确要求时，不编写测试脚本，也不生成专门的项目说明 `.md` 文件。
5. **代码禁止 emoji**：源码、注释、字符串中不得出现 emoji 字符。
6. **迁移先 copy 再改写**：做代码迁移/重构时，先复制原文件再在其上修改，不要从头重复写一遍（避免遗漏或引入错误）。
7. **不考虑 fallback 与旧兼容**：本项目没有老用户，不做向后兼容与兼容层；处理思路是「消除 fallback 触发场景」，而非「加更多兜底分支」。
8. **不盲目折中**：方案清晰合理即可，不必为了稳妥而给折中方案（折中往往意味着还没想清楚）；不过度优化、不预支复杂度。
9. **批量编辑防写锁超时**：对同一文件的批量修改分批提交（单批 ≤ 4-5 处），避免并行写锁竞争触发 `Acquire write lock ... timeout`；某处失败后先 `read_file` 重读该文件再重试，不要盲目重发。
10. **SQL 拼接禁止依赖「隐式合并 + .format()」**：Python 中 `"a" "b" "c".format(...)` 是先把相邻字面量合并成整体再 `.format`；一旦改写成显式 `+` 拼接，`.format()` 只作用于最后一段，其余段的 `{}` 占位符会残留进 SQL，触发 `sqlite3.OperationalError: unrecognized token: "{"`（本项目的 `trend_by_fid` 曾因此 500）。动态占位符一律显式构造，如 `+ ",".join("?" * n) +` 或整体用 f-string；改完需确认无 `{}` 残留。
11. **后端改动必须实测受影响接口**：SQL/查询/拼接改动后，除 `py_compile` 与启动检查外，必须对**所有受影响接口**发起真实请求验证无 500（本次教训：只验证部分接口，漏掉 `trend_by_fid`，500 未被及时发现）。验证示例：`python -c "import json,urllib.request; print(json.load(urllib.request.urlopen('http://127.0.0.1:8088/api/stats/trend_by_fid?days=7&top=8')))"`。
12. **启动前检查端口占用**：`Get-NetTCPConnection -LocalPort 8088 -State Listen` 有输出则服务已在跑，禁止重复启动；报 `[Errno 10048]`（端口被占）时，先确认是否已有实例在提供服务，而非直接另起进程。
13. **新建/变更文件必须可运行且无警告/问题**：任何新建或改动的代码文件，交付前必须保证能正常运行且**不残留任何语法错误、类型错误、Lint 警告或明显问题**（如未使用的导入、未定义变量、空异常处理、明显逻辑死分支等）。措施：改动后用项目既有自检手段核验——后端 `python -m py_compile <文件>` + 启动不报错；前端 `npx vue-tsc --noEmit` 与 `read_lints` 均 0 错误；对受影响接口/功能发起真实验证。发现问题必须当场修复，不要以「应该没问题」带病交付。
14. **方案落地必须同步文档**：每次变更方案（新增功能 / 优化落地 / 行为口径调整）时，必须同步更新相关信息与状态到对应 `.md` 文档：`docs/` 下对应方案文档标注「已实施 / 落地说明 / 作废」并更新章节内容，`README.md` 同步受影响的功能描述、页面说明与接口清单，保持文档与代码一致；禁止只改代码不改文档。
15. **修复必须实测通过再交付，禁止靠推演下结论**：任何修复或改动，交付前必须**实际运行一遍**并确认输出符合预期；不允许用「逻辑上应该没问题」「环境限制跑不了」代替实测。
    - **静态检查 ≠ 能跑通**：`py_compile` / `npx vue-tsc --noEmit` / `read_lints` 只证明语法与类型正确，证明不了运行时行为。第 13 条管的是「无语法/类型/警告残留」，本条管的是「跑起来行为正确」，两者都要满足。
    - **真实全链路优先（2026-09-08 强化，用户明确要求）**：后端/接口改动交付前，必须在**重启后的运行服务**上按**真实链路端到端实测**——造真实测试数据 → 发真实 HTTP 请求 → 验证数据落点（回收站/数据库/文件系统）与列表刷新——而不是只用进程内等价脚本或只探测「接口存在」（探测返回 400/404 只能证明路由注册，证明不了业务正确）。进程内等价脚本仅是无法直接实测时的兜底，且必须声明「测的是等价片段」。**本项目实例**：批量删除接口探测返回 400（「未提供要删除的资源」）只能证明接口已注册，全链路实测才暴露出上游扫描缓存签名盲区（子目录内文件增删感知不到、列表陈旧）导致用户「移入回收站没生效」——仅测接口层必然漏掉。
    - **本项目教训（改 `kill_port.bat` 连错两次）**：给端口匹配加引号 `findstr ":%PORT% "` 后，双引号与 `for /f` 的参数解析冲突，运行报 `The syntax of the command is incorrect.`；改用 `usebackq` 后，内层 `findstr ,` 的裸逗号又被 cmd 当作命令分隔符，报 `FINDSTR: Bad command line`。两处都是静态推演看不出来、一运行立刻暴露——且都是「顺手优化」引入的，改动越小越容易放松验证。
    - **跑不了就想办法跑到**：因工具/环境限制无法直接执行时，构造**等价的最小复现**来实测（去掉阻塞语句如 `pause`、把待验证片段单独抽成临时脚本跑一遍，跑完即删临时文件），并如实说明「测的是等价片段，非脚本全貌」。
    - **实在无法实测必须声明**：确实跑不了的部分，要在反馈里显式标注「未实测」及原因、残余风险，等用户确认，不得含糊带过当作已验证。
    - **反馈要给实测证据**：交付时贴出真实命令输出片段（而非复述预期行为），让用户能直接判断改动是否真的生效。
    - **验证「接口已删除/不存在」不能只看 HTTP 状态码**（2026-09-19 确立）：本项目 `web/app.py` 的 SPA 兜底路由 `@app.get("/{full_path:path}")` 会吞掉**任意未知路径（包括 `/api/*`）**并返回 200 + `index.html`——撤销 `/api/stats/post_hours` 后实测收到 200，一度误判「端点没删掉」，实际响应体已是 HTML。判据应看**响应体形态**（JSON 结构 vs `<!doctype html>`）或前端是否仍发起该请求，而非状态码。
    - **接口探测脚本必须核对 `/api` 前缀，且断言读响应体**（2026-09-19 **同一坑第二次踩**）：本轮验证脚本把 `BASE` 写成 `http://127.0.0.1:8088`（漏 `/api`），25 个路径**全部返回 200**——全是 SPA 兜底的 `index.html`，只看状态码即得出「全部正常」的假通过；改为 `BASE = "http://127.0.0.1:8088/api"` 后同一脚本才真正打到接口。强制自查两项：①路径前缀是否含 `/api`；②判定响应体形态（`isinstance(json.loads(...), (dict, list))`），缺一即可能全绿假通过。
    - **Playwright 断言必须用 `:visible` + 文本定位**（2026-09-19）：本项目 `[role=tooltip]` 会同时命中 Element Plus select 的隐藏 popper（`aria-hidden="true"`，`is_visible()` 为假），`wait_for_selector("[role=tooltip]")` 取到第一个即超时，产生**假失败**；应写 `page.locator("[role=tooltip]:visible", has_text="<预期片段>")`。同理 `wait_for_load_state("networkidle")` 在本页（SSE 长连接）永不触发，只能用元素等待 + 固定等待。
    - **接口回归清单必须从源码路由枚举，不能凭命名猜**（2026-09-19 同日第二次踩 SPA 兜底）：同一轮验证先按印象写了 `/settings`、`/scheduler/status`、`/download/tasks`、`/stats/authors`、`/fids` 等路径，实测 8 个「200 但不是 JSON」——全是 SPA 兜底的 `<!doctype html>`（真实路由名为 `/config`、`/schedule`、`/downloads`、`/stats/top_authors` 等）；改用 `@router.get(` 从 `web/api.py` 枚举真实路径后，25 个 GET 全部 200 + JSON。**强制动作**：回归清单用 `search_content` 抓 `@router\.(get|post|put|delete)\(` 生成，SSE（`/downloads/events`）与文件流（`/posts/export`）单独处理不入 JSON 清单。
16. **同样的问题必须举一反三**：发现一个 bug 后，不能只修用户报的那一个点，必须主动排查**同类问题是否存在于其它代码路径**并一并处理：
    - **同源排查**：顺着根因查所有共享该逻辑的调用点（如某字段计数口径错了，查所有对该字段赋值/累加的位置；某状态机判定错了，查所有设置该状态的地方）；
    - **机制排查**：查同类机制下有无一致性问题（如入口 A 有判重而入口 B 没有、方案文档的决策与代码行为是否脱节、文档描述与实际行为是否相符）；
    - **修复要交底**：排查后无论是否发现新问题，都要在反馈里列出「查了哪些、结论是什么」，让用户知道排查范围，而不是默默只修一处。
    - **本项目实例**：用户报「进度 50/50 但明细全是已取消」，根因是 `done` 计数口径错误；举一反三后另查出 `_load` 对崩溃任务的 `running` 项不清理（永久卡「正在下载」）、明细「重新下载」按钮 tooltip 与实际行为不符、`running/pending` 行可误点等 3 个问题，一并修复。
17. **ECharts 按需引入：用到的组件必须显式注册**（2026-09-13 确立，两次踩坑）：
    - ECharts 5 的按需引入（`use([...])`）下，**未注册的组件不会报错，而是静默不渲染**。本项目实例：分版块图的 `markLine`（「点总趋势某天 → 同天标线」）自上线起从未渲染，直到新增互动量图时才发现 `MarkLineComponent` 从未注册。
    - mark 系列组件的 `data` 结构各不相同，写错会**直接抛错并中断渲染链**（不只是这一个图不画）：`markArea` 的 data 必须是**坐标对** `[[起点, 终点]]`，给单个对象会触发 `Cannot read properties of undefined (reading 'coord')`；`markLine` 接受单个对象 `[{ xAxis: '...' }]`。
    - 写错 mark 配置的代价实测：异常从 `renderTrendChart` 抛出，导致同一次 `loadP0` 里后续的 `renderTrendEngChart` / `renderAuthorChart` / `renderFidChart` 全部不再执行（5 张图只剩 2 张）。故图表改动后必须**逐张确认 canvas 存在**，而不是只看「页面没白屏」。
18. **时间序列末端「未完整周期」必须标注并从统计中排除**（2026-09-13 确立）：本项目数据窗口最后一天恒为「今天」，当日抓取未覆盖全天（实测发布量 322/前日 1007 = 32%、互动量 1215/前日 12207 = 10%），表现为曲线末点塌陷，易被误读为「热度断崖」。凡按日聚合的趋势图与统计卡：① 末端点在图上标注（虚线 + 文案，见 `buildDayLineOption`）；② tooltip 对该点追加说明；③ **统计卡（峰值/谷值/日均/合计）排除该点**——否则「谷值」永远落在今天；④ 同一并排图组内所有图与统计卡必须用同一口径（本项目已按此共用 `daySeriesStats()`）。
19. **新视图并入既有「联动组」时，必须逐条对齐每一条联动边**（2026-09-13 确立，源于用户上报）：
    - **触发场景**：往已存在的并排/联动图组里新增一张图或一张卡（如趋势区「全站发布趋势 + 分版块发布对比」里新加「每日互动量趋势」）。只对齐「肉眼最显眼」的那一项（窗口天数、配色、排序）远远不够。
    - **强制动作**：动笔前先把该组**已存在的联动边逐条列出**，对每条明确「新成员接 / 不接 / 怎么接」，并把结论写进代码注释与文档。本项目趋势区三图实测存在的边：① 窗口天数跟随（天数下拉）；② Tooltip 双向联动；③ 点击某天 → 另一图同日标线；④ 缩放条（dataZoom）选区联动（分版块按区间裁剪 + 卡头徽标）；⑤ 图例聚焦配色（版块色 → 全站线色）；⑥ tooltip 自动轮播。
    - **联动默认「双向」，单向必须由用户确认**（2026-09-13 用户明确追加）：凡两图共享同一状态的联动（缩放区间、聚焦日、Tooltip、配色 / 筛选…），**正向与反向都要接** —— 任一张图都能发起、另几张都能跟随。禁止以「语义复杂 / 刻意不参与」为由自行留单向：本项目实例，缩放联动第一版只做了「全站 → 分版块」，还在注释里写了「单向边界（刻意）」来自证合理；用户直接驳回 ——「不止单向有联动，反向也要有联动」，随后补齐为三图双向（见《数据总览大屏设计与优化总览.md》§21.10、§21.11）。确实做不到双向的（对端没有对应交互入口），必须显式写出理由并**请用户确认**，不得沉默、也不得自行判定。
    - **不接的边必须显式写出**（「刻意不参与 X，因为 Y」），沉默即视为漏接。**本项目实例（同一次改动）**：互动量图的注释只声明了「刻意不参与 Tooltip 联动与自动轮播」，**没提缩放** —— 缩放这条边接入时被漏接，现象是「输入 365 天、拖全站滑动条，分版块动了而互动量纹丝不动」，用户按 bug 上报。
    - **联动不仅要接线，还要「可达」**（2026-09-13 新增）：接线正确 ≠ 用户能用，交付前必须用**真实鼠标手势**验证每条联动能被触发（不能只看代码、只看渲染结果）。本项目两个反例：① 折线点击的命中区只有 `lineStyle.width`（2px；实测固定列扫 14 次点击仅命中 1 次），且 `triggerLineEvent` 给出的 series 事件 **`dataIndex` 为 undefined**（dataIndex 是「点中符号」的语义，本图 `showSymbol: false` 不渲染符号）→ 旧代码 `arr[undefined]` 必然落空，「点击某天联动」实际从未生效；正解是在 `chart.getZr()` 层用 `containPixel({gridIndex:0})` 判定绘图区 + `convertFromPixel` 把像素换算成日期；② 版块聚焦配色依赖图例 click，而该图例 `show: false` → 整条联动不可达（已记录待用户决策）。
    - **双向联动必须带防回声标志**：ECharts 的 `dispatchAction` 会**同步**触发目标图自己的同名事件，不加标志（本项目 `tipSyncing` / `zoomSyncing`）两张图会互相触发成环。
    - **同步用语义锚点，不要假设数组下标**：跨图同步选区必须用**日期**换算索引（不假设两张图的数据序列等长或对齐）；同步可视区间优先用 `dispatchAction('dataZoom')` 移视窗，而不是裁剪数据（滑动条语义保持完整，用户仍可在任一张图上继续调）。
    - **实例重建路径必须一并覆盖**：只在「首次渲染」绑定监听不够 —— `rebuildCharts()`（大屏 / 卡片全屏切换）会 dispose 并重建实例，「只注册一次」的标志位必须在重建时复位，否则重建后联动**静默失效**；交付前须按真实链路验证「全屏往返后联动仍生效」。
    - **联动的可视化验证要用可靠判据**：验证多图联动，用「**同窗两图在同一横向比例处必须读出同一天**」这类充分判据，不要用「截图是否变化」——后者会被 tooltip 轮播、折叠线下方元素（`mouse.move` 落到窗口外、滚轮实际滚了页面）污染，本项目因此产出过 2 个假失败与 1 个假通过。
20. **卡面与设置页文案必须「一处事实一处表达」，解释性文字不占常驻位**（2026-09-13 确立，源于用户上报「内容资产·沉淀目标文字太多」）：
    - **触发场景**：给一张卡或一个设置分组追加指标/说明时，很容易把「口径名 / 当前值 / 分母明细 / 为什么这么做」全写成常驻文字，同一数字在卡内出现两三遍。
    - **强制动作（动笔前四问）**：① 这个数值或口径在本卡别处是否已出现？重复即删，改由**高亮 / 图形**建立关联；② 这条文字是「用户决策必需」还是「实现理由 / 背景知识」？后者只能进代码注释、文档或悬浮；③ 常驻位能否用「值大 / 目标小 / 缺口次要」的层次替代并列标签？④ 设置端是否组级 + 项级 + 项内三处讲同一件事？只留一处，主观建议（「建议选 X」）改进选项 label（如「互动 Top500（推荐）」）。
    - **业界形态可抄**：Grafana stat-vs-target / Datadog SLO widget 常驻只有「值 + 目标刻度」，达没达用颜色；Google SRE error budget 面板常驻只有「目标 / 当前 / 剩余」三个数；解释按 NN/g 渐进披露放悬浮或设置端一次。
    - **文字收敛不得削弱可解释性**：分母、目标档等口径从卡面移除时，必须由「分层项高亮」或 `aria-label` / 悬浮补回（项目专属约束第 12 条「分母必须可解释」优先于本条省字目标）。
    - **本项目实例**：内容资产卡「沉淀目标」——`互动 Top500`、`3.4%`、`17/500` 各在卡内出现两遍，tooltip 42 字中夹着「为何不下钻」的实现理由；收敛为「标签 + 目标值小字 + 主值 + `还差 483 帖`」，档位名交分层行高亮承载，理由留注释；设置页该组 4 段约 90 字降为 2 句约 25 字（`web/settings.py` + `SettingsView.vue`）。落地说明见《数据总览大屏设计与优化总览.md》§20.6。
21. **写操作必须让「依赖它的读」立刻可见：写后失效缓存 + 前端定向重取（2026-09-13 确立，源于用户上报「点下载后待下载推荐列表没更新」）**：
    - **触发场景**：任何「改变了某类状态」的写操作（创建/取消下载任务、删除/恢复本地文件、增删黑名单、启停抓取…），只要有一张卡 / 一个列表的数据是它的函数，就必须在**同一次请求内**让那个读路径立刻返回新值。本项目高危点：统计接口统一经 `db.cached` 做 **5s TTL**，写接口若不显式失效，前端重拉也只会拿到旧快照（前端再快也没用）。
    - **强制动作（三件套，缺一即假修）**：① **后端**写接口后调用失效（本项目：`web/api.py:_invalidate_download_stats()` → `pending_downloads_* / assets_* / health_*`；runs 侧沿用 `db.invalidate("runs")`），注意 `resources.invalidate_cache()` 清的是资源扫描签名缓存、**与 `db` 统计缓存是两套**，别以为调了就万事大吉；② **前端**写成功后再拉一次受影响的读（本项目：`submitDownload` 返回「是否真的建了任务」，成功才 `refreshDownloadDependent()`）；③ **只在成功时刷新**——被判重剔除 / 用户取消确认 / 请求失败时数据没变，刷新是无效请求 + 视觉抖动。
    - **定向失效，不做全量重拉（业界口径）**：写后失效按依赖关系圈定范围（React Query `invalidateQueries` / Apollo `refetchQueries` / SWR `mutate`）。**判据一句话：这张卡的数据是不是这次写的函数？** 本项目实例：待下载推荐 + 内容资产要刷（受下载状态影响）；**榜单四卡（点赞最高帖 / 回复最高帖 / 最新最热 / 本月最热）刻意不刷**——它们按点赞 / 回复 / 互动排序，本就包含已下载帖，重拉只会得到相同数据，还会打断滚动位置与 NEW 标。新增卡时按同一句话判一遍，别默认「都刷一遍最保险」。
    - **列表页同理**：帖子浏览页在「未下载」筛选下提交下载后要重刷当前页（该行已退出列表），本页被刷空时回退一页。
    - **不要用「轮询会在 5s 后自愈」当理由**：本项目 `enable_auto_refresh` 可能为 `false`（实测用户环境即为关闭），此时列表**永远不会**更新。
    - **本项目实例与实测**：报错现象「点行内『下载』，刚提交的帖子还停在推荐位」；修复后接口级实测（提交后 ~2s 重取，远早于 5s TTL，目标帖已消失、在途 0→1；取消 + 删目录后立刻回归）+ 浏览器级实测（Playwright 真实点击，成功提示 t+0.53s、**t+0.76s 换榜**）。详见《数据总览大屏设计与优化总览.md》§21.12（含举一反三对照表）。
22. **给既有状态机新增一个状态，必须先把「依赖该状态机」的每一处分支点列全并逐处对齐**（2026-09-15 确立，源于下载中心新增「已暂停」）：
    - **触发场景**：新增状态（或改变既有状态语义）时，最容易只改「主流程」那一处，而漏掉散落各处的判定点——它们在类型检查、编译、启动检查里**全部静默通过**，只在用户操作时暴露。
    - **强制动作（动笔前先列清单）**：列出所有「按状态分支」的位置并逐处决定是否对齐。本项目 `DownloadTaskManager` 实测清单：① 终态集合 `_TERMINAL` 与 `_load`（重启恢复口径）；② `_finalize_status`（终态判定）；③ `_execute` 的「补提交槽位」循环条件（**有两处，漏一处状态就永远生效不了**）；④ 队列令牌失效与 `prioritize`/`cancel` 的分支；⑤ 展示层排序权重、状态文案/tag 配色、筛选选项、行内按钮可见条件。**判定口诀：新增的状态是「终态」还是「非终态」，直接决定它要不要进 `_TERMINAL`、要不要写 `finished_at`、重启后是保留还是标失败。**
    - **「暂停」不能复用「取消」**：两者都是「停下来」，但取消是**终态**（未跑项置为 cancelled，只能重下）、暂停必须是**非终态**（未跑项保留 pending，可直接续传）。用取消冒充暂停会让状态列显示「已取消」却提示「继续下载」——语义自相矛盾。**新增交互前先问：用户要的是「停下来还能接着」还是「停下来就结束」？** 前者必须新建非终态。
    - **非终态在「收尾窗口」内必须禁止重新入队（本次最隐蔽的一处）**：暂停一个运行中任务后，到 worker 真正收尾之间存在窗口（在跑链接可能还要几十秒）。此时若允许重新入队，**第二个 worker 会在同一任务上并发跑第二遍**——与「进行中任务禁止重跑」同一风险的另一个入口，必须用显式标记（本项目 `pause_requested`）挡掉并在 worker 收尾的最后一步才清除。
    - **同一动作的「单条入口」与「批量入口」必须复用同一实现**：本项目行内「下载」= 只传 1 个 ID 的 `batch-start`，因此两条入口的语义、提示、边界守卫天然一致；禁止为批量另写一套（否则「单条能续跑、批量却重下」这类漂移迟早出现）。同时抽掉旧实现（`retry()` 的重置逻辑并入 `_requeue_locked()`），不留两处近似逻辑。
    - **状态立即变更 vs 等真实收尾**：HTTP 请求发出后无法中断，故「暂停」应**立即**改状态（否则大文件要等几分钟才看到反馈，用户会以为点错了），并用「正在收尾」的标记挡住此期间的续跑；**反馈必须真实**，不能既说「已暂停」又允许它继续跑。
    - **非终态收尾时必须自查「还有没有剩余工作」**（2026-09-16 复核新增）：暂停/中断类状态的收尾分支不能只看「有没有收到请求」，还要看「还剩不剩活」。漏了这一问会留下**悬空状态**——状态是「已暂停」却已无任何未跑项、`finished_at` 为空，用户必须再点一次「开始下载」才能让它变成 已完成/失败（本项目 2 链接任务实测：停在 `paused` + `fail:2`）。判定方式：收尾后仍为 `pending` 的项就是当初没提交出去的那批，有则进非终态、无则直接按明细判终态。
    - **同一个用户可见入口，在所有可进入状态下的口径必须一致**（2026-09-16 复核新增）：界面上「开始下载」是一个按钮，那么它对「已暂停 / 失败 / 已取消」任务必须有**同一种**行为。本项目初版让「暂停只续跑、失败才重跑」，结果同一批勾选里同一动作两种结果（`links=1` vs `links=3`），且暂停任务永远收敛不到完成态。「继续」与「重跑」的区分只有实现者看得见，对用户就是行为不一致——要么统一口径，要么给两个按钮。
    - **同一实体的并发执行要用显式标记硬挡，不能只靠「状态检查 + 队列令牌」**（2026-09-16 复核新增）：两类真实漏洞——① 令牌校验与真正执行之间**必须释放锁**（否则死锁），这个窗口里状态可能被改（本项目：点了暂停，任务照样跑完）；② 另一个入口把**同一实体**重新入队（本项目：任务运行中对某条失败链接点「重新下载」会把整个任务重新排队 → 两个 worker 并发跑同一任务 → 重复下载 + 计数翻倍，任务并发数 ≥ 2 时可达）。做法：执行入口拆成「持锁守卫 + 内部实现」两层，用一个进程内标记（本项目 `_executing`，`try/finally` 清理、加载时复位）硬挡重复执行，并让所有会重新入队的入口共享同一守卫（含前端置灰，避免用户点了才报错）。
    - **本项目实例**：用户要求「重试」更名「下载」+ 新增「开始下载 / 全部暂停」（有在途任务时按钮自动切换）。落地含 2 个新接口、1 个新非终态、前端自持勾选集合与动态主按钮；接口级 16 项 + 浏览器级 12 项（桌面/移动双视口）实测全通过（§11.15）。次日用户要求复核逻辑，按要求做对抗性复核后确认并修复 3 处问题（悬空 `paused`、口径不一致、并发执行竞态），复测接口级 19 项 + 浏览器级 10 项全通过（§11.16）。
23. **真实链路实测必须与真实数据隔离，且「造状态」的时机要对**（2026-09-15 确立，源于下载中心实测踩坑）：
    - **触发场景**：按第 15 条要求做真实端到端实测时，测试会真的写持久化文件、真的改回收站 / 任务列表——直接打真实实例会污染用户数据。
    - **强制动作**：① 用**独立端口 + 临时持久化文件**起一个测试实例（本项目：`TXXY_WEB_PORT=8089` + `TXXY_DOWNLOAD_TASKS_FILE`/`TXXY_DOWNLOAD_HISTORY_FILE` 指向 `outputs/_tmp_*.json`），前端构建产物共用、数据完全隔离；② 构造**零副作用**的测试输入（本项目用 `http://127.0.0.1:1/...`：连接必然被拒、任务真实进入「下载中 / 失败」但不落任何文件）；③ 测完杀掉测试实例、删除临时文件，并**核对真实数据文件未被污染**（本项目实测 `tasks_has_test_url=False` / `history_has_test_url=False`，任务数 98 未变）。
    - **环境变量不跨命令留存**：在 `execute_command` 里设置的环境变量**下一次调用不保证还在**（本项目实测丢失，导致测试实例一度读写了真实任务文件）。**启动命令必须把端口与数据文件路径写在同一条命令里**，不要依赖上一条命令的 export。
    - **造「正在运行」的测试状态必须在页面打开之后**：浏览器冷启动要几秒，短任务这时已经跑完——页面渲染出来就没有在途任务，断言必然假失败（本项目第一版脚本即栽在这里）。**先开页面，再造状态，再断言。**
    - **断言以 API 实际状态为准**：验证 UI 联动时不要看截图猜（本项目截图分辨率下「失败」与「已暂停」标签难以分辨，一度怀疑有 bug，定点核验后证明是误读）；也不要靠 `page.wait_for_load_state('networkidle')`——本页有 SSE 长连接，它永不触发，必须改用元素等待。
24. **定时/后台调度类能力必须同时满足六条硬约束**（2026-09-16 确立，源于「页面上配置定时抓取」落地）：
    - **触发判据必须幂等**：判据是「(计划时刻, 日期) 是否已处理过」并**落盘**，绝不是「现在几点到了没」——后者在重启、多 tick、时钟回拨时会重复触发（APScheduler 的 JobStore、Airflow 的 DagRun 唯一性都是这个思路）。本项目：`outputs/scrape_schedule_state.json` 的 `fired` 表。
    - **错过执行要显式定策略并在页面可见**：cron 的语义是「进程不在就不跑」，systemd timer 要 `Persistent=true` 才补，Airflow 用 `catchup` 开关。本项目取 `skip`：晚于计划时刻超过容差（10 分钟）即记「未执行（当时服务未运行）」到状态与页面，**不补跑**（抓取是当日数据窗口任务，补陈年批次无意义）。
    - **必须复用唯一执行入口，并把它的防重窗口补上**：调度只允许调用既有入口（本项目 `runs.start_run`，防重/pid/日志都在里面），禁止另起 subprocess；同时注意既有防重可能只看「库里的 running 记录」，而**子进程写入该记录之前有几秒空窗**——必须同时判 pid 文件（`active_pid()`），否则调度与手动触发会各起一个批次。
    - **同一时刻只能有一处调度**：OS 计划任务 / 容器 cron / 应用内调度三者只能启用一个，否则同一天抓两次（双倍请求源站）。本项目实况：Windows `txxy_daily_batch` 与容器 `cron` 均已停用，只保留页面调度；页面上写明了这条互斥。
    - **调度线程必须「打不死」且可观测**：单次判定异常只记日志、线程继续（线程一旦退出，定时能力会**静默失效**——用户以为在跑其实没有）；状态里给出心跳（本项目 `last_tick`）供页面确认调度活着。
    - **「下次执行」必须与触发判定同源计算**：两处各算一套必然漂移。本项目实现过程中两种错都踩过——① 只找「已到点」的时刻，08:05 时直接显示明天 08:00（漏了今天 20:00）；② 把「早已过容差、接下来只会被记为未执行」的历史时刻显示成「下次执行」，页面出现「下次执行 00:00」这种过去的时刻。最终口径：**下次执行 = 今天最早一个「还会真的跑起来」的时刻（未到点，或刚过点仍在容差内），没有则明天第一个**。
    - **触发入口必须原子，执行侧还必须有 Job 级单实例锁**（2026-09-16 用户上报「连点两次跑了两个批次」后确立）：
      ① **服务端串行化**：把「检查 → 拉起进程 → 落 pid 文件」整体放进同一把锁。只做「检查」不做锁，两次**并发**触发会**双双通过检查**（第一个批次的进程还没写运行记录、pid 文件也还没落）——真实发生过；**不要指望前端按钮的 loading 态**，那只是体验，不是互斥。
      ② **执行侧兜底**：能被启动的入口不止页面（控制台、计划任务、容器 cron），因此脚本自身要用**进程序列化**——本项目用 **OS 级文件锁**（Windows `msvcrt.locking` / POSIX `fcntl.flock`，非阻塞）在**动共享资源之前**抢占 `outputs/run_batch.lock`，抢不到就打印「跳过」并退出。两个要点：**用 OS 锁而不是「pid + 存活检查」**（进程被强杀时 OS 自动释放，无残留锁，也不依赖 psutil）；**锁住的范围要避开内容字节**（Windows 字节范围锁是强制锁，锁住内容会让排查时连读都读不了），且**持有者信息必须在抢到锁之后才写**（抢失败方绝不能改这个文件，否则会把持有者覆盖成自己——实测踩到）。
      **为什么后果严重**：两个批次会互相抢外部共享资源。本项目里每个批次启动时都会「按端口定位强制结束占用进程」接管 1024 镜像，于是两批共 13 个版块里 7 个因「目标机器积极拒绝」全失败（实测 ID=135/136 两条同日记录）。
    - **调度类功能的页面状态必须包含「正在运行」并自行刷新**（同时确立）：只显示「下次执行 / 上次结果」是不够的——用户点了「立即运行一次」后页面毫无变化，会以为按钮没生效。做法：状态接口返回 `running`（本项目区分 `state=running`（有运行记录，带批次 #id 与已运行分钟）与 `state=starting`（进程已拉起、记录未写））；**按钮可用性与后端守卫同源**（运行中直接禁用并给出原因悬浮，而不是点了才报 409）；页面按 5 秒轮询（不可见时暂停）——长任务（25~40 分钟）的状态必须自己刷新，不能指望用户手动刷新页面。业界参照：Windows 任务计划程序的 Status=Running、Jenkins 的进行中蓝球、Airflow 的 DagRun 状态。
    - **本项目实例**：设置页新增「定时抓取」组（开关 / 时刻列表 / `--restart` / 镜像开关 + 只读状态区 + 立即运行一次），默认 08:00 与 20:00 每天两次；落地含新模块 `web/scheduler.py`、5 个白名单参数（新增 `times` 类型）、`GET /api/schedule`、`config._env_bool/normalize_times` 两个收敛点。实测：判定逻辑 14 项（打桩执行器，不真抓站）+ 真实服务端到端 10 项 + 浏览器 12 项全通过。上线当天按用户上报复现「两个批次」并补上原子触发 + `run_batch` 单实例锁 + 运行中状态（实测 12 项判定/互斥 + 9 项真实服务与浏览器）。详见《定时抓取调度调研与建议.md》（含业界对比、第八节实施记录与 8.6 事故复盘）。



25. **界面问题先用「度量」定位再改样式；会增长的列表从一开始就配齐「搜索 + 分页 + 计数」**（2026-09-16 确立，源于「演示轮播卡片错位」与「黑名单太长」）：
    - **「看起来错位」不要凭肉眼改 CSS**：先用 `bounding_box()` / `getBoundingClientRect()` 把相关元素的 x/y/宽高量出来，错在哪一目了然（本项目实测同一行内**标签列 132px、控件 1002px**——控件被「默认：A → B → …」长句撑开了固有宽度）；改完用**同一份度量复验**（132 → 1150），而不是再截一张图看看。
    - **一行放不下的控件不要硬塞进左右分栏**：「标签｜控件」布局里，若控件固有宽度接近或超过行宽（经验阈值 > ~40%），该行改为**堆叠**（标签在上、控件在下、控件占满整行），只让紧凑控件（开关/数字/下拉/短文本）保持同排——与 Element Plus / Ant Design 表单「复杂控件独占一行」一致。判定按**控件类型**（多选组、列表编辑器）而不是屏幕宽度，这样桌面与移动端行为一致、无需两套规则。
    - **会增长的列表从一开始就要有「搜索 + 分页 + 计数」**：管理型列表（黑名单、任务、资源）条目只会变多，等长到影响使用再补就晚了。默认做法：**前端切片分页**（数据本就在内存，与既有页面同口径）+ 搜索（**把用户可能用来找它的字段都纳入**，如本项目把「备注」也算进黑名单搜索）+ 计数（`共 N 条`，筛选时追加 `筛选出 M 条`）；筛选变化回第 1 页、条目减少时页码**收敛到最后一页**（防停在空页）。不选无限滚动（管理场景要「共多少条、在第几页」的确定性）、不选纯滚动容器（只是换了个地方滚）。
    - **前端分页必须写清升级触发条件**：全量返回 + 前端切片只在「几十条」量级成立；条目进入数百条以上应改**服务端分页**（`?page=&size=&q=`，分页控件与计数展示无需改动），并把这个阈值写进文档——否则会长期停在「能跑但越来越慢」的状态。
    - **本项目实例**：设置页「演示轮播」错位（宽控件行改堆叠，标签列 132px→1150px）与「链接黑名单」搜索+分页（每页 5 条、共 17 条实测 4 页）同期落地，15 项浏览器断言（桌面 + 移动）全通过。详见《参数设置页调研与建议.md》§十。


26. **「只在本机可访问的服务」要让局域网其它设备用上，必须由本机进程做同源中继；链接基址必须按「谁最终发这个请求」分别派生**（2026-09-17 确立，源于用户问「手机怎么才能打开 http://127.0.0.1:1024/...」）：
    - **触发场景**：需要让手机 / 局域网其它设备打开一个只在本机可达的资源（本项目：本地镜像 `web.exe` 提供的站点页面）。最容易犯的错是把「用 `127.0.0.1:1024` 访问它」当成「它只绑回环」的证据或反证——**这是两件事**；也最容易顺手选「把端口暴露到局域网」（改绑定 / `netsh portproxy` + 防火墙），那需要管理员权限、每台机器重做一遍、且失败点（绑定没改成功 / 规则没生效 / IP 变了）散在环境里，用户看到的是「手机打不开」而查不到原因。
    - **强制动作（动手前先量）**：① 先实测监听地址（`netstat -ano | findstr :<port>` / `Get-NetTCPConnection -LocalPort <port>`），再决定方案；**绑定地址改不动时（第三方 exe / 无配置文件），正解是「本机进程做同源中继」**——浏览器只访问同源路径，由服务端转发到回环，从而与访问方式（本机 / 局域网 IP / 未来域名）解耦。本项目实例：`netstat` 实测 `TCP 127.0.0.1:1024 LISTENING`，安装目录只有 `web.exe`（无可改绑定的配置），故落地 `web/mirror.py` 的 `/mirror/{path}`。
    - **链接基址必须按消费方派生，禁止用「服务端进程级常量」统一下发**。判据一句话：**谁最终发这个请求？** ① 浏览器打开的 → 同源中继相对路径（`/mirror/...`，任何访问方式自适应）；② 服务端自己发请求的（下载中心提交、抓取）→ 本机回环地址（对服务端有效）；③ 粘到别处 / 分享的 → 同源绝对地址；④ **离开本机环境的产物（CSV 导出）→ 不带任何域名，只存相对路径**（`txxy_env.to_storage_path`）：导出物会在未知环境 / 设备上被打开，**带任何域名都会有一类环境失效**——带本机镜像地址在手机/异地打不开，带业务域名在「只能靠镜像上网的桌面」打不开；相对路径与库内、采集端 CSV 同一形态，由使用者按环境拼前缀最稳（2026-09-17 按用户口径收敛，中途曾按业务域名实现一版，用户驳回：**"按现有链接导出即可"**）。
    - **中继必须自带降级，且上游必须写死**：连不上回环服务 / 上游 5xx → 302 到公开域名同一路径（本项目需求即「1024 不行时用 PUBLIC_DOMAIN」）；上游地址只能取自唯一配置源（`txxy_env.LOCAL_PROXY`），**绝不接受调用方传入地址**，否则等于开了任意转发器（SSRF）。
    - **HTML 重写要覆盖两类链接，只做静态属性会留下「看得见点不通」的链接**：① 静态属性（`href/src/action="/x"`）；② **页面 JS 运行时生成的**（本项目实测相关帖 `a[href="/htm_data/..."]` 由脚本注入，服务端重写看不到），后者用「点击瞬间兜底脚本」改写到中继前缀。判据不是我重写了多少条，而是**页面上每一条可点链接点下去都落到中继**。
    - **降级 / 中继类功能必须两条链路都实测，并用可控上游逐项验证透传**：上游可用链路 + 上游不可用链路（本项目：真实镜像的 8089 隔离实例 + `TXXY_LOCAL_PROXY=http://127.0.0.1:1` 的死上游实例 + 置空表示「本环境无镜像」），加上一个可控上游（临时 20 行 `http.server`）逐项验证 Range(206/Content-Range)、gzip 原样透传、多条 Set-Cookie、上游 302 的 Location 改写、5xx 降级。**透传的坑**：`requests.iter_content` 默认会解压响应体，若同时透传 `Content-Encoding`/`Content-Length` 就会让浏览器解析失败——必须走 `raw.read`（本项目 `_iter_raw`）；上游不支持 HEAD 时（本项目 `web.exe` 对 HEAD 返回 404）要改用上游 GET 再按 HEAD 语义只回头。
    - **共享后台服务（本地镜像）必须「单一 owner」：谁启动谁关闭，退出关停前必须确认没有别的消费者在跑**（2026-09-17 加，源于把镜像也纳入看板启动器）：本项目 `web.exe` 有两个潜在使用者——抓取批次（`run_batch`）与看板启动器（`start_web`）。规则：① 端口已在监听 → 后来者 `ensure_*()` 返回 None，只消费、绝不接管；② 退出时只关「本次启动的」（`shutdown_*` 拿到 None 即跳过）；③ **关停前必须判「有没有别的消费者在用」**：看板退出时要复用批次单实例锁（`run_batch.acquire_single_instance_lock`，抢不到=批次在跑）决定是否保留，否则会把批次正在用的镜像关掉、整批请求失败（与 2026-09-16「互相抢镜像」同类事故）；锁要持有到关闭完成再释放。④ 两个使用者的失败口径可以不同且应当不同：**抓取端 fail-fast**（没镜像抓不了站 → 终止本次批次），**看板端 warn-only**（镜像只是加速项 → 照常启动，链接自动降级到业务域名）。绝不能让可选依赖拦住主服务启动。
    - **atexit 处理函数里禁止 import 新模块**（2026-09-17 实测踩到，极易漏）：解释器关闭阶段不能再注册 atexit 钩子，若被 import 的模块在导入期注册了钩子（本项目 `run_batch` 的导入链即如此），会抛 `RuntimeError: can't register atexit after shutdown` → 后果是「退出时镜像没关掉、web.exe 残留在 1024 上」，且因为异常被 except 包住，只留一行日志。正解：把依赖模块在**启动期预加载**（本项目 `_preload_batch_lock()`，在启动镜像时一并加载），退出时只用已加载的模块引用；无法预加载时按「保守保留」处理。
    - **启动长期驻留服务必须用「干净环境」，并且别用助手的 shell 直接拉起用户的服务**（2026-09-17 实测，事故级）：IDE / 编辑器工具常把 shim 目录塞进 `PYTHONPATH`，其中的 `sitecustomize.py`（Python 启动时自动导入）会拦截文件操作。本项目实测：某 IDE 注入的「安全删除守卫」在 `POST /api/resources/delete` 里抛 `SystemExit(1)`，而 **uvicorn 只兜 `Exception`**，于是该异常穿透整个栈把看板进程带走——用户看到的是「删除报 500 + 服务直接退出」，日志里连 uvicorn 的 `Shutting down` 都没有（极易误判成「用户自己 Ctrl+C 了」）。**两个必须做的动作**：① 启动器入口清空 `PYTHONPATH`（本项目 `start_web.bat` 里 `set "PYTHONPATH="`，`setlocal` 范围内；`start_web.py` 启动时检测外部 `sitecustomize` 并告警 + 从 env 摘掉 `PYTHONPATH` 供子进程）；② **AI 助手的执行环境自带该变量**，所以「由助手 `Start-Process` 拉起用户服务」= 把 shim 注入用户服务（本次事故就是这么来的）——验证类重启也要显式清环境，或干脆让用户自己双击启动。诊断口径：`curl /api/health` 正常但某写接口 500、且日志无 uvicorn 关闭记录 → 先查是否被外部 shim 打断（对比 `python -c "import sys; print('sitecustomize' in sys.modules)"`）。
    - **日志根目录必须绝对化（相对路径 + 中途 `chdir` = 日志写进「谁也没预期的目录」）**（2026-09-17 实测）：`file_logger.LOG_ROOT` 原为相对 `"outputs"`，而 `start_web.py` 在加载 `web/app.py` 前会 `os.chdir(web/)`，于是看板日志落进 `web/outputs/<日期>/`——排查「看板为什么退出」时在项目根 `outputs/` 里**找不到当次会话日志**（本次因此多绕了一圈；定位手段：全盘 grep 报错原文找落点）。已改为基于 `file_logger.py` 自身位置的绝对路径。**通用规则：凡「按 CWD 解析」的路径都要检查调用方是否会 chdir**。
    - **改 `.bat` 的三条硬约束**（2026-09-17 实测，`start_web.bat` 上连踩/确认三处）：① **必须保持 CRLF**：整份文件行尾变 LF 后 cmd 会成片解析失败（实测报一堆 `'skip'/'t'/'arse' is not recognized`）；② **中文不要出现在 `set` / `if` 的引号里**：系统 ANSI 码页（本项目 ACP=936）读行尾时会吞掉后续字节，可能把 `set "X=中文"` 的引号吃掉，实测报 `'ROR_DESC' is not recognized`，值也错——中文只放在 `echo` 行；③ **非 `echo` 行不要以全角标点结尾**（`，。：` 等末字节在 ANSI 下会吞掉 CR，把下一行并进本行命令，实测 `REM ...拉起，` + 下一行 → `'下第二个命令窗口或孤立进程。' is not recognized`，且该问题在改动前的 HEAD 版本就存在——顺手把这两行 REM 改成 ASCII 结尾）。改完必须**在真实控制台**跑一遍并检查输出里有无 `is not recognized`（用隐藏窗口 + 重定向 stdout 验证是不可靠的：`chcp` 效果受影响，会给出假失败）。
    - **本项目实例与实测证据（2026-09-17）**：新增 `web/mirror.py`（同源中继）+ `web/config.py` 的 `MIRROR_PREFIX/MIRROR_UPSTREAM/PUBLIC_DOMAIN` + `web/app.py` 注册（必须早于 SPA 兜底路由）+ 前端唯一实现 `utils/postUrl.ts`（`postOpenUrl` / `postCopyUrl`）替换 4 处入口（PostsView 打开与复制、DashboardView 榜单、ResourcesView 原帖）+ Vite dev 代理 `/mirror`；同日把 1024 端口守护从 `run_batch.py` 抽为 `mirror_service.py`，`start_web.py`/`start_web.bat` 新增 `--mirror/--no-mirror`（默认确保镜像可用）。实测：真实镜像比对「直连页 vs 中继页」根相对属性全部改写到中继、页面不泄漏 `127.0.0.1:1024`；机制 9 项（请求头 / Range / Location / Set-Cookie / 5xx 降级 / gzip / HTML 重写 / 死上游降级 / 4xx 透传）全通过；降级两实例（未配置镜像、死上游）路径与查询串均正确；浏览器级桌面 1440 + 移动 390 双视口真实点击（含以局域网 IP 模拟手机：非安全上下文走 execCommand 复制降级），打开 URL 为 `/mirror/htm_data/...`（HTTP 200）、剪贴板为同源绝对地址、**浏览器全程未请求过 `127.0.0.1:1024`**；JS 运行时生成的链接点击后同样落到 `/mirror/...`（200）；CSV 导出实测 355 行「链接」列全为相对路径 `/htm_data/...`（0 行含 `127.0.0.1:1024`、0 行含 `txxy.com`），同时列表接口仍返回展示域名 `http://127.0.0.1:1024/...`（下载中心在服务端下载、前端取相对路径都依赖它，不能一起改）。看板启动器的镜像托管实测（2026-09-17，隔离端口 + 真实 web.exe）：`ensure` 真实拉起 web.exe 并等就绪、重复 `ensure` 返回 None（只消费不接管）、批次锁被别的进程占用时退出保护拒绝关闭（镜像留在端口上）、批次结束后真正关闭（走「按端口强杀」兜底：引导 PID 与实际监听 PID 不同）、atexit 接线（子进程启动镜像后正常退出 → 端口释放，修复前这里是 False）、`--no-mirror`/`--mirror` 与 build 参数任意组合解析正确；`start_web.bat` 在真实控制台的探针运行无任何 `is not recognized`，banner 显示 `1024镜像: off (--no-mirror)`；最终用 `start_web.bat`（默认）重启后 8088 / 8090 / 1024 三端口齐备且稳定，`/mirror/htm_data/...` 返回 200（真镜像，7 处已重写链接）。文档索引：`docs/帖子链接中继转发知识点.md`（**本条规则的完整知识点与实测记录，首查这里**）、`README.md`（Web 服务说明）、`docs/项目结构介绍.md`（展示层）、`docs/Docker部署使用手册.md` §7.3（域名与链接分层）、`docs/参数设置页调研与建议.md` §十一（为何 `TXXY_PUBLIC_DOMAIN` 不进设置页）。

27. **批量主操作按钮必须「默认可达」且移动端与桌面端选择控件同权**（2026-09-18 确立，源于用户上报「手机进下载中心看不到全选、开始下载不可用」）：
    - **触发场景**：页面有「作用在勾选集合上的批量按钮」（开始下载 / 全部暂停 / 批量删除等），且存在「无需用户先勾选就有一个天然全集」的可操作对象（如全部未完成任务、全部命中文件）。
    - **强制动作（对齐迅雷等成熟下载器）**：① 主按钮在「存在可操作全集」时必须**默认可用**，文案体现「全部 X」语义（本项目下载中心：无在途但有未完成任务 → 「全部开始」，不必先勾选）；勾选只用来「**收窄作用域**」（只动勾选的），**不是解锁按钮的开关**——旧设计把「开始下载」默认禁用、要靠逐个勾选才点亮，而手机端又无全选控件，导致用户进来看不到可点的开始按钮，与「有在途即全部暂停（无需勾选）」不对称。② 主按钮文案随作用域动态变化并露出影响数量（「全部开始(N)」/「开始下载(N)」），让用户不必点下去才知道本次作用范围；禁用态只在「真的没有任何可操作对象」时出现，并给一句话说明原因，而非静默禁用（手机端 tooltip 不随点击显示，更要把口径写进可见文案）。③ 移动端（卡片形态）必须提供与桌面端表头勾选框**同源、同作用域**的全选控件：桌面靠 `el-table` 表头复选框，移动端卡片要在卡片区顶部补「全选本页」；两者复用同一选中集合与同一 `toggleSelectPage` / 作用域口径，避免出现「桌面能全选、手机只能一个个勾」的能力缺口。
    - **举一反三（已排查）**：ResourcesView 的「全选当前结果」本就桌面/移动共用（无需改）；TrashView 仅行内「恢复/彻底删除」、无批量勾选（无此问题）；其余含 `:disabled` 的批量入口（PostsView / RunsView / SettingsView）按①重新核对「禁用是否仅因没先勾选、是否缺移动端全选」。
    - **本项目实例**：下载中心主按钮原实现 `primaryDisabled = 非暂停模式 && 勾选的未完成任务数===0`，手机端无全选时完全无法启动批量下载；改为「开始模式也支持无勾选默认全部开始、有勾选收窄」，并补手机端「全选本页」（作用域=当前页，与桌面表头勾选框一致）。落地见 `web/frontend/src/views/DownloadsView.vue` 的 `startTargets` / `primaryLabel` / `primaryDisabled` / `primaryTip` 及移动端 `.tc-select-all`。

28. **顶部统计卡 / 汇总计数必须覆盖状态机的「全部状态」，否则「总数」与「分类之和」对不上**（2026-09-18 确立，源于用户上报「下载中心任务总数 100，进行中0+已完成99+失败0=99」）：
    - **触发场景**：页面顶部有一组「总数 + 按状态分类」的统计卡（或任意「总和 = Σ 分类」的展示），而分类卡只列了状态机的**部分**状态。本项目下载中心后端任务共 6 种状态（`pending`/`running`/`paused`/`done`/`failed`/`cancelled`，见 `web/download_tasks.py` 的 `_TERMINAL` 与状态机），但原顶部卡只统计了 4 种（进行中=running+pending、已完成=done、失败=failed），漏掉了 `paused`(已暂停) 与 `cancelled`(已取消)；于是「任务总数 100 = 分卡之和 99」永远差那 1 个落在漏统状态里的任务，用户一眼判为 bug（实际是显示口径不全，数据本身未损坏）。
    - **强制动作**：凡出现「总数 + 分类计数」并排的卡片，分类项必须**穷举状态机的每一个状态**，使 `总数 == Σ(各分类计数)` 恒成立。新增一个状态（或改某状态语义）时，除第 22 条要求的「逐处分支对齐」外，必须同步核对「这组统计卡有没有把它算进去」——统计卡是状态机的又一处分支点，漏接就会复现「100 != 99」。分类项过多（≥6）导致一行排不下时，按栅格换行（本项目 3 列 → 2 行 3 列）而非砍掉某些状态。
    - **图标同理（前端）**：模板里 `<el-icon><Xxx /></el-icon>` 用的图标必须在 `web/frontend/src/main.ts` 的按需注册列表里显式列出（全量注册会增大主包），否则该图标静默不渲染；本项目本次补「已暂停」卡即新增注册了 `Warning`。
    - **本项目实例**：下载中心补「已暂停」「已取消」两张卡（`pausedCount` / `cancelledCount` 两个 computed 本就存在，无需新增逻辑），6 卡正好对应 6 状态；`vue-tsc --noEmit` 与 `read_lints` 0 错误。

29. **共用产物目录里判定「某类发生过」必须按文件语义取证，不能用「目录存在 / 目录里有任意文件」当证据**（2026-09-19 确立，源于用户上报「没跑批次，运行记录却出现 scraper 单跑异常」）：
    - **触发场景**：某个业务的存在性靠扫文件系统判定（如「今天有没有跑过抓取」= `outputs/<日期>/` 里有没有产物），而该目录是**多个服务共用**的。本项目 `outputs/<日期>/` 既有抓取产物（`run_batch_*.log` / `scraper_*.log` / CSV / 进度），也有 `file_logger` 给任何进程双写的会话日志（`web_*.log` / `share_*.log` / `download_files_*.log`）——只要当天开过 Web / share 服务，目录必然存在且必然有 `.log`。
    - **强制动作**：① 判据必须限定到**该类业务的产物语义**（文件名前缀 / 特定内容），不是「目录里有任何文件」；② **没有证据就承认没有**——返回空列表 / `None` / 404，**禁止拼一个「空明细 + 默认状态」的壳**（本项目旧逻辑：无 `run_batch_*.log` 就回退 `_parse_scraper_logs`，没 scraper 日志时返回「0 版块 + status=error」的伪记录）；③ 凡是**以「列表首条」为准**的下游（本项目 `/api/stats/health` 的「最近批次」= `runs.list_runs()[0]`）必须跟着一起验证——列表被污染，下游全部跟着错，且错得离「病历簿」很远（健康条直接报「最近批次异常结束」）。
    - **修完后要往回看一层**：别只盯 `list`，同模块的 `detail` 通常躺着同一套拼壳逻辑（本项目 `get_run_detail(date)` 与 `list_runs()` 同源，只改 list 会让详情接口继续返回 200 + 空明细）。
    - **本项目实例与实测**：`web/runs.py` 的 `list_date_dirs()` 旧判定「目录里有任意 `.log` 即一次运行」→ 只开 Web + share 的日子被判成一次抓取 → 无 `run_batch` 日志 → 回退 scraper 解析得到空明细 → 列表首条出现 `source=scraper / status=error / 13 项全 0` 的伪记录，前端显示为「scraper 单跑」异常，同时健康条被污染为 `warn`。修复：新增 `_has_scrape_log()`（只认 `run_batch_*.log` / `scraper_*.log`），`get_run_detail()` 无证据时返回 `None`，详情路由转 404。8089 隔离实例对照实测：修复前 `/api/runs` 119 条且首条为上述伪记录、`/api/runs/20260919` 返回 200 空明细、`/api/stats/health` 为 `warn`「最近批次异常结束」；修复后 118 条、首条恢复为真实批次 `#143`（2026-09-18 ok 13/13）、日期详情 404、健康条 `ok`。文档索引：`docs/项目结构介绍.md`（`/api/runs/{date_str}` 接口口径）。
30. **前端图表/布局的浏览器实测有三个必踩时机与判据坑，断言前先自查**（2026-09-19 确立，源于趋势空窗标注与热门榜卡头对齐实测）：
    - **触发场景**：用 Playwright 等真实浏览器验证「渲染结果 / 几何对齐 / 只有特定数据才出现的图形特征」。
    - **坑一：测量时机必须在数据渲染完成后**。`v-if` 挂在数据上的元素（如下拉控件 `v-if="todayTop?.date"`）在数据到达前不渲染，此时量高度得到的是**中间态**（本项目热门榜卡头实测 21px，稳态 57px），据其判断「对齐修复无效」即误判。强制动作：断言前先 `wait_for_selector` 等到**参与布局的数据驱动元素**出现，再测量。
    - **坑二：真实数据不触发的图形特征，用 route 拦截注入造数据做端到端实测**，不要退化为「等价脚本复写算法」。本项目 365 天窗口内无中段 0 值日，markArea 空窗带在生产上永不渲染——用 `page.route` 拦截 `/api/stats/trend` 把响应中段 3 天置 0 即可测**真实部署代码**。注意**注入日期必须从被拦截的实际响应里动态选**：前端默认窗口是 `days=7`（曾按 30 天窗口算好日期注入，全部落在窗外，注入组与对照组一模一样还查不出原因）。
    - **坑三：canvas 像素判据要按透明画布算**。ECharts 画布背景透明，`markArea` 的 `rgba(144,147,153,0.13)` 在 `getImageData` 里是 **(144,147,153, α≈33) 本体色低 alpha**，不是叠白底后的 (240,241,242)——按后者写判据必然 0 检出（本项目第一版即假失败）。判据：色值 ±10 + `8 < alpha < 90`，再按列统计找连续段；同时跑**不注入的对照组**排除网格线/axisPointer 误报。
    - **本项目实例**：空窗带注入实测 2/2 PASS（两趋势图各 160 列连续灰带、对照 0 列）；卡头对齐在等待 `.el-select` 渲染后实测 [57,57,57,57]。落地记录见《数据总览大屏设计与优化总览.md》§22。
31. **诊断「配置类行为异常」必须查完整配置优先级链；检查进程与服务进程的环境不同步**（2026-09-19 确立，源于用户上报「数据总览抓取进度不实时更新」）：
    - **触发场景**：某功能「该动不动 / 该显示不显示」，代码逻辑排查无果时——优先怀疑配置被某一层覆盖，而不是继续读代码。
    - **强制动作**：按本项目配置优先级链**逐层取证**：`outputs/web_settings.json`（页内设置覆盖）→ `.env`（显式环境覆盖，**必须直接读文件内容**）→ 代码默认值（`web/config.py`）。禁止只查其中一层就下结论；三层都拿到实证后再定位覆盖源。
    - **关键坑：裸 `python -c "os.environ.get(...)"` 查不到 `.env` 的值**——服务进程启动时经 `txxy_env.load_dotenv()` 加载了 `.env`，而临时检查进程没有加载；两边环境不同步会造成「环境变量是 None、但服务行为像设了 0」的假象（本次实测即如此，一度误判为代码自相矛盾）。
    - **改配置默认值必须同步排查所有写死旧口径的位置**：`.env` 显式值、模块 docstring、代码注释、`README.md`——本次一处行为异常背后有四处脱节（`.env` 遗留 `TXXY_ENABLE_AUTO_REFRESH=0` 覆盖代码默认 True；`config.py` docstring 仍写「默认 0/关闭」；`dashboard.ts` 注释写「默认关闭 / 30 秒」；`DashboardView.vue` 注释写「每 30 秒」而实际 `REFRESH_INTERVAL=5000`）。
    - **本项目实例**：`.env` 遗留值 → `/api/config` 下发 `enable_auto_refresh=false` → 前端隐藏 Header 自动刷新开关且不启动 5s 轮询 → 抓取进度徽标（`runningBatch`）与健康条停在首载快照。修复：注释掉 `.env` 该行（回落代码默认开启）+ 修正三处旧口径注释 + 重启服务；实测开关出现且默认开、13s 内 3 次 `/api/runs` 轮询、徽标与接口值一致（96=96）、数据库版块级进度 20s 内 65%→73% 证明全链路畅通。附注：批次级进度是 13 版块均值，收尾单版块每涨 13 页批次才涨 1%，短窗口徽标不动属粒度特性而非故障。
32. **确认弹窗的「取消」分支必须显式拦截，且同文件同类函数写法必须一致**（2026-09-19 确立，源于参数设置页举一反三实测）：
    - **触发场景**：任何用 `ElMessageBox.confirm` / `window.confirm` 包裹的破坏性操作（恢复默认、清空、删除…）。
    - **坑的形态**：`await confirm(...).catch(() => false)` 把取消 rejection 吞掉后**没有用返回值拦截、继续往下执行**——用户点「取消」操作照样执行，确认弹窗形同虚设。该 bug 静默通过 vue-tsc / lint / 启动检查，**只有真实点击「取消」才暴露**；更隐蔽的是点「确认」时行为完全正常，常规正向测试发现不了。
    - **正确写法（与取消语义严格配对）**：`const ok = await confirm(...).then(() => true, () => false); if (!ok) return`——取消必须 return，确认才继续。
    - **同文件同类函数必须逐个对比写法**：本项目 `SettingsView.vue` 里 `resetAll`（正确：`if (!ok) return`）与 `resetOne`（错误：`.catch` 吞掉不拦截）同页并存——发现一处写法差异时，必须把同文件所有同类确认流翻出来对比，不能假设「一个对了其它都对」。
    - **测试确认流必须双向**：只测「确认后生效」不够，必须加「取消后不生效」断言（本项目实测：取消断言直接命中此 bug）。
    - **本项目实例**：`resetOne` 点「取消」也会恢复默认。修复后浏览器级实测 9/9 PASS（含「取消后仍 1200」「确认后回落 600」双向断言）。
33. **同名指标必须确认「两处是同一量纲」；不同量纲的同类数字要分栏承载、下钻落点必须同口径**（2026-09-19 确立，源于用户上报「内容资产『下载失败 3』与下载中心『失败』对不上」）：
    - **触发场景**：同一张看板上有两处都叫「失败」（或「总数 / 完成 / 待办」等），用户期望它们对得上，但实际一个按 A 口径计、一个按 B 口径计——撞名是这次对不上的直接原因（本项目：资产卡「下载失败」= 下载履历里「最近一次失败且此后未成功」的帖/链接数；下载中心「失败」= 任务列表里 `status='failed'` 的任务数，一个失败任务可含多条失败链接，且任务会被清空/轮转移除，数字天然不等于前者）。
    - **强制动作**：① 凡出现两个**同名或近义**的计数，**动笔前先核「两者是不是同一量纲、同一数据源」**；是则统一取同一个派生值（本项目资产卡的 `failed` 与缺口清单的 `count` 都直接来自 `_asset_snapshot()["failures"]`，保证「卡上 N = 清单 N 条」）；不是则**改名区分**（本项目任务侧一律改叫「失败任务」）并**分栏/分视图承载**（本项目下载中心用分段切换「下载任务 / 失败缺口」两个视图），不要把它们塞进同一个数字里让用户去减。② **下钻落点必须与被点的数字同口径**（Grafana/Datadog 的 drill-down 一致性）：点「下载失败 N」进去必须看到 N 条，而不是另一个口径聚合出来的 M 条；若明细页无法用同一口径表达，按第 11/12 条改为悬浮说明、不做下钻。③ 文案要把量纲差异写进常驻可见处（视图切换旁一行提示「卡上『下载失败 N』= 此处 N 条」），避免用户靠猜。
    - **业界参照**：Sonarr/Radarr 的 `Wanted → Missing`（缺失清单，可逐条重试）与 `Activity`（任务历史）分栏；qBittorrent 的 `Errored` 是独立条目集合，与任务状态并列可单独筛选——都不会让「缺失清单的条数」去和「任务历史的计数」对齐。
    - **本项目实例与实测**：资产卡「下载失败」下钻带 `?view=failures` 直达下载中心「失败缺口」视图（同源同口径，4 条对 4 个）；真实点击「重新下载」一条 → 该缺口履历置 `alive`、`/api/downloads/failures` 计数 4→3 且卡面数字/清单条数/分段控件徽标三者一致；任务侧「失败任务」保留但明确标注「按任务计数」。落地见 `web/api.py: GET /api/downloads/failures`、`web/frontend/src/views/DownloadsView.vue`（分段视图 + 缺口清单）、`web/frontend/src/composables/useAssets.ts`（`onStateClick` 失败项下钻分支），设计记录见《数据总览大屏设计与优化总览.md》§25。

34. **首屏必须留给「主功能」；按内容撑开的筛选控件在窄屏必须自持横向滑动**（2026-09-19 确立，源于用户上报「资源管理排版是否合理」）：
    - **触发场景 A（概览挤占主功能）**：页顶的概览类内容（统计卡 / KPI 卡 / 洞察卡）随迭代越加越多，主功能列表被推到首屏之外——「页面能用」但用户进来第一眼看不到自己要操作的东西。
    - **强制动作 A**：交付前用**真实浏览器量取「主功能容器起点」**（`document.querySelector(主容器).getBoundingClientRect().top`）并与视口高比较，起点 ≥ 视口高即判不通过；概览类内容默认收敛为**「一行 KPI 摘要 + 详情收起（`v-show`）/抽屉」**，且摘要只放常驻统计卡**尚未承载**的数字（与第 20 条一致，别再复述一遍库存三数）。详情保留在同一页（`v-show`）而非另起一套 DOM，避免复制内容与口径。
    - **触发场景 B（窄屏筛选控件被裁）**：`el-segmented` 这类 **inline-flex、按内容撑开**的筛选控件，窄屏容器放不下时，若祖先 `overflow-x` 为 `hidden`（本项目 `.el-main` 即为 hidden）会**直接裁掉尾部选项且不可滚动**——该 bug 静默通过 `vue-tsc` / lint / 启动检查，桌面视口也完全正常，只在窄屏真实点击时暴露。
    - **强制动作 B**：这类筛选条一律包 `.filter-scroll`（**全局唯一实现，在 `style.css`**），窄屏自持横向滑动；**禁止**「窄屏另换 `el-select`」这类第二套控件（第 1 条：同一能力只能一处），页面内也不得再写一份该类样式。交付前必须用**真实点击验证最后一档可选**（不是看渲染、不是看 `scrollWidth` 的算术判断）。
    - **断点要按「谁会溢出」定，不要机械对齐布局层**：本项目布局层移动断点是 `<768px`，但 `.filter-scroll` 必须用 `max-width: 768px`——下载中心原实现即为 768px，本次抽公共样式时收回 767px，**768px（iPad 竖屏，筛条 638px / 可用 478px）立刻退回 `overflow-x: visible` 被裁**，靠回归实测才发现（同一坑第二次踩：改公共样式的断点会波及其它页面）。
    - **本项目实例与实测**：资源管理页「内容资产 + 容量洞察」两卡合计 660px → 列表卡起点 1600×900 `y=858`、1366×768 `y=880`（完全不可见）、390×844 `y=1804`（滚 2.1 屏）；收敛为一行摘要后为 `231 / 231 / 582`，展开 / 再收起往返正常，两卡高度与改前一致（290 + 364）无需重写。窄屏类型筛选 8 档宽 460px、容器 312px，「云盘清单 / 文本 / 其他」右缘 397/447/497 > 视口 390 且 `.el-main` 裁切 → 点不到；包 `.filter-scroll` 后 `clientW 312 / scrollW 460`、点末档「其他」选中生效（`scrollLeft 148`），抽屉内同款（`350/396`，`bodyScrollW` 由 416 收回 390）与下载中心 390/768/1600 三档回归全通过。文档索引：《资源管理页面优化方案.md》第三十九节、`README.md` 资源管理条目。
    - **遗留（未做，已在文档登记）**：桌面 1600 下工具栏仍折 2 行（左控件 950px + 右动作 418px > 容器 1352px）；移动端 4 张统计卡纵排占 393px；展开态仍存在「103 个目录出现 3 次」等同一事实多处表达。

35. **同一视觉单元在多个视图出现时，样式必须抽到唯一共享来源（CSS 同样适用），禁止各视图复制一份**（2026-09-19 确立，源于用户连续三轮上报「下载中心清单表格要照搬帖子浏览」）：
    - **触发场景**：同一套视觉单元（表格表头、版块标签、标题链接、操作按钮、筛选条…）出现在两个及以上视图，且被要求「保持一致」。最容易的做法是「把 A 的 CSS 复制到 B」——这正是第 1 条禁止的重复造轮子；结果是改一处漏一处，B 永远差一点，用户只能反复上报「还是不一样」。
    - **强制动作**：动笔前按第 1 条自查「这段样式别处是否已有」；有则**抽到唯一来源**（本项目口径：跨视图共用的组件级样式放全局 `web/frontend/src/style.css`，如既有的 `.filter-scroll`、本次的「帖子浏览系清单表格共用单元样式」段），两处复用同一 class；**不允许**在两个 `<el-table>` / 两个脚手架各写一份相同声明。若样式只该作用于特定一组表格而非全站，用一个**标记类**限定（本项目 `.post-table`：两处 `<el-table>` 都加该类，表头等规则写 `.post-table th .cell`，其余页面表格不受影响）。
    - **「照搬」的验收方式**：不是截图肉眼看，而是**抽取两处同名元素的 `getComputedStyle` 逐项对比**（本项目实测：标题链接 `rgb(48,49,51)` / `text-decoration: none` / `14px` PingFang SC、版块标签 `12px`/`border-radius:10px`/`padding:1px 7px`/`1px` 边框、表头 `.cell` `display:flex`+左右 `6px`、次要文字 `#909399` 三处全部一致，console 错误 0）——颜色类差异要能解释（如版块标签色随 fid 派生、操作按钮色随动作 type 不同，属预期）。
    - **本项目实例**：失败缺口 / 可重下两清单的表格单元样式曾在 `PostsView.vue` 与 `DownloadsView.vue` 各存一份（`.fid-chip`、标题链接、表头规则重复），导致视觉始终对不上。已收敛为 `style.css` 共享段 + `.post-table` 标记类，并从 `PostsView.vue` 删除 scoped 重复定义。文档索引：《数据总览大屏设计与优化总览.md》§25.8。
36. **多列 KPI 卡栅格的折行断点必须按「侧栏 + 内边距等固定开销」折算，且文字行必须锁单行**（2026-09-19 确立，源于用户上报「下载中心 6 张状态卡文字换行溢出」）：
    - **触发场景**：给页面加多列等宽卡片栅格（6 列 / 4 列）时，最容易只写 `repeat(N, 1fr)` + 一个凭感觉的窄屏断点（如照搬 768px），而漏算左侧导航（展开 212px / 折叠 64px）与内容区内边距的固定开销——视口 1200px 时实际单卡只剩 ~145px，放不下 4 个全角字（13px ≈ 52px），标签溢出 / 数值换行只在真实浏览器里暴露。
    - **强制动作**：① 断点用像素算式折算：`单卡最小宽 = 内边距 + 图标 + gap + 最长文字宽`，全行要求 = `N*单卡 + (N-1)*gap + 固定开销`，把算式写进样式注释；低于该宽度逐级折行并压缩图标/内边距/字号，不硬塞。② 卡内「标签 / 数值」行加 `white-space: nowrap`（数值配 `font-variant-numeric: tabular-nums` 防位数变化抖动）。③ 文字包裹层必须带 `min-width: 0`（复用全局 `.stat-body`）——flex/grid 子项默认 `min-width: auto`，长文字会撑破卡片而不是被压缩。
    - **批量替换防漏**：同一模板多处相同结构（如 6 张卡同款 `<div>` 包裹）用 replace_all 批量加类时，**夹有注释行的实例不会命中两行锚点**——改完后必须 grep 原模式确认归零（本次「失败任务」卡即因此漏掉，靠举一反三复查发现）。
    - **本项目实例**：下载中心 6 张卡原断点 768/520/380 下的 768-1200px 区间单卡仅 80-140px；改为 ≥1200 六列 / 767-1199 三列 / 360-559 两列（压缩图标与字号）/ ≤360 单列，Playwright 12 视口逐卡断言「未换行 + 无溢出」全通过；举一反三实测资源管理 / 回收管理 4 列卡均无同类问题（其断点 1024/640 折算后余量充足）。文档索引：《下载中心设计与实现.md》§11.17。

37. **「下载/落盘媒体到指定目录」类需求必须复用 `download_files.process_one_detail`，通过 `output_root` 改输出位置，禁止另写下载逻辑**（2026-09-22 确立，源于「自动下载」合流）：本项目图片/视频/种子下载与磁力/云盘链接清单导出已在 `download_files.process_one_detail`（经 `_process_page_impl` → `extract_*`）统一实现，并按 `sanitize_title(标题)` 分目录。新增任何「把帖子媒体沉淀/归档到某目录」的需求（如自动下载、二次归档、导出到指定盘），都必须调用同一原语并传 `output_root=<目标根目录>`——底层三处 `DOWNLOAD_ROOT` 读取均已改为「`output_root or DOWNLOAD_ROOT`」，传 `None` 时行为与下载中心完全一致（零回归）。**禁止**为沉淀/归档再写一份「取详情页 → 抽取媒体 → 下载」的逻辑（那是重复造轮子，且会与主链路漂移：磁力/云盘清单、种子下载、断点续传等都会漏）。**本项目实例**：自动下载（`web/precipitate.py`）把候选帖子作为 `kind=auto` 任务提交到下载中心队列（`download_tasks.manager.submit(urls, kind="auto")`），由下载中心统一落盘到 `downloads/`，复用全套媒体与清单能力，不另写下载逻辑、不写 `post.json`/`post.md` 元数据。详细设计见 `docs/自动下载设计与实现.md`。

38. **「向本地磁盘批量落盘」前必须判磁盘水位并设可配置停止阈值，停止信号复用既有健康条/状态 last，不要为此新建通知子系统**（2026-09-22 确立，源于「自动下载」合流）：本项目本机存媒体，盘满是头号风险。任何会向磁盘连续写入较多数据的功能（自动下载、批量下载…），落盘前必须 `shutil.disk_usage(目标目录).free` 低于阈值（默认 20GB，与资产条 `diskLow` 红色判据一致）即停止，且要**持续判**（提交每批前 + 单条落盘前都判，防止并发写满）。阈值走页内白名单 `precipitate_min_free_gb`（含上限钳制），不写死。停止后的提示**复用既有载体**：① 写进任务状态 `last.reason`（设置页可见）；② 全局「磁盘剩余」由已有的健康条/资产条负责标红——**不要为它另建「站内信」**（本项目没有该子系统，新建工程量过大且重复，属重复造轮子）。**本项目实例**：自动下载（`web/precipitate.py`）在 `run_precipitate` 提交前判 `downloads/` 所在磁盘可用空间（`_disk_free_gb`），低于 `precipitate_min_free_gb` 即停止提交，汇总返回 `disk_low/disk_free_gb/threshold_gb`，调度器 `PrecipitateJob` 据此生成「磁盘可用空间不足…已停止自动下载」文案。详细设计见 `docs/自动下载设计与实现.md` §6。

39. **库里取出的帖子相对路径 `/htm_data/...` 当作请求 URL 使用前，必须先经 `config.to_display_url()` 拼回环境自适应绝对地址**（2026-09-22 确立，源于用户上报日志 `Invalid URL '/htm_data/...': No scheme supplied`）：本项目「域名不入库」约束下，`posts.url` 恒为相对路径，而 `download_files.process_one_detail` / `requests.get` 等只要完整 `http(s)` URL（其文档与入参校验明确「相对路径不自动拼根、直接跳过/报错」）。凡把库内相对路径拿去发请求的地方，调用前必须先用 `config.to_display_url(url)`（唯一实现在 `txxy_env._with_domain`，前缀取 `display_domain()`：本机有 1024 镜像则用镜像地址、否则业务域名），与下载中心同源口径，禁止在调用点手写拼接或假定某种固定域名。**举一反三**：下载中心的提交接口（`web/api.py` 的 `downloads` POST）已强制 `http(s)` 前缀才会入队、前端恒发绝对地址（经 `postUrl.ts` 中继），故无此坑；唯一漏网的是自动沉淀 `precipitate_post`，已补 `abs_url = config.to_display_url(post["url"])`。**本项目实例**：`web/precipitate.py:148` 原 `download_files.process_one_detail(post["url"], ...)` 直接喂相对路径，触发 `No scheme supplied` 重试三次后判失败；改为先拼绝对地址后，实测 `to_display_url("/htm_data/2609/22/7433528.html")` → `http://127.0.0.1:1024/htm_data/2609/22/7433528.html`（`SCHEME_OK=True`）。

40. **手动触发类接口必须把结果同步写回与定时入口同一份调度状态（`state["last"]`），禁止只弹 toast 而让设置页状态面板陈旧**（2026-09-22 确立，源于用户要求核查「自动沉淀是否更新实时状态」）：本项目设置页「上次结果 / 磁盘告警」读的是 `scheduler.status()` → `state["last"]`，由 `handle_slot` 经 `_mark_locked` 落盘；而 `POST /precipitate/run` 原先只 `return run_precipitate()` 直接返回，**绕过了 `handle_slot`，`last` 不更新**——点「立即沉淀」只弹 `ElMessage`，状态面板与磁盘告警永远停留在上一次定时运行的值，表现为「实时状态不更新」。强制动作：定时与手动沉淀共用同一个「汇总 → (action, reason)」翻译函数（`PrecipitateJob._result_from_summary`），手动接口调用 `scheduler.scheduler.record_precipitate_run(summary)` 走 `record_now` → `_mark_locked` 落同一份 `last`；前端 `precipitateNow` 的 `finally` 本就 `loadSchedule()`，落盘后下次轮询即刷新，无需另改前端。**本项目实例**：重构后 `record_now` 写入 `last={at:当前HH:MM, reason:...}`，实测 `status()["precipitate"]["last"]["reason"]` 对常态返回「已沉淀 3 帖（共 5，跳过 2）」、对 `disk_low` 返回「磁盘可用空间不足（3.0 GB < 阈值 20 GB），已停止沉淀…」，与定时沉淀同源一致。

41. **Python 服务重启必须确认旧进程已真正退出，否则旧内存代码继续跑、新进程抢不到端口成僵尸**（2026-09-22 确立，源于用户重启服务后 `No scheme supplied` 仍出现）：Python 模块在 import 时一次性读入内存，**运行中改 `.py` 不会生效**；若「重启」只是又拉起一个 `start_web.py` 而旧进程未被 `Stop-Process` 杀掉，则①旧进程顶在 8088 上当活服务、其调度线程继续按旧代码触发沉淀（旧 bug 复现）；②新进程 `bind 8088` 失败（端口被占）却可能仍跑起调度线程，成为「绑端口失败但逻辑照跑」的僵尸。多次重启会累积多个重复 `start_web.py` / `share_server.py`（本次实测同机有 4 个 `start_web.py`、2 个 `share_server.py`，旧实例仍按修复前的 `precipitate.py` 打 `No scheme supplied`）。强制动作：重启前先 `Get-NetTCPConnection -LocalPort 8088 -State Listen` 拿占用 PID 并 `Stop-Process -Id <pid> -Force`，再 `Get-CimInstance Win32_Process -Filter "CommandLine like '%start_web.py%'"` 确认无残留后才启动；判活以「`netstat`/`Get-NetTCPConnection` 端口占用 + 进程命令行」为准，**不要只看日志有没有新输出**（僵尸进程也会打日志）。**本项目实例**：用户重启后报错仍在，排查发现 4 个 `start_web.py`、8088 被其中一个旧进程占着；杀掉全部陈旧实例、端口释放后重启，修复代码（`to_display_url` 拼域名）才真正生效。

42. **实时推送通道（SSE/轮询）禁止「每帧全量重算 + 查库」；静态字段按 key 缓存，变化检测用廉价内存签名**（2026-09-22 确立，源于用户上报「下载中心表格显示有点慢」）：本项目下载中心靠 SSE（每 500ms 内存 diff 推全量概要快照）+ 轮询兜底刷新。原实现 `downloads_events` 的循环里**无条件** `json.dumps(_attach_task_titles(manager.summary()))`——`_attach_task_titles` 对**全部任务的全部 URL** 查一次 `posts.db`（`url IN (...)` 分块），即「只要页面开着，无论有无变化每 0.5s 查一次库 + 全量序列化」，且同步查库阻塞 async 事件循环；同时前端 `applyTasks` 每帧 `tasks.value = list`（全新对象数组）导致 `el-table` 整表重建、操作列闪烁、滚动跳动。强制动作：① **静态字段按 key 缓存**：任务标题由提交 URL 决定、任务建好后基本不变，按 `task_id`（+ urls 指纹）缓存有序去重标题列表，path→title 再单独缓存（`_PATH_TITLE_CACHE` 仅缓存命中项、未收录 path 不缓存以自愈）；② **变化检测用廉价内存签名**：SSE 先算 `summary()` 的 `id:status:done:log_seq:ticket:...` 拼接/JSON 签名（不含 urls/titles 等大字段），签名不变只发心跳、变了才 `_attach_task_titles` + 序列化；③ **前端复用未变化行对象引用**：`applyTasks` 对每行用渲染相关字段签名，未变则复用上一帧同一对象引用，配合 `el-table row-key="id"` 让不变行就地 patch，消除整表重建。验证：起独立端口 + 独立任务文件临时实例（规则 23），旧代码 6s 约推 12 帧、新代码仅 3 帧（首帧 + 启动自检 1 帧 + clear 触发 1 帧），且标题反查仍正确。**本项目实例**：`web/api.py` 的 `_attach_task_titles`/`_summary_sig` 与 `web/frontend/src/views/DownloadsView.vue` 的 `applyTasks`（`taskObjById` 复用 + `row-key="id"`）。

43. **「全量磁盘扫描类统计」必须做进程内 TTL memo 且把 stat 移出持锁区（2026-09-22 确立，源于用户上报「下载中心表格信息要等好多秒」）**：本项目下载中心「失败缺口 / 可重下 / 内容资产卡」都依赖 `download_tasks.manager.asset_snapshot()`，它会对**每一个 URL** 做一次 `os.path.is_dir` + `any(iterdir())` 磁盘 stat（URL 数 = 任务项总数 + 下载履历条数，随库增长）。**三个强制动作**：① **进程内 TTL memo**：这类「全量扫描 + 多接口共用」的结果必须在 manager 内 memo 一次（TTL 与 `db.cached` 的 5s 同步），让 `failures`/`re-downloads`/看板共享同一份计算——否则多个接口在挂载时各自独立重算一遍，等于把最贵的扫描做 N 倍；② **磁盘 stat 移出持锁区**：扫描必须只浅拷贝内存引用、释放 manager 锁后再逐 URL stat，绝不在 `with self._lock:` 内做 IO——否则活跃下载时 Windows Defender 实时扫描每个落盘文件会让扫描膨胀到秒级，并**阻塞** `summary()`（主任务表）/ SSE / 提交 / 取消，把主表一并卡住；进一步：热路径判定「目录是否还在」只用 `os.path.is_dir`（纯元数据，Defender 不扫文件内容）、**不要** `any(iterdir())` 枚举目录内容——实测活跃下载时枚举大目录会被实时扫描拖到百毫秒级（本场景该枚举曾把冷扫描从 57ms 顶到 18.5s），而空目录数恒为 0（已下载目录成功时必非空），`is_dir` 与「is_dir+有内容」口径完全等价；同一目录被多个 URL 复用时还要按目录名去重缓存（`_classify_urls` 的 `dir_exists`），每个目录只 stat 一次（3230 个 URL 实际只有约 890 个去重目录）；③ **写后失效**：memo 与 `db.cached` 同源失效（本项目唯一注入点 `api._invalidate_download_stats` → `manager.invalidate_asset_snapshot`），提交 / 删文件后缺口清单与可重下清单立即刷新（规则 21）。**不要误判为存储介质问题去换 SQLite**：下载任务 JSON 启动即整体读入内存、不逐请求解析，慢在「磁盘目录是否还在」的事实与 posts.db 视图查询，两者换库都无解，且会破「Web 不写库 / 文件可迁移」约束（规则 3、通用工程约束 1）。**本项目实例**：实测 `re-downloads` 冷 699ms、`failures` 217ms，二者各自独立全量扫 `_asset_snapshot` 且持锁；改造后 manager 级 memo 让三接口共享一次扫描、stat 移出锁区（快照扫描进行中 `summary()` 仍 1.4ms 返回），返回字段与口径零漂移（alive=130 / gone=764 / active=0 / failures=5 一致）。

44. **批量反查必须合并成一次查询；以「归一化键」为 dict 的结果必须消除顺序依赖**（2026-09-22 确立，源于用户上报「重启后下载中心要等十几秒」）：
    - **触发场景**：① 在循环里对每个实体各调一次「按路径批量查库」的函数；② 把多种 URL 形态归一化成同一个键后写入 dict。两者叠加会让「重启后首个请求」慢十几秒，且只在批量规模变化时暴露，静态检查与类型检查全部静默通过。
    - **强制动作**：
      ① **循环内禁止逐实体查库**：先把全部待查 key 收集起来、**合并成一次**批量查询，再分发给各实体。本项目 `_attach_task_titles` 原对 197 个任务各调一次 `_post_meta_by_paths`（=197 次 `posts_filtered` 视图查询，每次都要重算黑名单子查询 + 开关连接），实测冷缓存 **16908ms**；改为 `_warm_path_titles` 单次查库后 **471ms**（约 36 倍）。
      ② **归一化键冲突必须显式规定取舍**：库里同一帖子可能同时存在「相对路径」与「镜像域名完整 URL」的重复行且标题不同（实测 737 个路径中 8 个冲突）。以归一化结果为 dict 键时直接覆盖会**依赖查询返回顺序**——**批量大小 / 分块边界一变，结果就跳变**（本项目表现为某任务标题在「整批查」与「分任务查」之间来回变）。必须显式偏好规范形态（本项目：`url` 原本就是相对路径的行；依据见项目前提 2「域名不得入库」），使结果与批量大小无关。
      ③ **验证必须覆盖「批量大小无关性」**：改成批处理后必须断言「整批查询」与「拆成多批再合并」得到同一结果（本项目已加该断言），否则只测一次会漏掉顺序依赖。
    - **测量教训（同一个坑第二次踩）**：性能优化**必须测「重启后首个请求」，不能只测稳态**。本项目第一轮改造后稳态实测 175ms 即判断修好，但用户实际体验的是「重启后第一次」——那是 **10377ms**（标题缓存冷启动，冷热差两个数量级）。只报稳态等于假通过。**强制动作：重启后按秒轮询目标接口、画出延时衰减曲线**（本项目正是靠 `t+0s=10377ms → t+3s=93ms` 的曲线才锁定「仅首次慢」），并逐项确认冷/热两态。
    - **本项目实例**：`web/api.py` 的 `_warm_path_titles` / `_derive_titles` / `_post_meta_by_paths`。修复后重启后首个 `/api/downloads` 由 10377ms 降至 790ms、稳态 ~100ms；197 个任务标题与改造前逐任务查库结果零差异，且整批 vs 拆半合并标题一致。残余：`re-downloads` 冷 ~650ms（`posts_filtered` 对 764 个 gone 路径的查询），稳态 ~130ms。

45. **按「某天」筛选/统计帖子前，必须核对时间列的写入侧语义，禁止把「最近覆盖写入时间」当「入库时间」**（2026-09-23 确立，源于用户要求核查「自动下载是否按当天入库的帖子下载沉淀」）：
    - **触发场景**：任何以日期为窗口的筛选 / 统计 / 推荐（自动下载、待下载推荐、趋势图、榜单、导出…），只要用到 `date` / `created_at` / `update_at` / `update_date` 这类时间列。
    - **本项目事实（先背下来再动手）**：`date` / `created_at` = 帖子**真实发布**信息（由 HTML 发布时间戳派生）；`update_at` / `update_date` = 最近一次**覆盖**写入时刻，**首次插入硬编码为空串**（`scraper.py: save_to_sqlite`）。且抓取是**全站重抓**（单批写库 12 万+ 条），存量历史帖天天被 upsert 刷成当天 → 用 `update_date` 当「入库日」会**双向**偏差：历史帖被当成当天新帖、当天首次入库的新帖（该列为空）被整个漏掉。
    - **强制动作**：① 动笔前先读 `init_db.py` 的列注释 + **写入侧代码**，确认「这个列在哪些分支被写、首次写入什么」；② 用真实数据跑一次**对照**（新口径命中数 vs 旧口径命中数 vs 两者差集中真正的当天帖），用数字证明口径，不要靠字段名推断；③ 把口径结论写进代码注释与对应文档。
    - **本项目实例**：「自动下载」原按 `update_date = 当天` 筛选，实测候选 816 条中**仅 1 条**为当天发布（其余跨 2026-02 ~ 2026-09），且当天首次入库的 123 帖（版块白名单内 57 帖）全部漏掉；2026-09-23 改为 `date = 当天`（发布日）后，同条件候选 398 条**全部为当天发布**（旧口径同条件 47967 条，其中当天发布仅 341 条 —— 差额 47626 条全是历史帖）。
    - **附带教训（配置脏值）**：用户设置里 `precipitate_keywords` 含 `V]`（全库命中 1042，匹配的是标题尾缀 `[2V]` 之类），把候选从 90 条放大到 816 条 ——「一次性提交 793 帖」这种异常量级，先查**配置是否被误填**，再看代码。文档索引：`docs/自动下载设计与实现.md` §3、§9。

46. **「零结果」提示必须能区分根因，禁止把多种失败原因压成一句「已提交 0 帖」**（2026-09-23 确立，源于用户质疑「提交 0 帖到下载中心（共 0，下载中心已下跳过 0）」是否合理）：
    - **触发场景**：批量任务（自动下载 / 批量下载 / 导入 / 清理…）的汇总文案或 toast 出现 0 或空结果。
    - **问题**：0 至少有三种根因——① 上游压根没数据（抓取没跑/失败）；② 有数据但被筛选条件全部挡掉；③ 命中了但已下载过/被去重跳过。三者处置完全不同（查抓取 / 改条件 / 无需操作），压成一句会让人以为「功能正常，只是今天没货」，**掩盖抓取中断**。原文案还把恒为 0 的项（「共 0」「跳过 0」）一起复述，全是噪声。
    - **强制动作**：① 汇总里必须带**分母**（本次窗口内的上游总量，如当天发布帖数 `today_posts`），让 0 有参照；② 文案按根因**分档**（无数据 / 没命中 / 命中但已下过 / 资源不足 / 未启用）；③ 文案生成收敛到**唯一函数**，定时状态、手动触发 toast、前端提示、状态面板四处共用，禁止前端照着字段自拼（本项目 `precipitate.summary_reason()`，前端直接渲染 `reason`）；④ 0 值不展示恒为零的项。
    - **本项目实例**：自动下载汇总新增 `today_posts` 与 `reason`；文案由「已提交自动下载 0 帖到下载中心（共 0，下载中心已下跳过 0）」改为 `当天（2026-09-23）尚无发布数据入库（抓取未完成或未运行），未提交任何下载` / `当天发布 3 帖，无一命中筛选条件…` / `命中 3 帖，但下载中心已全部下载过（跳过 3），未重复提交` 等五档。文档索引：`docs/自动下载设计与实现.md` §9。

47. **代码变更必须配「足够」的真实测试集：覆盖全分支 + 真实数据 fixture + 全隔离环境，禁止靠推演或只抽测一条主路径**（2026-09-23 用户明确要求）：
    - **触发场景**：任何后端逻辑 / 文案 / 状态机改动的交付前（第 15 条管「跑一遍」，本条管「跑多少、怎么跑」）。
    - **强制动作**：
      ① **先列分支再写用例**：把改动引入的分支逐条列出（正常 / 空结果 / 边界 / 异常 / 开关关闭 / 资源不足 / 重复触发），每条一个真实用例，**不是只测最常见的那条**。
      ② **数据用真实副本 + 造 fixture**：复制真实库到临时文件、在副本里插测试数据（如「当天发布的测试帖」），不要靠生产数据碰运气、也不要用 mock 代替真实链路。
      ③ **环境全隔离**：独立端口 + 临时 DB / 设置 / 任务 / 履历 / 落盘目录。本项目可用环境变量：`TXXY_WEB_PORT`、`POSTS_DB`、`DOWNLOADS_DIR`、`TXXY_WEB_SETTINGS_FILE`、`TXXY_DOWNLOAD_TASKS_FILE`、`TXXY_DOWNLOAD_HISTORY_FILE`、`TXXY_PRECIPITATE_SCHEDULE_STATE_FILE`、`TXXY_SCRAPE_SCHEDULE_STATE_FILE`。
      ④ **断言对着响应体字段值与文案，并核对数字自洽**（如 `submitted == total - skipped_dup`、文案里的数字与字段一致），不要只断言「没 500」。
      ⑤ **测完清理**临时文件，并核对真实文件未被污染（本项目每轮核对 `outputs/web_settings.json` / `download_tasks.json`）。
    - **测试集本身就是找 bug 的手段**：本轮「连跑两次自动下载」用例暴露 `manager.submit()` 未失效资产快照 memo —— 新任务的 pending 链接在 5s TTL 内对去重不可见，第二次触发会把同一批 URL 重复提交（两个 worker 并发写同一文件）。已把失效内聚进 `submit()`（自动下载直连 submit、不经过 api 层那套失效）。修复后同一用例由「第 2 次仍提交 2 帖」变为「提交 0 帖、跳过 3」。
    - **环境变量名的坑（本次实踩）**：路径类变量**没有** `TXXY_` 前缀（`POSTS_DB` / `DOWNLOADS_DIR`），写成 `TXXY_POSTS_DB` 会静默连回真实库（还会把测试下载落到真实 `downloads/`）。起测试实例后先用一个只读断言确认连的是临时库，再跑用例。
    - **本项目实例**：自动下载文案改动配了 7 个用例（当天无数据 / 有数据未命中 / 命中并提交 / 命中但已下过 / 磁盘不足 / 未启用 / 定时与手动文案同源）+ 1 个在途去重用例，全走真实 HTTP 接口、全隔离 fixture，全部断言通过后才交付。

## 技术栈
- 后端：Python3 + **FastAPI**；SQLite 只读（`db/posts.db`，WAL，`PRAGMA query_only=ON`）；统计接口经 `db.cached(key)` 做 **5s TTL** 内存缓存。
- 数据写入由项目根目录独立 `scraper.py` 负责，**Web 进程严禁写库**（下载中心 `download_tasks.py` 仅做文件系统下载）。
- 前端：Vue3（`<script setup lang="ts">`）+ Vite5 + **Pinia** + **Element Plus**（中文 locale）+ **ECharts5** + vue-router4（`createWebHistory`）。
- HTTP 统一走 `src/api/index.ts` 封装的**原生 fetch**（10s 超时 + 同 key 请求去重取消 + 统一 `ApiError` 错误类型；POST/DELETE 不参与去重）。

## 目录结构（勿随意新增顶层目录）
```
txxy_test/                  # 抓取脚本在项目根：scraper.py / run_batch.py / run_recorder.py / init_db.py 等
├── txxy_env.py       # 唯一配置源：环境判定 / 域名 / URL 转换 / 版块映射 / dotenv 加载
├── mirror_service.py # 1024 本地镜像（web.exe）端口守护唯一实现（run_batch 与 start_web 共用）
├── http_headers.py   # 唯一 UA 与 Accept 定义（零依赖，抓取与下载模块共用）
├── txt_export.py     # TXT 清单导出（磁力 / 云盘共用）
├── download_files.py 等    # 下载模块（download_tasks.py 复用其 process_one）
└── web/
    ├── app.py        # FastAPI 入口（GZip + /api 耗时监控 + SPA 静态托管）
    ├── api.py        # 路由：/api/config、/api/schedule、/api/stats/*、/api/posts、/api/runs、/api/resources、/api/downloads
    ├── mirror.py     # 帖子链接同源中继（/mirror → 127.0.0.1:1024，镜像不可用时 302 到业务域名）
    ├── config.py     # 配置（DB_FILE、ENABLE_AUTO_REFRESH 默认开启、下载中心参数、定时抓取默认值）
    ├── db.py         # 只读连接 + 5s TTL 缓存 + URL 归一化
    ├── ratelimit.py  # 接口限流（固定窗口，/posts/export、/resources 挂载，超限 429）
    ├── atomicfile.py # JSON 原子落盘（临时文件 + 替换，可开 .bak 轮转）
    ├── scheduler.py  # 定时抓取调度（60s tick 线程 + 幂等状态落盘 + 复用 runs.start_run）
    ├── runs.py / resources.py / download_tasks.py  # 运行记录 / 资源扫描 / 下载中心队列
    └── frontend/src/
        ├── api/ stores/ router/ layout/ components/ views/ utils/ composables/
        └── views/    # Dashboard / Posts / Runs / Resources / Downloads / Trash / Settings 七个页面
```
路由为 `/`、`/posts`、`/runs`、`/resources`、`/downloads`、`/trash`、`/settings` 七条（`/trash` 为 2026-08-31 经用户确认新增的回收管理页，此前为五条；`/settings` 为 2026-09-08 经用户确认新增的参数设置页，此前为六条）。

## 项目专属约束（必须遵守）
1. **自动刷新默认开启**：`config.ENABLE_AUTO_REFRESH` 默认 `1`；前端 `REFRESH_INTERVAL = 5000` 轮询；`db._TTL = 5`。禁止回退为关闭 / 30s 轮询 / 60s 缓存，除非用户明确要求。
2. **优先复用**：新增 UI 用 Element Plus 与现有 `components/`；图表复制 `DashboardView` 既有 ECharts option 结构。
3. **图表网格线规则**：Y 轴横向线显示（色 `rgba(0,0,0,0.08)`），X 轴竖向线隐藏；`axisPointer` 悬停纵向线必须为深色（非白色）。
4. **Tooltip 顶层**：所有 ECharts `tooltip` 必须 `appendToBody: true`，防遮挡。
5. **接口一致性**：后端新接口加在 `api.py` 并用 `db.cached` 包裹统计查询；前端在 `src/api/index.ts` 加同名方法。
6. **错误处理**：后端已统一 `HTTPException(detail)`（无 `{ok:false,msg}` 形态），前端 `src/api/index.ts` 的 `get()` 已统一解析 `detail`；前端 `try/catch` 后 `ElMessage.error`，禁止裸抛或静默吞错。
7. **状态管理**：跨页状态归并 `useAppStore`（全局刷新版本号/最后更新）+ `useDashboardStore`（自动刷新开关/最后更新时间/Header 每秒实时时钟），禁新建分散 store。
8. **禁止**：引入项目未用新依赖（React/Tailwind/Redux 等）、Web 写库、改动既有路由路径与 history 模式、`<script setup>` 外使用 Options API。
9. **交付自检**：前端改动后 `cd web/frontend && npx vue-tsc --noEmit` 必须 0 错误，且 `read_lints`（ts-plugin IDE 诊断）也必须 0 错误——两者对模板的类型检查严格度不同，需双通道都通过；后端 `python web/app.py` 可启动且既有接口行为不变。
10. **模板禁止对 setup 变量内联赋值**：`<script setup>` 顶层 `let` 变量（带字面量初始值，如 `let x: boolean = false`）在模板中直接写 `@mouseenter="x = true"`，会被 ts-plugin 按初始值收窄为字面量类型 `false` 而误报「不能将类型 true 分配给类型 false」（vue-tsc 不报此错）；一律用方法包装，如 `@mouseenter="setPaused(true)"`。
11. **下钻必须继承上下文口径（数字自洽）**：从榜单 / 统计图表下钻到明细页时，必须携带当前生效的筛选口径（统计范围、时间窗口等），保证「榜单上的数字」与「明细页条数」严格一致——榜上显示 431 条，点进去就必须是这 431 条，而不是该维度的全部数据（如该作者累计 4314 条）。这是 BI 与看板类产品的通行做法（Grafana 的 dashboard variables、GA 的全局日期范围均如此）。判断标准：下钻目标是**明细记录列表**则继承上下文；是**实体档案页**（看全貌）则不必。明细页必须把生效条件集中展示为摘要条并支持逐项清除，让用户看得见、能调整，而不是散落在筛选框里靠猜。
12. **进度类指标必须「有目标 + 分层 + 多态」**（2026-09-13 由内容资产卡确立，详见《内容资产沉淀进度调研与建议.md》第二节五条原则与《数据总览大屏设计与优化总览.md》第二十节）：
    - **漏斗只放同量纲计数**（收录 → 已沉淀 → 本地文件），字节量级另立「存储」视图，禁止把体积当漏斗一级；
    - **分母必须可解释**：库存类覆盖率会被长尾稀释（实测全库 0.09% vs 互动 Top500 3.4%，差 38 倍），一律给分层档位，不用全库平均值代表进度；
    - **库存 / 在途 / 缺口 / 失败 同屏**：只看库存等于只看结果不看过程；
    - **进度必须有目标基准**：目标 + 当前 + 还差多少（本项目的沉淀目标档位与覆盖率在参数设置页）；
    - **数字不下钻要说明理由**：口径无法在明细页表达时（如「按互动量取前 N 名」）不做下钻，改为悬浮说明，禁止下钻到另一个口径的列表（数字必须自洽）。
13. **前端修复默认兼容移动端 + 全流程实测才交付**：涉及前端展示页面的改动，除非用户明确要求「仅桌面端」，默认都要一并兼容修复移动端——响应式布局、触摸手势、窄屏错位、分页与按钮在手机上的可达性等都需覆盖，不能「桌面正常即可」。交付前必须按真实链路端到端实测通过：覆盖桌面与移动两种视口，验证点击 / 滑动 / 缩放 / 翻页 / 关闭等关键交互在两种环境下均无异常；移动端可用浏览器设备模拟（DevTools 设备工具栏）或真实窄屏窗口验证。发现问题当场修复，禁止以「桌面正常」带病交付（与第 15 条「修复必须实测通过再交付」同源，本条进一步锁定移动端口径）。

## 已有共享实现索引（写新代码前先查此表，禁止再写第二份）
能力已存在时直接调用/扩展，不得另起炉灶；发现表中有遗漏的可复用实现，补充到此表。

| 能力 | 唯一实现位置 | 说明 |
|---|---|---|
| 环境判定 / 域名 / URL 转换 | `txxy_env.py`（项目根） | **唯一配置源**：`detect_env()`、`PUBLIC_DOMAIN`、`LOCAL_PROXY`、`use_local_proxy()`、`display_domain()`、`to_storage_path()`、`to_display_url()`、`to_fetch_url()`、常量 `DEFAULT_PUBLIC_DOMAIN` / `DEFAULT_LOCAL_PROXY` |
| `.env` 加载 | `txxy_env.load_dotenv()` | 全项目唯一 dotenv 实现；`web/config.py` 等模块一律复用，不得另写 |
| 展示端配置 | `web/config.py` | 只读不定义域名，域名相关全部取自 `txxy_env`（配置源加载失败直接抛错，不静默降级到兜底值） |
| URL 归一化 | `web/db.py: normalize_url()` | 内部转调 `txxy_env.to_display_url()`，不要在别处再写前缀替换逻辑 |
| 统计查询缓存 | `web/db.py: cached()` | 5s TTL，新增统计接口必须用它包裹 |
| 接口限流 | `web/ratelimit.py` | 固定窗口限流，新接口需限流时挂载它 |
| HTTP 请求头 / UA | `http_headers.py`（项目根，零依赖） | 唯一 UA 与 Accept 定义：`build_headers(ACCEPT_HTML/IMAGE/VIDEO)`。禁止在模块里重写 UA 字面量 |
| TXT 清单导出 | `txt_export.py: save_lines_txt()` | 磁力 / 云盘等「每行一条」清单共用，各模块只传文件名与日志标签 |
| JSON 原子落盘 | `web/atomicfile.py: write_json_atomic()` | 唯一的「临时文件 + 原子替换」实现，支持 `indent` 与 `backup` 轮转 |
| 版块映射 | `txxy_env.SECTIONS` / `fid_name()` | 抓取端与展示端共用，禁止各存一份再靠注释提醒同步 |
| 前端 HTTP 请求 | `web/frontend/src/api/index.ts` | 原生 fetch 封装（超时 / 同 key 去重 / `ApiError`）+ `sseUrl()`；新请求一律走它，禁止裸 `fetch` 或硬编码 `/api` |
| 帖子链接拼装（打开 / 复制） | `web/frontend/src/utils/postUrl.ts` + `web/mirror.py` | 浏览器侧链接一律经同源中继 `/mirror`（本地镜像只绑回环，手机无法直连）；禁止在视图里直接 `window.open(row.url)` |
| 本地镜像同源中继 | `web/mirror.py`（路由前缀常量 `web/config.py: MIRROR_PREFIX`） | 转发到 `txxy_env.LOCAL_PROXY`；连不上 / 5xx → 302 到业务域名。禁止另写第二个转发实现或在前端拼 `127.0.0.1:1024` |
| 1024 镜像端口守护（启停 web.exe） | `mirror_service.py`（`ensure_web_service` / `shutdown_web_service`） | 探测端口 → 未监听则启动（须注入回车）→ 等就绪；只关自己启动的。`run_batch.py` 与 `start_web.py` 都必须用它，禁止再写第二份 |
| 前端颜色 | `web/frontend/src/utils/fidColor.ts` | 唯一色板：`colorForFid()`（按 fid 取模）/ `colorByIndex()`（按排名）。禁止另建第二套色板 |
| 前端时间格式化 | `web/frontend/src/utils/time.ts` | `formatFullTime` / `formatDateTime` / `formatMinuteTime` / `formatRelativeTime` / `formatShortTime`。禁止在页面内自己补零拼字符串 |
| 回收站数据与操作 | `web/frontend/src/composables/useTrash.ts` | TrashView（表格版）与 ResourcesView（抽屉版）共用；额外刷新用 `onChanged` 回调 |
| 目录名 ↔ 帖子判定 | `web/resources.py: match_dirs_to_posts()` | 按 `sanitize_title(库内标题) == 目录名` 比对（目录名是标题清理 + 截断 80 字的结果）；下载履历磁盘恢复的唯一判据。`source_lookup()` 仅供展示（容错优先），判定必须走前者 |
| 资产口径快照 | `web/download_tasks.py: DownloadTaskManager.asset_snapshot()` | 一次持锁返回 alive/gone/active + 已认领目录 + 首次落盘时间 + 目录映射 + 失败清单；资产卡 / 待下载队列 / 帖子页筛选共用，禁止再逐个 getter 各扫一遍 |
| 下载履历 | `outputs/download_history.json`（`TXXY_DOWNLOAD_HISTORY_FILE`） | `url → {dir, first_at, checked_at, fail_at, fail_error}`；`first_at` 只在缺失时写（增长曲线/速率），`fail_at` 非空即「当前失败缺口」。原子写 + `.bak` 轮转 |
| 页内参数设置 | `web/settings.py`（白名单 / 范围钳制 / 原子落盘 / 来源标注） | 唯一的参数设置实现：`get/get_int/get_float/get_bool` + `snapshot/update/reset/apply_runtime`；默认值仍在各归属模块（`config.py` / `download_files.py` / `resources._CACHE_TTL`），此处**只存覆盖值**，禁止复制第二份默认值；类型支持 `int/float/bool/text/array/enum/times`（`enum` = 单选枚举，非法值回落默认，前端用 `el-select` 渲染；`times` = "HH:MM" 时刻列表，规格化统一走 `config.normalize_times`） |
| 计划时刻规格化 | `web/config.py: normalize_times()` | 环境变量 / 设置文件 / 页面保存 / 调度器读配置四处共用的唯一解析：丢弃非法项、去重、升序、限量 `MAX_SCHEDULE_TIMES`。禁止在别处再写一份时刻解析 |
| 布尔环境变量解析 | `web/config.py: _env_bool()` | `1/true/yes/on` 为真，空/未设置取默认；新增布尔配置一律用它，不再复制 `os.environ.get(...).lower() in (...)` |
| 定时抓取调度 | `web/scheduler.py: ScrapeScheduler` | 应用内调度（60s tick 守护线程）：复用 `runs.start_run` 执行、幂等键 = (日期, 计划时刻) 落盘 `outputs/scrape_schedule_state.json`、错过不补跑、上一批在跑则跳过。禁止另起 subprocess 或第二套调度 |
| 错误提示 | `web/app.py`（`HTTPException(detail)`）+ 前端 `ElMessage.error` | 后端统一 `detail`，前端统一解析，禁止另造 `{ok:false,msg}` 形态 |
