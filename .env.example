# ---- 业务域名与本地代理（唯一配置源：项目根 txxy_env.py，全项目只有这一处域名默认值）----
# 各环境抓取 / 入库 / 展示共用同一业务域名，通常无需配置；
# 本地 1024 代理只是传输层（仅影响实际请求，不进入数据与入库链接）。
# 覆盖时取消下面注释（显式配置优先级最高）：
# TXXY_PUBLIC_DOMAIN=https://txxy.com     # 唯一业务域名（代码默认即此值）
# TXXY_USE_LOCAL_PROXY=1                  # 本地代理开关：留空=环境自适应（仅 local 开）；1/0 强制
# TXXY_LOCAL_PROXY=http://127.0.0.1:1024  # 本地代理地址，一般不用改
# TXXY_DISPLAY_DOMAIN=                    # 页面链接前缀：留空=按环境（本地 1024 / 其它公开域名）；填了强制用它
# 兼容：历史键 PUBLIC_ROOT / REMOTE_ROOT_URL 若仍被设置，会当作业务域名识别（平滑迁移）

# ---- Web 服务 ----
TXXY_WEB_HOST=0.0.0.0                 # 容器内必须 0.0.0.0 才能对外访问
TXXY_WEB_PORT=8088                    # 容器内端口（一般不动）
TXXY_ENABLE_AUTO_REFRESH=0

# ---- 端口映射 ----
# 宿主机映射端口：Docker 部署统一 18088（deploy/ 下四个脚本均写入此值）；
# 本地 start_web.bat（Python 服务）用 8088，两者错开，可同时运行互不影响。
# 留空即可，部署脚本会自动写入 18088；手动填写则以你的值为准
# （脚本不会覆盖已填的值）。
TXXY_HOST_PORT=

# ---- 镜像（离线部署必填） ----
# 在线构建/部署留 txxy:latest 即可；离线环境改为已 load 的版本 tag，
# 例如 TXXY_IMAGE=txxy:v0.1.0-a1b2c3d4e5
TXXY_IMAGE=txxy:latest

# ---- 时区 ----
TZ=Asia/Shanghai
