# ---- 域名（唯一配置源：项目根 txxy_env.py。只有一个键，且有默认值 —— 零配置可跑）----
# 入库恒为相对路径 /htm_data/...；页面链接前缀 = 访问链当前粘住的 host。
# 覆盖时取消注释（显式配置优先级最高；也可在参数设置页运行时改，页内 > .env）：
# TXXY_FETCH_CHAIN=http://127.0.0.1:1024,https://txxy.com
#   有序访问链（逗号分隔）：成员为同一站点的同构端点，按序访问，某项连不上自动切下一项；
#   最后一项 = 公网主域（业务域名 / 中继降级目标），不能是内网地址。
#   默认：本地 Windows = 本机镜像 + 公网主域；Docker / Linux = 仅公网主域

# ---- Web 服务 ----
TXXY_WEB_HOST=0.0.0.0                 # 容器内必须 0.0.0.0 才能对外访问
TXXY_WEB_PORT=8088                    # 容器内端口（一般不动）
TXXY_ENABLE_AUTO_REFRESH=0

# ---- 端口映射 ----
# 宿主机映射端口：Docker 部署统一 18088（deploy/ 下四个脚本均写入此值）；
# 本地 start_web.bat（Python 服务）用 8088，两者错开，可同时运行互不影响。
# 留空即可，部署脚本会自动写入 18088；手动填写则以你的值为准
# （脚本不会覆盖已填的值）。
TXXY_HOST_PORT=

# ---- 镜像（离线部署必填） ----
# 在线构建/部署留 txxy:latest 即可；离线环境改为已 load 的版本 tag，
# 例如 TXXY_IMAGE=txxy:v0.1.0-a1b2c3d4e5
TXXY_IMAGE=txxy:latest

# ---- 时区 ----
# 容器内时区由 Dockerfile 的 ENV TZ=Asia/Shanghai 提供，此处不再重复设置。
# 不要设置 TZ：IANA 时区名会被本机 Windows 子进程的 CRT 误解析为 UTC+1，
# 使 datetime.now() 比本地时间早 7 小时（详见 run_batch.py 中剔除 TZ 的说明）。
