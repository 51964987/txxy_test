# 数据总览大屏 — 设计与优化总览

> 本文档汇总数据总览页（路由 `/`，`DashboardView.vue`）的设计现状、已落地优化与待确认候选方案，避免方案散落多文件造成冗余。
> 最后更新：2026-09-10（新入榜 NEW 标记口径修正 + 热门榜窄屏板块 tag 处理，详见第十六节；2026-09-01 变更见第十一节）。

---

## 一、页面布局与信息模块

页面分区加载：P0（首屏立即加载）+ P1（视口懒加载，`IntersectionObserver`，`rootMargin: 200px`）。

```
Header（Logo + 导航 + 实时时钟 + 更新时间 + 自动刷新开关 + 全屏沉浸）
├─ ① 4 张 KPI 指标卡（横排，主指标 + 副指标双层）
├─ ② 每日发布趋势（左右 1:1 双图）：全站发布趋势 + 分版块发布对比（双向 Tooltip 联动）
├─ ③ 活跃作者 Top10 / 活跃版块 Top10（左右 1:1 横向条形图，P0）
└─ ④ 热门榜（P1 懒加载，4 栏）：点赞最高帖 / 回复最高帖 / 最新最热 / 本月最热
```

> 变更说明：原「③ 版块分布（排行榜轮播 + 环形图）」已于 2026-08-28 整体移除，由「活跃作者 / 活跃版块」条形图取代（数据源 `top_authors` / `top_fids`，见 1.6）；`/api/stats/fid_dist` 仍保留，用于 KPI 副指标「覆盖 N 个版块」。

### 1.1 KPI 指标卡（4 张，2026-08-27 命名贴合后；2026-09-11 新增「最新最热帖 / 媒体文件」并去重「最近入库」；2026-09-12 移除该两张回归 4 张）

历史沿革：原 7 张合并为 4 张（合并记录见 git 历史）；2026-08-27 随发布时间口径变更整体改名。

| 卡片 | 主指标 | 副指标 | 数据来源 |
|---|---|---|---|
| 累计收录 | `total`（库内标题去重收录量） | 近7日发布 +`week_new` · 覆盖 `fidDist.length` 个版块 | `/stats/overview` + 前端 `fid_dist` |
| 今日发布 | `today`（按真实发布日统计） | 较昨日环比 ↑/↓% · 昨日 `yesterday` | 计算 |
| 发帖作者 | `total_users`（author 去重） | 今日更新 `active_users` 人 · 活跃率 % | `/stats/overview` |
| 最近入库 | 最近批次日期（`latest_run_at` 取 MM-DD） | 抓取中徽标（running 时）· 更新于 HH:MM（中性） | `overview.latest_run_at` + `/api/runs` |

> 2026-09-11 调整：原「最近入库」副指标的「今天/昨天/N 天前」新鲜度告警，与 R1 采集健康条的「已 N 天无入库」同信号重复，已移除（告警语义唯一归属健康条）；该卡仅保留中性的「更新于 HH:MM」+ 抓取中徽标。同步新增「最新最热帖 / 媒体文件」两张 KPI（补齐热度质量与媒体资产两个视角，原 4 卡偏收录规模与时间）。栅格由 4 列改为 3 列以承载 6 卡（2×3）。
> 2026-09-12 调整：移除「最新最热帖 / 媒体文件」两张 KPI，回归 4 张——热度质量已由热门榜「最新最热」栏覆盖、媒体资产已由 R4 内容资产漏斗覆盖，两张 KPI 与既有区块重复；布局改为 DashboardView 作用域内覆盖（宽屏 4 列 / 中屏 2×2 / 窄屏单列），全局 `.stat-grid` 保持 3 列（主要供下载页沿用）；资源页 KPI 与看板同源，同样作用域内覆盖为 4 列一行（窄屏 2×2 / 手机单列）。

### 1.2 每日发布趋势图

- 已拆分为「全站发布趋势」+「分版块发布对比」双图并排，实现**双向 Tooltip 联动**、悬停暂停自动轮播；两卡样式统一（2026-08-27，以分版块卡为基准）。
- 全站趋势卡：纯色折线（联动聚焦时换版块色）+ 淡面积，tooltip 皮肤/轴样式/动画与分版块卡一致，富格式内容（较上日环比、7 日均）保留。
- 联动聚焦（2026-08-28）：点击分版块折线聚焦某版块（全站折线换色高亮）、点击全站图聚焦某日（分版块首系列显示该日纵向标记线）；卡头出现「联动聚焦」徽标，点 ✕ 解除。
- ~~σ 区间带、日均参考线、峰/谷/最新脉冲点~~已于 2026-08-27 风格统一时移除（统计信息由头部统计卡承载：全站卡 峰值/谷值/日均，分版块卡 对比版块/最活版块/峰值日增）；CSS 流光两卡共用保留；数字滚动保留。
- 维度轮播序列 `TREND_DAYS_SEQ=[7,14,21,28]`；天数下拉 `allow-create` 支持自定义输入；数据点 >31 时启用 DataZoom。

### 1.3 版块分布 · 排行榜（已移除）

> **2026-08-28 整体移除**：排行榜轮播 + 环形图区块下线，由「活跃作者 / 活跃版块」条形图取代（见 1.6）。以下为历史设计记录。

- 固定可视区 8 行 × 40px，每 2000ms 平滑上滚轮播（末尾补位无缝循环）。
- 行：`[名次徽章] [版块名(fid)] [今日新增/最新] [总量条数(占比)]`；前 3 名金/银/铜渐变徽章。
- 交互：悬停暂停轮播、点击行 `goPostsWith({fid})` 下钻帖子浏览；tooltip 显示最近抓取/今日/昨日。

### 1.4 版块分布 · 环形图（已移除）

> **2026-08-28 随版块分布区块一并移除**。以下为历史设计记录。

- 内外半径 42%~68%，确定性 13 色色板（与排行榜同色）；占比 ≥4% 显示外部标签。
- 每 2200ms 自动高亮轮播，中心显示当前版块名称/数量/占比；点击扇区/图例 `goPostsWith({fid})` 下钻。

### 1.5 热门榜（4 栏，P1 懒加载）

数据来自 `/stats/boards`（点赞/回复最高帖）+ `/stats/today_top`（最新最热：最新数据日期内点赞+回复综合 Top）+ `/stats/month_top`（本月最热：最新数据月份内综合 Top）。原「今日新增版块 / 今日热议帖」两栏已于 2026-08-28 替换为「最新最热 / 本月最热」。

交互规则（2026-08-26 调整，2026-08-28 增补）：
- **点赞最高帖 / 回复最高帖**：标题点击 → `window.open` 原帖 URL（新标签）；卡片空白区点击 → 下钻该版块（`/posts?fid=&sort=likes_desc|replies_desc`）；「查看更多」→ 带排序下钻帖子浏览（`/posts?sort=likes_desc` / `/posts?sort=replies_desc`）。
- **最新最热 / 本月最热**（2026-09-01 补下钻与口径展示，同日追加 P1/P2）：标题点击打开原帖 URL；**卡片空白区点击 → 下钻「该版块 + 该时间窗」**；「查看更多」→ 下钻整个时间窗（最新最热=最新数据日当天，本月最热=最新数据月份的月初~月末），排序随卡头切换联动（`engagement/likes/replies/hot` → `engagement_desc/likes_desc/replies_desc/hot_desc`）。卡头新增**排序维度切换**：综合 / 点赞 / 回复 / 热度（时间衰减，HN 式 `(score-1)/(age+2)^1.8`，解决老帖靠累计量霸榜），切换只重拉对应榜单。每行主指标显示**互动量**（点赞+回复，排序依据显性化），tooltip 附**互动率**（回复/点赞）与类型（围观型 / 均衡型 / 热议型），回复数≥点赞数时行内显示「热议」小标。本月最热额外显示发布日、卡头 sparkline（本月每日互动量）、**NEW 新入榜标记**（对比快照，仅首次进入 Top10 的当天标 NEW、跨天自动消失，口径修正见第十六节）；最新最热不显示发布日（各行同一天、无信息量），也不做 NEW（每日窗口整体更替，会全量刷成 NEW 而失去意义）。四栏版块 tag 统一为中性色，避免颜色竞争。
- **每行悬浮「下载」按钮**：直接 `POST /api/downloads` 创建下载任务（`@click.stop.prevent` 阻断卡片跳转），进度在下载中心查看。

### 1.6 活跃作者 Top10 / 活跃版块 Top10（2026-08-28 上线，2026-09-01 加口径与环比）

- 左右 1:1 横向条形图，P0 随首屏加载（`/stats/top_authors`、`/stats/top_fids`）。
- 作者条点击 → 帖子浏览按作者精确过滤（`/posts?author=`）；版块条点击 → 跳该版块列表；颜色：作者多色区分，版块按 `FID_PALETTE` 确定性色板。
- **下钻继承统计口径**（2026-09-01）：下钻时按当前口径附带 `date_from` / `date_to`（`all` 口径不传，即看全部），使榜单数字与帖子页结果自洽——在「近 7 日」榜上看到 431 条，点进去就是这 431 条，而不是该作者全部的 4314 条。帖子页顶部「当前条件」摘要条展示全部生效条件，可逐项清除或清空全部。
- **统计口径可切换**（2026-09-01）：卡片右上角 `累计 / 近 7 日 / 近 30 日` 分段控件，两卡独立记忆，切换只重新拉取对应榜单，不整页刷新。

| 口径 | 排序与主值 | 候选范围 |
|---|---|---|
| 累计（默认） | `total` | 全部 |
| 近 7 日 | `week` | 仅近 7 日有发帖（`HAVING week_c > 0`） |
| 近 30 日 | `month` | 仅近 30 日有发帖（`HAVING month_c > 0`） |

> 非累计口径必须加 `HAVING` 过滤，否则榜单会被大量 0 值占满，切换后看起来毫无变化。
> 口径切换确实会改变排名：实测累计第 2 名的作者近 7 日只有 234 条，被 431 条的作者反超。

- **环比展示**（2026-09-01）：近 7 日 vs 前 7 日（第 8~14 天）的涨跌百分比，`delta` 字段：
  - 条尾随主值一并展示，涨绿 `↑x%` / 跌红 `↓x%` / 持平灰 `持平`；
  - 前 7 日为 0 时无计算基准，`delta=null`，按橙色「新增」展示。
- **文案统一**：两卡副标题同构 —— `按<口径>发帖量 · 点击查看该<作者|版块>帖子`。
- **空态**：某口径下无数据时显示「该时间段暂无数据」，不再是空白区。
- **实现要点**：图表复用「数据指纹跳过重绘」机制，指纹已纳入口径（`name:value:valueLabel`），否则切换口径后数值未变时图表不会刷新。

---

## 二、API 接口与后端支持

| 接口 | 路径 | 优先级 | 说明 |
|---|---|---|---|
| 应用配置 | `GET /api/config` | - | 自动刷新开关 |
| 数据总览 | `GET /api/stats/overview` | P0 | 累计/今日/昨日/近7日/用户数 + `latest_run_at`（入库活动时间，新鲜度依据） |
| 趋势 | `GET /api/stats/trend?days=N` | P0 | 每日发布趋势（7/14/21/28 天，联动各版块 `trend_by_fid`） |
| 版块分布 | `GET /api/stats/fid_dist` | P0 | KPI 副指标「覆盖 N 个版块」（版块分布图已移除，接口保留） |
| 活跃作者榜 | `GET /api/stats/top_authors?limit=10&range=all` | P0 | `range=all\|7d\|30d` 切换统计口径（非法值 422）；返回 `total/today/week/month/prev_week/delta/value`，`value` 为当前口径主值 |
| 活跃版块榜 | `GET /api/stats/top_fids?limit=10&range=all` | P0 | 与作者榜同构（`range` 语义相同） |
| 热门榜 | `GET /api/stats/boards` | P1 | 各版块点赞/回复最高帖 |
| 最新最热 | `GET /api/stats/today_top?limit=10&sort=engagement` | P1 | `sort=engagement\|likes\|replies\|hot`；返回 `total`（当日帖数）/ `days`（=1）供卡头说明样本规模 |
| 本月最热 | `GET /api/stats/month_top?limit=10&sort=engagement` | P1 | `sort` 同上；额外返回 `daily`（每日互动量，sparkline）与 `is_new`（新入榜，对比快照） |
| 运行记录 | `GET /api/runs` | - | 「抓取中」徽标实时进度（B1） |
| 最近帖子 | `GET /api/stats/recent?limit=N` | - | 最新帖子列表（前端当前未使用，接口保留） |

> 历史更名：`today_top_fids` → `today_fids`、`rising` → `today_top`（旧文档中的接口名，现均已按右列名称提供）；`today_fids` 与 `recent` 前端当前未使用，接口保留。

- **数据库**：SQLite（WAL），`db/posts.db`；统计接口统一 `db.cached()` 5s TTL。
- **性能优化**：`_board_top` 改 per-fid `ORDER BY ... LIMIT 1` 命中表达式索引；新增 `idx_posts_likes_expr` / `idx_posts_replies_expr` / `idx_posts_author` / `idx_posts_created_at`；P1 懒加载减少首屏压力。

## 三、加载与刷新

- **P0**：`Promise.all([overview, trend, fidDist, topAuthors, topFids])` 并行；渲染后启动趋势 Tooltip 轮播；「分版块发布对比」滚动进入视口后懒加载 `trend_by_fid`。
- **P1**：`IntersectionObserver`（rootMargin 200px）进入视口才 `loadBoards()`（`boards` + `today_top` + `month_top` 并行）。
- **自动刷新**：默认 5s（`REFRESH_INTERVAL=5000`），由 `useDashboardStore` 管理；仅静默刷新已加载区块，不显示遮罩。
- **骨架屏**：P0 时 KPI/图表骨架占位；P1 时热门榜骨架。

---

## 四、每日新增趋势图 — 优化方案明细

> 分阶段落地状态（已与代码核对）：

**第一阶段（✅ 已完成）**：A1 均值参考线、A3 实时脉冲点、B1 头部统计条、B2 Tooltip 增强、C2 数据点下钻、D1 预加载缓存、趋势天数轮播(7/14/21/28)+平滑过渡。

**第二阶段（✅ 已完成）**：A4 多段渐变填充、A5 标准差区间带、E1 峰谷脉冲增强、E2 折线流光动画(CSS shine)、E3 数字滚动(RollingNumber)。
> B4 智能异常标注已按需求**移除**（2026-08-25 去掉 markPoint 及 `MarkPointComponent` 引用，σ 区间带与日均虚线保留）。

**第三阶段（📋 待确认）**：F1 Tooltip 迷你趋势图、F2 图表洞察文字摘要、A6 坐标轴美化、F3 Y 轴智能自适应、F4 全屏模式、F5 响应式自适应、F6 图例交互、F7 背景科技感装饰、F8 数据密度映射。

**按需实施**：B3 同比去年对比（需后端 API）、C3 Brush 框选缩放、C4 图表导出 PNG。

---

## 五、版块分布 · 排行榜 — 待确认候选方案（已作废）

> 2026-08-28 版块分布区块整体移除，本节候选方案随之作废，保留仅作历史记录。均为纯前端（除非标注需后端）：

| 方案 | 方向 | 关键内容 | 是否需后端 |
|---|---|---|---|
| A1 占比进度条 | 视觉 | 每行底部占比条，宽度=count/总量% | 否 |
| A2 前三名高亮 | 视觉 | 前 3 行背景渐变 + 左侧色条 + TOP 角标 | 否 |
| B1 增量趋势箭头 | 信息 | `today-yesterday` 绿↑/红↓ ±n | 否 |
| C1 与环形图联动 | 交互 | 悬停行 → 环形图 `dispatchAction` 高亮对应扇区 | 否 |
| C2 排序维度切换 | 交互 | 头部 Tab：总量/今日/昨日，前端重排 | 否 |
| D1 轮播高亮跟随 | 动效 | 当前行淡蓝高亮随滚动迁移 | 否 |
| D2 入场渐显动画 | 动效 | 每行延迟淡入 + 徽章弹跳 | 否 |
| B3 底部汇总条 | 信息 | 固定「共 N 版块 · 累计 M 条」 | 否 |
| C3 行悬停 Tooltip | 交互 | 原生 title 升级自定义浮层 | 否 |
| B2 行内 Sparkline | 信息 | 需按 fid 日增量序列 | **是** |
| A3/E1/E2 | 视觉/响应式/无障碍 | 徽章优化、多断点、reduced-motion | 部分/否 |

---

## 六、热门榜（点赞/回复最高帖）— 待确认候选方案

> 2026-08-28 热门榜后两栏已替换为「最新最热 / 本月最热」，与旧两栏相关的条目不再适用；其余仍待确认，当前仅输出，未变更代码：

| 方案 | 方向 | 关键内容 | 是否需后端 |
|---|---|---|---|
| A1 热度进度条 | 视觉 | 卡片底部细条，宽度=value/列最大值% | 否 |
| A2 前三名高亮 | 视觉 | 前三行背景渐变 + 左侧色条 + TOP 角标 | 否 |
| B2 点赞/回复双指标 | 信息 | 两卡互换展示对侧指标 | 视数据 |
| C1 悬停 Tooltip | 交互 | 深色浮层展示标题/版块/点赞/回复/时间 | 部分（需补齐字段） |
| D1 入场渐显 | 动效 | 卡片按 `i*70ms` 延迟淡入 + 徽章弹跳 | 否 |
| D2 悬停微动效 | 动效 | 上浮 + 左侧蓝边条 + 数值高亮 | 否 |
| A3 标题多行 | 视觉 | 悬停展开两行 | 否 |
| B1 作者+时间 | 信息 | 标题下补 `作者 · 日期` | **是** |
| B3 热度标签 | 信息 | 「🔥热」/「NEW」按阈值或时间 | **是** |
| C2 键盘可达性 | 交互 | `tabindex` + 回车跳转 | 否 |
| E1 每版块多帖 | 数据 | 每版块取前 2-3 帖 | **是** |
| E2 窄屏适配 | 响应式 | ≤768 隐藏 tag、≤480 仅留数值 | 否 |

---

## 七、参考来源（同类最佳实践）

- **阿里云 DataV / Grafana / AntV**：大屏榜单、联动高亮、进度条、σ 区间带、同比对比。
- **GitHub Trending / 掘金 / 知乎 / 微博热榜**：排序切换、增量角标、前三名强调。
- **Grafana**：时序图参考线、区间标注、Transform 聚合、Box Zoom。
- **ECharts 官方**：markPoint/markLine、Brush、DataZoom、visualMap。

---

## 八、实施约束与自检

1. 前端改动后 `cd web/frontend && npx vue-tsc --noEmit` 0 错误，且 IDE `read_lints` 0 错误。
2. 后端新增/改动接口需 `python web/app.py` 可启动并对受影响接口真实请求验证无 500。
3. 不引入新 UI 依赖，颜色体系沿用科技蓝 `#2f6fed` + `FID_PALETTE` 确定性色板。

---

## 九、2026-08-27 发布时间口径与命名调整（已落地）

1. **入库口径**：`posts.created_at` = 帖子真实发布时间戳（Unix 秒，来自 HTML `data-timestamp` 去尾 `s`）；`posts.date` = 由该时间戳派生的真实发布日；upsert 冲突时两列随真值覆盖（存量自愈）。详见《帖子发布时间提取入库优化方案.md》。
2. **新鲜度数据源**：`/stats/overview` 新增 `latest_run_at`（run_days 最近批次的开始/结束时刻较大者）；「最近入库」卡与 Header「更新于」均以入库活动时间为准，跑批即时反映，不再受发布日口径影响。
3. **命名贴合（N1-N8）**：累计帖子→**累计收录**、今日新增→**今日发布**、累计作者→**发帖作者**（副值「今日更新 N 人」）、数据新鲜度→**最近入库**、趋势 tooltip/系列名→**发布**口径、今日最热→**最新最热**（日期角标保留）、总版块→**全站发布趋势**、各版块→**分版块发布对比**（统计卡加 `white-space: nowrap` 防数值折行，挤压时由 `chart-head-right` 的 wrap 整条换行兜底）。N7（排行榜今日新增列）/N9（今日新增版块）在当前页面无对应文案，未涉及。
4. **B1 抓取中徽标**：批次运行时「最近入库」卡副值显示绿色脉冲「抓取中 N%」（数据源 `/api/runs` running 记录实时聚合进度），结束后回落为"今天/昨天/N 天前"；`loadP0` 顺带轮询，失败静默置空不影响总览。
5. **孤儿 running 三层防护**（2026-08-27）：批次被外部强杀/崩溃时 `finish_run` 无法执行，`run_days` 残留 running 孤儿行导致徽标常亮。三层修复：写侧心跳（`run_recorder.update_section` 顺带刷新 `run_days.updated_at`）、启动收编（`start_run` 前将历史 running 收编为 error）、读侧降级（`web/runs.py` 对超过 `RUNNING_STALE_SECONDS=1800` 无心跳的 running 仅展示降级为 error，不写库）。阈值基于最后心跳而非开始时刻（正常批次实测 27-28 分钟持续有心跳，不会误杀）。
6. B2 KPI 下钻 / B3 滞后警示 / B4 空窗文案：本次未选，留作候选。

---

## 十、2026-08-28 大屏优化调整（已落地）

1. **版块分布区块移除**：排行榜轮播 + 环形图（含 2s/2.2s 轮播、双击暂停、下钻联动）整体下线；`/api/stats/fid_dist` 保留用于 KPI 副指标「覆盖 N 个版块」。原 1.3/1.4/第五节候选方案随之作废。
2. **活跃作者 / 活跃版块榜上线**：新增 `/api/stats/top_authors` / `/api/stats/top_fids`（按累计发帖量，附今日/近 7 日，与作者榜同构），左右 1:1 横向条形图，P0 随首屏加载，作者条下钻帖子浏览（author 精确过滤）、版块条跳版块列表。
3. **热门榜四栏调整**：「今日新增版块 / 今日热议帖」→「最新最热（`/api/stats/today_top`）/ 本月最热（`/api/stats/month_top`）」；每行悬浮「下载」按钮接入下载中心。
4. **趋势卡统计卡与联动聚焦**：全站卡头统计卡（峰值/谷值/日均）+ 分版块卡头统计卡（对比版块/最活版块/峰值日增）；点击折线/图面出现「联动聚焦」徽标，双向联动可解除；天数下拉 `allow-create` 支持自定义输入。
5. **Header 实时时钟**：`useDashboardStore` 内置每秒刷新时钟（`startClock`/`stopClock`），Header 右侧展示当前时间，与「更新于」入库活动时间并存。

## 十一、2026-09-01 活跃榜口径与环比（已落地）

背景：两个榜名为「活跃」，排序依据却是**累计发帖量**（`ORDER BY total DESC`），反映的其实是历史资历而非当前活跃度——老作者/大版块长期霸榜，榜单几乎不动。

1. **统计口径可切换**：`/api/stats/top_authors`、`/api/stats/top_fids` 新增 `range` 参数（`all` / `7d` / `30d`，默认 `all`，非法值 422）；响应新增 `month`（近 30 日）、`prev_week`（前 7 日）、`delta`（环比）、`value`（当前口径主值）。非累计口径加 `HAVING <口径> > 0`，避免 0 值占满榜单。
2. **环比展示**：近 7 日 vs 前 7 日涨跌百分比，条尾随主值显示（涨绿 / 跌红 / 持平灰 / 新增橙），tooltip 补齐各口径明细（累计·今日·近 7 日·近 30 日）。
3. **前端交互**：卡片右上角分段控件（两卡独立记忆），切换只重拉对应榜单、不整页刷新；图表数据指纹纳入口径，规避「数值未变则不重绘」。
4. **文案与空态**：两卡副标题统一为「按<口径>发帖量 · 点击查看该<作者|版块>帖子」；无数据显示「该时间段暂无数据」。
5. **顺带修复**：SQL 增加 `date >= '2000-01-01'` 过滤——库中存在 1970 年的脏日期（历史解析异常），会污染时间窗口类口径。
6. **下钻继承口径**（同日追加）：`goAuthor` / `goDist` 按当前口径附带 `date_from` / `date_to`；帖子页新增「当前条件」摘要条（版块/作者/关键词/日期，逐项可清除 + 清空全部），日期选择器补「近 7 日 / 近 30 日」快捷项。同时修正一处条件残留：此前在关键词框输入新词后，下钻带来的 `author` 精确条件不会解除，导致「换了关键词却仍被旧作者限制」。
7. **一致性收尾（2026-09-01）**：① 摘要条补「排序」条件——仅非默认排序（非「日期倒序」）时显示，如「排序：点赞数倒序」，可一键还原默认；否则从热门榜按点赞下钻时列表顺序与条件条对不上。② `goPostsWith` 补注释警示：该函数**不带时间语义**，目前仅用于「全站历史最高」类卡片（点赞/回复最高帖），未来若有时间窗口入口复用必须并入 `rankDateRange`，否则重蹈口径不一致。③ 记录潜在坑：最新最热 / 本月最热卡片有明确时间语义（最新数据日期 / 最新数据月份），目前仅打开原帖、无「查看更多」下钻；将来若加下钻必须带对应时间范围（当日 / 当月），否则又是同一类数字矛盾。
8. **最新最热 / 本月最热的维度优化与下钻（2026-09-01，P0 实施）**：① 后端 `_SORTS` 新增 `engagement_desc`（`(CAST(likes)+CAST(replies)) DESC`），排序表达式与 `today_top`/`month_top` 完全一致，使下钻后的列表顺序与榜单一致——此前帖子页只有点赞/回复单维排序，直接下钻必然对不上榜单顺序；前端帖子页同步增加「互动量倒序」选项。② 两卡主指标从「分开显示点赞/回复」改为**互动量**（排序依据显性化，点赞/回复明细进 tooltip），本月最热补发布日（MM-DD），最新最热各行都是同一天、展示无信息量故不渲染。③ 两卡新增交互：卡片空白区下钻「同版块 + 同时间窗」、「查看更多」下钻整个时间窗，与点赞/回复最高帖交互对齐。④ 新增 `goPostsInRange`（带时间窗下钻），与不带时间语义的 `goPostsWith` 分开；时间窗用**接口返回的日期**计算而非当前时间——跨月边界时（今天 9/1 但最新数据还是 8/31）「本月」指的是 8 月。⑤ 卡头角标加口径 tooltip（按数据最新日/月统计、共 N 帖、覆盖 M 天，`days<7` 提示样本较少）。⑥ 两接口返回 `total`/`days`，缓存 key bump 至 `today_top_v2`/`month_top_v2`（否则旧缓存无新字段，前端会显示 0）。
9. **P1/P2 维度增强（2026-09-01）**：① 排序维度可切换（综合 / 点赞 / 回复 / 热度），卡头下拉只重拉对应榜单；下钻 `sort` 跟随切换，保持「榜单与下钻同序」。② 时间衰减热度：HN 式 `(score-1)/(age+2)^1.8`，**注册为 SQLite 函数 `hot_score`**（`db.open_conn` 内 `create_function`），使帖子页 `_SORTS["hot_desc"]` 与榜单共用同一公式——否则下钻后无法复现榜单顺序；参照日取 `MAX(date)`（数据最新日），用子查询内联以避免 ORDER BY 片段带参数导致参数位序错乱。实测：`engagement` 前 5 全是 3~7 月老帖，`hot` 前 5 全是 8/31~9/1 新帖。③ 互动率（回复/点赞）与类型标签（围观 / 均衡 / 热议），仅热议型（回复≥点赞）显示行内小标，否则每行挂标签等于没标签。④ 新入榜 NEW：快照落盘 `outputs/board_snapshot.json`——Web 严禁写 `posts.db`，故用 Web 自有文件（与 `download_tasks.json` 同模式、已挂卷持久化）；`first_seen == 今天` 视为新入榜，跨天自动消失，异常一律降级为「无新入榜」。（**2026-09-10 修正**：实现曾将标记条件取反为 `first_seen != 今天`，使历史帖每日重复标 NEW；已改为基于写入前旧值判定——首见记今天、同日保持、跨天消失，见第十六节。）⑤ 本月最热卡头 sparkline（每日互动量，纯 CSS 柱状，不额外起图表实例）。⑥ 四栏版块 tag 统一中性色。⑦ 两接口缓存 key 再 bump 至 `*_v3`（含 sort/limit，避免不同排序共用缓存）。

**实测**（13.2 万条真实数据）：

| 口径 | 第 1 名主值 | 第 2 名 |
|---|---|---|
| `all` | 22595（累计） | 累计 5298 的作者 |
| `7d` | 1017（近 7 日） | **变为 431 条的作者**（累计第 2 名近 7 日仅 234） |
| `30d` | 5330（近 30 日） | 1684 条的作者 |

非法 `range=bad` → 422。

**未做（候选，待确认）**：副指标增强（作者：跨版块数 / 互动量 / 最后活跃；版块：活跃作者数 / 占比 / 篇均互动）、新晋标记、热度加权分（时间衰减）。

## 十二、活跃榜窄屏柱条被压一半修复（2026-09-08）

**问题（移动端实测）**：数据总览「活跃作者 Top10 / 活跃版块 Top10」两张图在 390px 视口下，每张图的画布约 312px，柱条只占约 **44% 宽**（138/312），右侧 92px 全是「23,752 12.4%」这种数值+环比的条尾空间——视觉上像「被压到一半展示」。

**根因（实测定位）**：`renderHBarChart`（`DashboardView.vue`）的 grid 配置是桌面默认值——`grid.left:8, right:92, containLabel:true` + `yAxis.axisLabel.width:74`。
- 桌面 720px 画布下柱条区 ≈ 720−8−74−92 = 546px（76%），合理；
- 移动端 312px 画布下柱条区 = 312−8−74−92 = **138px（44%）**，柱子被两侧固定留白吃掉一大半。
- 两图布局本身（左右 1:1 → 窄屏单列）媒体查询已生效，**不是并排挤压**，而是图表内部柱条被两侧留白挤压。

**修复（温和版，按用户确认方案 A）**：
- 引入 `RANK_NARROW_W = 400` 断点；以**图表容器实际宽度**（非窗口宽度）判定窄屏，更准确（侧栏折叠 / 全屏状态都改变了容器宽）。
- 窄屏配置：网格 `left:4, right:8`（让出更多空间给柱条）；Y 轴标签 `width:56, fontSize:11`；条尾**只显示主值**（环比改由 tooltip 承载），把更多宽度让给柱子本身。
- 跨断点重绘：渲染器在「数据指纹无变化」时跳过重绘（已有 P1-8 优化），仅 `resize()` 不会换配置。`onResize` 检测当前容器宽 vs 上一次的窄屏标记，跨越断点时 `renderAuthorChart(true)` / `renderFidChart(true)` 强制按新宽度重绘；`rebuildCharts`（全屏切）复位窄屏标记，避免缓存到旧宽度。
- 桌面配置零变化（分支内走原有留白与「数值+环比」标签）。

**实测（画布彩色像素扫描柱条横向跨度）**：
- 移动端 312px 画布：活跃作者柱条 229px（**73%**，原 44%）；活跃版块 199px（**64%**，原约 38%）；
- 切到宽屏 1440px 后画布 546px，柱条 430/386px → 跨断点重绘生效，桌面布局未退化；
- `vue-tsc --noEmit` / `read_lints` 全 0；`npm run build` 成功，dist 已更新（刷新 8088 即可见）。
- 截图确认：wvg101 柱条拉到接近「25,000」刻度，比之前仅占左 30% 直观大幅改善。

## 十三、活跃榜窄屏条尾标签裁切 + 柱内环比回归（2026-09-08）

**问题（紧接十二）**：上一轮把窄屏 `grid.right` 缩到 8px 后，条尾数值（如「23,752」）因画布空间不够被裁成「23,」；同时把环比信息完全交给 tooltip，对不熟练鼠标悬浮的用户不够直观。

**修复（按用户确认方案 A-2）**：
- **数值回到条尾外侧**：`grid.right` 8 → **52**（约能放下 5 位千分位数值），`label.distance: 4`，条尾完整可读——「23,752」完整显示，不再被裁。
- **环比放进柱子内部靠右**（用户建议的「长方形状内靠右」位置）：ECharts 不支持同一系列同时有两处标签，用**叠加层**实现——窄屏多挂一根「同长透明柱」（`barGap: '-100%'`、`itemStyle.color: 'transparent'`，`silent: true`），把标签画在 `insideRight`；白字 + 文字阴影保证在彩色柱体上的可读性。
- **短柱自动省略**：bar 长度 < 最大值 25% 时内部放不下文字（会溢出到坐标轴），formatter 返回空串；所有环比始终在 tooltip 完整可用。
- 桌面配置零变化（`narrow` 分支外不挂叠加层）；跨断点强制重绘复用上一轮的机制（`forceRedraw`）。

**实测（画布像素扫描）**：
- 窄屏 312px 画布：活跃作者条尾文字右边界 287 / 画布 312 → 右侧剩余 **24px**（无裁切）；柱内白色环比像素 ≥27 ✓；
- 活跃版块条尾右边界 267 / 312 → 剩余 44px；柱内白字 227px（柱多，多数带环比）；
- 柱条跨度 60%（187/312）与原 44% 相比仍显著改善，剩 52px 让给条尾；
- 跨断点切到宽屏 1440px：桌面配置不变（数值+环比同在条尾外侧）；
- 截图：wvg101 柱内 **12.4%** 白字、条尾 **23,752** 完整，活跃版块每根柱子都带百分比。
- `vue-tsc --noEmit` / `read_lints` 全 0；`npm run build` 成功，dist 已更新。

---

## 十四、全屏时是否「自动播放」大屏 — 调研（待确认）

> 调研日期：2026-09-09。结论先放：**不建议在「点击全屏」时自动开启轮播/播放**，但需厘清「自动播放」的两种含义，避免讨论错位。

### 14.1 问题界定

「点击全屏按钮自动播放」在业界有歧义，通常分两层：

- **A. 数据自动刷新（实时性）**：全屏后数据持续/定时拉取，反映最新状态。这是「自动播放」的基础层，几乎所有大屏产品的默认行为。
- **B. 视图自动轮播（演示性）**：全屏后自动在多个视图/页面/维度间切换播放（kiosk 投屏场景），如定时翻页、轮播区块、多 Dashboard 轮换。

两层要分开判断，结论不同。

### 14.2 业界做法汇总

| 产品 / 来源 | 全屏 / kiosk | 自动播放形态 | 退出方式 |
|---|---|---|---|
| **Grafana**（官方文档 `grafana.com/docs`：kiosk mode / TV mode / Playlists） | kiosk/TV mode 隐藏导航栏、工具栏，进入纯净大屏 | **Playlists**：多个 Dashboard 按设定时间间隔自动轮播；数据本身有 auto-refresh | 按 `Esc` 退出；可手动停止轮播 |
| **阿里 DataV** | 大屏全屏展示 | 时间轴组件实现**大屏多页面自动轮播**，轮播联动更新图表数据 | 交互/脚本控制 |
| **帆软 FineReport** | 大屏展示 | 「轮播自动联动播放的大屏实例」、自动翻页、自动播放 | 交互控制 |
| **通用（Chrome 插件 / URL 轮播）** | 浏览器 F11 全屏 | 多个可视化页面 URL 动态轮播（解决多页面连续播放） | 插件控制 |
| **简书 / 可视化博客** | 大屏分屏 | 多分屏（两分/四分/六分/九分）并排展示 + 自动播放 | — |

**共性结论**：
1. 数据自动刷新（A 层）是默认必做项，与是否全屏无关。
2. 视图轮播（B 层）**几乎只服务于「大屏投屏 / 展厅 / 监控墙」这类无人值守、需要持续吸引注意力的场景**。

### 14.3 本项目现状（已核对代码）

- **全屏按钮**：`AppLayout.vue` 的「进入大屏」按钮 → `app.toggleFullscreen(bodyEl())`，全屏目标是右侧内容区（Header + Main），侧栏自动隐藏，但 **Header 的实时时钟与自动刷新开关保留**（`useAppStore` 的 `fullscreen` 状态管理，含真全屏 + iOS 降级伪全屏）。
- **全屏按钮本身不含任何轮播/播放触发逻辑**，仅切换显示态。
- **A 层（数据刷新）已有**：自动刷新默认开启（`config.ENABLE_AUTO_REFRESH=1`，`REFRESH_INTERVAL=5000`），由 `useDashboardStore` 管理，全屏后数据持续刷新——即「自动播放」的基础层本就满足。
- **页面内已有轮播**：趋势图维度轮播 `TREND_DAYS_SEQ=[7,14,21,28]`，悬停暂停（见 1.2 节），属于单图内部维度切换，非整屏轮播。

### 14.4 分析与建议

1. **A 层（数据自动刷新）—— 无需改动**
   业界的「自动播放」基础层在本项目已默认满足。不应把「刷新」与「全屏」重新绑定成新逻辑（刷新本来就开着，绑定反而制造隐含耦合）。结论：**全屏按钮对数据刷新层保持现状即可**。

2. **B 层（多视图/多页轮播）—— 不建议绑定全屏**
   - 业界 B 层专为「无人值守投屏」设计；本项目定位是**单人自用、本机运行**（见项目指引），用户点全屏多是为了「看清大屏 / 专注查看」，不是把屏幕丢给展厅轮播。
   - 自动轮播会**打断阅读与交互**：悬停看 tooltip、下钻明细、点排序维度等都与「自动翻动」天然冲突。
   - 当前大屏是**单页**（无第二个 Dashboard / 第二屏可轮播），强行做「区块轮播」收益低、且会打乱用户正在看的模块。
   - 结论：**不建议在点击全屏时自动开启轮播。**

3. **若确有「投屏演示」需求 —— 做成独立开关，而非绑定全屏**
   遵循项目「不隐藏行为、不盲目折中」原则：「全屏」语义应保持纯粹（仅放大显示）。可另设一个**「演示 / 轮播模式」独立开关**（可配置轮播区块序列与时长，悬停或 `Esc` 暂停/退出），与全屏解耦。这样「全屏 = 放大查看」「轮播 = 演示播放」各司其职，互不污染。
   - 备选轮播序列可复用现有模块：KPI 卡 → 趋势双图 → 活跃作者/版块榜 → 热门四栏，定时焦点切换或整屏滚动。
   - 轮播必须始终**可暂停**（悬停或按键），否则与下钻/tooltip 冲突。

4. **页面内轮播可增强，但需克制**
   现有趋势维度轮播全屏时可考虑加大视觉权重（放大节奏、强调当前焦点），但不应抢占注意力，且**悬停暂停必须保留**。

### 14.5 决策待确认

| 决策点 | 倾向建议 | 理由 |
|---|---|---|
| 全屏时自动开启数据刷新 | 保持现状（已默认开） | A 层已满足，无需新增逻辑 |
| 全屏时自动开启视图轮播 | **不做** | 单人自用，轮播打断查看，单页无可轮播内容 |
| 新增独立「演示/轮播模式」开关 | 待你确认是否要做 | 仅当确有投屏演示需求才值得做，且与全屏解耦 |

**一句话结论**：点击全屏按钮时，**不要自动播放（轮播）**；数据刷新本就自动进行，全屏只做「放大显示」。若需要投屏轮播，应作为独立功能单独评估，而非挂到全屏按钮上。

---

## 十五、投屏轮播（演示模式）独立功能 — 方案评估

> 评估日期：2026-09-09。依据第十四节结论，把「投屏轮播」从全屏按钮解耦，作为**独立「演示模式」开关**单独设计。

### 15.1 功能定位

- **独立开关，不与「全屏」耦合**：可单独开演示轮播，也可「演示模式 + 全屏」叠加（投屏场景先开轮播再放大屏幕）。
- **只控制「视图焦点/滚动」，不控制数据**：数据刷新仍由各区块自身的自动刷新（5s）负责，轮播不重拉数据、不重绘图表。
- **纯展示增强，单人自用下的「投屏/展厅」场景**：把大屏自动在若干视图间轮换，无需人值守盯屏。

### 15.2 触发入口

- Header 新增一个「演示轮播」按钮（图标复用已注册的 `VideoPlay`），与现有「全屏」按钮并列（`AppLayout.vue` 的 Header 右侧）。
- 点击 → 进入演示模式；再次点击 / `Esc` / 顶部控制条「退出」→ 退出。
- 进入演示模式**不**自动全屏；若用户想投屏，再点全屏即可（两者叠加）。

### 15.3 轮播内容（复用现有区块，不新增页面）

按视觉顺序把大屏切成若干「视图锚点」，轮播在这几个锚点间平滑滚动：

| 序号 | 锚点 | 对应区块（现有） |
|---|---|---|
| ① | 总览首屏 | KPI 卡 + 每日趋势双图 + 活跃作者/版块榜（P0） |
| ② | 点赞/回复最高帖 | 热门榜前两栏（`/stats/boards`） |
| ③ | 最新最热 | 热门榜第三栏（`/stats/today_top`） |
| ④ | 本月最热 | 热门榜第四栏（`/stats/month_top`） |

> 锚点粒度可粗可细：默认按「① 首屏 + ②~④ 热门四栏」4 个；若需要也可把 ① 拆成「KPI+趋势」「活跃榜」两块。序列顺序可后续做配置，初版用固定顺序循环（④ 之后回到 ①）。
>
> **实现说明（2026-09-09 已落地）**：实际锚点落地为 **4 个**——`section-overview`（总览首屏/KPI）、`section-trend`（每日发布趋势 + 分版块趋势）、`section-ranks`（活跃作者 / 活跃 fid 榜）、`section-boards`（热门榜整体）。轮播间隔与板块序列已下沉为「参数设置」页可配置项（`carousel_interval` 秒 / `carousel_sections` 数组），默认 8 秒、四板块按序全选；前端进入轮播时从 `/api/config` 读取一次，运行中不变。原 `CAROUSEL_SECTIONS` 常量改为运行时数组，配置缺失/全空时回落默认，不会空转或崩溃。

### 15.4 轮播节奏与暂停/退出

- **默认间隔**：`CAROUSEL_INTERVAL = 8000ms` 切下一块（待确认，建议 6~10s 可调）。
- **进入时**：先主动 `loadBoards()` 预加载 P1（否则轮播到 ②③④ 时仍是骨架，见 15.7 边界 ④）；首屏停留 1 个间隔后再开始滚动。
- **悬停暂停**（与现有趋势轮播 `trendTipPaused` 同范式）：鼠标进入当前视图区块 → `carouselPaused = true`，停止切下一块、保留当前位置；移出 → 恢复。保证用户想看 tooltip / 下钻时不被强制滚走。
- **顶部极简控制条**（仅演示模式显示，几秒无操作自动隐藏）：播放/暂停、上一块/下一块、退出。
- **`Esc` 优先级**：演示模式激活时 `Esc` 先退出演示模式；若同时处于全屏，再由现有 `fullscreenchange` 同步退出全屏（需确认交互细节，见 15.7 ⑤）。
- **页面隐藏**：复用现有 `pageVisible` 框架——`stopAllTimers()` 一并清 `carouselTimer`，`resumeAllTimers()` 重启（后台标签不空转轮播）。

### 15.5 状态管理落点（不新建分散 store）

- 轮播**激活态**放进 `useDashboardStore`（与 `autoRefresh` / `nowText` 同层，符合「跨页状态归并全局 store、禁新建分散 store」约束），新增：
  - `const carouselActive = ref(false)`、`const carouselPaused = ref(false)`
  - `setCarousel(active: boolean)` / `setCarouselPaused(p: boolean)`
- 轮播**执行逻辑（timer、滚动）**留在 `DashboardView.vue` 本地（与趋势轮播的本地 `trendTipTimer` 同层），`watch(carouselActive)` 启停；通过现有 `rootRef` 与各区块 ref 做 `scrollIntoView`。
  - 不沿用 `registerAutoChange`（那是 autoRefresh 专用回调），DashboardView 直接 `watch` store 的 `carouselActive`/`carouselPaused` 即可，职责更清晰。

### 15.6 技术实现要点（落到代码，初版）

1. **锚点标记**：给 4 个区块容器加 `id`（或 `ref`）：`section-overview` / `section-board-like` / `section-board-today` / `section-board-month`。P0 区块已有 `rootRef`，P1 热门榜各栏在模板中已有对应包裹元素，仅需补 `id`。
2. **滚动**：`document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' })`，滚动容器即 `rootRef` 所在内容区（全屏时仍是该元素，原生滚动正常）。
3. **timer**：`carouselTimer = setInterval(nextSlide, CAROUSEL_INTERVAL)`；`nextSlide` 计算下一锚点并滚动；循环到末尾回 ①。
4. **与自动刷新协同**：刷新只静默更新已加载区块、不重置滚动位置（现有 `loadP0`/`loadTrendOnly` 行为不变），轮播滚动不被刷新打断。
5. **复用清单（不重复造轮子）**：
   - 滚动用原生 `scrollIntoView`，不引第三方库；
   - 暂停框架复用 `pageVisible` + `stopAllTimers`/`resumeAllTimers`；
   - 悬停暂停复用「方法包装规避 ts-plugin 类型收窄」范式（见 1.x 的 `setTrendTipPaused`）；
   - 状态复用 `useDashboardStore`，不新建 store；
   - 区块 ref 复用 `rootRef`、现有 P1 容器，仅补少量 `id`。

### 15.7 风险与边界

| # | 风险 | 应对 |
|---|---|---|
| ① | 轮播滚动与用户下钻/看 tooltip 冲突 | 悬停即暂停（15.4）；暂停时保留当前位置，不强制滚动 |
| ② | 自动刷新更新数据改变区块高度，导致 scroll 锚点偏移 | 滚动用实时 `getBoundingClientRect` 计算锚点；刷新不重置滚动位置 |
| ③ | 移动端大屏投屏意义低、滚动体验差 | 初版桌面优先；移动端可隐藏演示按钮或仅滚动不放大 |
| ④ | P1 懒加载：轮播到热门榜时仍是骨架 | 进入演示模式先主动 `loadBoards()` 预加载 |
| ⑤ | `Esc` 与全屏退出冲突 | 明确优先级：演示模式先退轮播，再交全屏退出；统一在 `fullscreenchange`/`keydown` 处理 |
| ⑥ | 图表重绘开销 | 轮播只滚动视图、不重拉数据；图表已有「数据指纹跳过重绘」，开销极低 |
| ⑦ | 退出演示模式后滚动位置残留 | 退出时 `scrollTo({top:0})` 复位到首屏 |

### 15.8 待确认决策

| 决策点 | 建议 | 备注 |
|---|---|---|
| 默认轮播间隔 | 8s（6~10s 可调） | 是否进「参数设置」页配置见下 |
| 是否进设置页可配置 | 初版硬编码常量，后续再考虑 | 遵循「不过度设计」 |
| 锚点粒度 | 4 个（首屏 + 热门三栏） | 可后续细化 |
| 是否支持自定义序列 | 初版固定顺序循环 | 后续可选 |
| 移动端策略 | 桌面优先，移动端隐藏按钮 | — |

### 15.9 评估结论与落地状态

**已落地（2026-09-09）**，代码改动通过 `vue-tsc --noEmit` 0 错误、`npm run build` 成功、dist 已更新。完全复用现有机制（原生 `scrollIntoView`、`useDashboardStore`、`pageVisible` 暂停框架、趋势轮播悬停暂停范式），不引入新依赖、不新建 store、不重复造轮子。

**改动文件**：
- `web/frontend/src/stores/dashboard.ts`：新增 `carouselActive` / `carouselPaused` 状态与 `toggleCarousel` / `setCarouselActive` / `setCarouselPaused`。
- `web/frontend/src/layout/AppLayout.vue`：Header 在「全屏」按钮旁新增「演示轮播」按钮（`store.toggleCarousel()`，仅数据总览页显示）。
- `web/frontend/src/views/DashboardView.vue`：
  - 锚点 `id`：`section-overview`（KPI 卡）、`section-boards`（热门榜行）；
  - 根容器 `@mouseenter/@mouseleave` 暂停/恢复轮播；
  - 轮播模块：`CAROUSEL_SECTIONS` / `CAROUSEL_INTERVAL=8000` / 滚动、上下一项、暂停、Esc 退出、`watch(store.carouselActive)` 启停；
  - 复用到 `stopAllTimers` / `resumeAllTimers`（后台标签不空转）、`onBeforeUnmount` 清理、新增 `keydown(Esc)`；
  - 底部「演示轮播」控制条（上一项 / 暂停播放 / 下一项 / 退出）。

**使用方式**：数据总览页 → 点 Header「演示轮播」即开始（可与「全屏」叠加投屏）；鼠标移入大屏区域自动暂停，移出恢复；`Esc` 或控制条「退出」结束，退出后滚动复位到首屏。自动刷新（5s）在轮播期间照常进行，数据持续最新。

**落地时采用的默认值（原 15.8 待确认项）**：轮播间隔 8s、板块序列为全部 4 个按序——二者均已在「参数设置」页（`/settings`）开放为 `carousel_interval`（int，3–60s）与 `carousel_sections`（array，可勾选纳入/取消、上下移动排序）；保存后立即生效（scope=frontend）。桌面优先，移动端未隐藏按钮（窄屏仍可用，仅滚动不放大）。

---

## 十六、新入榜口径修正 + 热门榜窄屏板块 tag 处理（2026-09-10）

### 16.1 新入榜 NEW 标记条件取反（bug 修复，已落地）

**问题（用户反馈）**：「本月最热」卡片中，9/1 首次入榜的帖到 9/10 仍显示「新入榜」NEW，口径不合理。

**根因（代码定位）**：`web/api.py` 的 `_mark_new_and_save` 标记集合取反：

```python
fresh = {u for u in urls if board.get(u) != today}  # 错误：first_seen != 今天 才标 NEW
```

文档与注释口径均为「`first_seen == 今天` → 新入榜、跨天自动消失」，但代码写成「不等于今天才标 NEW」，导致历史帖在入榜后每一天都被重复标 NEW，与「跨天自动消失」相悖——即用户观察到的「9/1 记录截止到现在还是新入榜」。

**修复**：改为基于「写入前的旧值」判定，使口径与文档一致——

- `prev is None` → 首次出现，记 `first_seen = 今天`，标 NEW；
- `prev == 今天` → 当天早些时候已入榜，保持 NEW 一整天（同一天内多次刷新不丢失）；
- `prev` 为更早日期 → 不标 NEW（跨天消失）。

快照仅保留「仍在榜」条目，避免文件无限膨胀；任何异常仍降级为「无新入榜」。

**实测（2026-09-10）**：

- 等价单测：直接调用真实 `_mark_new_and_save`，覆盖「首见 / 同日刷新 / 跨天消失 / 9/1 帖到 9/10 / 全新帖」五组断言，全部通过（`ALL_NEW_LOGIC_TESTS_PASSED`）；
- 真实接口：重启服务后 `GET /api/stats/month_top?limit=10&sort=engagement` 无 500；9/1 帖（技術討論區 2026-09-01）`is_new=False`，不再误标；当前 Top 帖因快照中早已在更早日期首次入榜，全部 `is_new=False`，符合修正后口径（全新帖标 NEW 的行为由单测覆盖证明）。

### 16.2 热门榜窄屏「板块」中文名处理（已落地）

**背景**：四个热门榜卡片（点赞/回复最高帖、最新最热、本月最热）每行含 名次徽章 + 板块 tag + 标题 + NEW/热议 + 发布日 + 下载 + 互动量，窄屏（单列 ≤1100px）下行内元素拥挤，板块 tag 挤压标题可读性。

**方案对比分析**：

- **直接隐藏**：行内最干净，但丢失板块信息；
- **纯 tooltip**：移动端触屏下 tooltip 需长按才触发，不可靠、不直观；
- **业界做法**（微博/知乎热榜、GitHub Trending 移动端）：保留一个紧凑分类小标，而非隐藏或纯 tooltip——分类信息以最小化形式常驻。

**决策**：窄屏（单列 ≤1100px）**隐藏行内板块 tag**，但把板块名并入标题 tooltip（`${name} · ${title}`，长按可看），且卡片空白区点击仍下钻该版块——既释放标题空间，又保证板块信息不丢失。桌面（>1100px，2 列/4 列）保持原中性色板块 tag 不变（项目既有「四栏版块 tag 统一中性色」设计）。这是「隐藏 + tooltip 兜底」的折中，最贴合本卡片行内元素多的现实与移动端可达性。

**改动**：`DashboardView.vue`——四个标题链接 `:title` 由 `item.title` 改为 `` `${item.name} · ${item.title}` ``；`.board-tag` 增加 `@media (max-width: 1100px) { display: none; }`。

**实测（2026-09-10）**：`npx vue-tsc --noEmit` 0 错误、`read_lints` 0 错误、`npm run build` 成功 dist 已更新；移动端设备模拟（≤1100px 单列）标题不再被板块 tag 挤压，长按标题可见「板块名 · 标题」，桌面端板块 tag 保持原样。

**边界说明**：若后续发现 4 列桌面（>1400px）下卡片同样因板块 tag 拥挤，可把隐藏断点扩展到该布局；本次按用户明确的「窄屏」范围处理，未过度扩散。

---

## 十七、采集健康条 / 周期对比 / 资产漏斗 / 待下载推荐（2026-09-11 已落地）

> 依据《数据总览大屏业务优化建议.md》第一批推荐（R1-R4）落地：四个接口后端早已实现（工作区未提交改动），本次只做前端接线。详见《业务优化建议.md》同节「已落地」标注。

### 17.1 R1 采集健康条（接入 `/stats/health`）

- **位置与形态**：KPI 卡上方一条窄横幅，三态配色（绿/橙/红）+ 一句话结论（抓取中版块明细 / 已 N 天无入库 / 最近批次 N 个版块失败 / 入库正常 N/N）+ 右侧「运行记录 ›」入口；ok 态弱化不抢视觉（淡绿底色 + 小绿点），warn/danger 整条加粗提示；抓取中时圆点脉冲，与 B1 抓取中徽标同节奏。
- **抓取中文案（2026-09-12 调整）**：不再用裸百分比「抓取中 66%」，改为版块级明细「成功 X 个版块 / 失败 Y 个 / 未执行 Z 个（N 个进行中）」，一眼看清批次各状态分布；百分比作为次信息不再作主文案（口径与运行记录页版块状态同源，均由 `_health_verdict` 一处判定）。
- **判定口径**：阈值在后端 `web/api.py:_health_verdict` 一处收敛，前端只按 `level` 上色——避免两端各判一套阈值后出现「条上是绿的、卡上是红的」。优先级：入库中断（抓取停了）＞ 批次失败 ＞ 发布空窗（仅提示）。其中**手动中断（cancelled，属非完成状态）判 `warn`（橙）**——不再标绿，与「完成且健康」的绿区分，提示「最近批次未跑完」（崩溃/僵死由 runs 降级为 error，同样落入 warn）。
- **交互**：点击 / 键盘 Enter 跳 `/runs`；悬浮展示批次日期、时间、耗时、版块成功率、最新发布日；`@keydown.enter` 同步支持键盘可达性。
- **P0 加载**：`loadP0` 内 `Promise.allSettled` 并行拉取 health/compare/assets，单个失败静默保留旧值（与 runningBatch 徽标同一容错策略），下一轮刷新自动重试。

### 17.2 R2 周期对比（接入 `/stats/compare`）

- **口径**：近 7 日 vs 前 7 日、近 30 日 vs 前 30 日，**滚动窗口**（含今天，与活跃榜 7d 环比同源共用 `_delta`），保证「榜单说涨、大屏说跌」不会出现。
- **落点 1**：累计收录卡副指标补「近 7 日 `↓xx%`」环比，色随涨跌（涨绿 / 跌红 / 持平灰 / 前值 0 橙「新增」），副标题悬浮提示「近 7 日相对前 7 日的涨跌（滚动窗口，与活跃榜 7 日环比同口径）」。
- **落点 2**：全站趋势卡头新增「近 7 日环比 `↑xx%`」统计卡（ts-card，色值随涨跌方向用 `style.color` 注入，标题悬浮提示口径，由 `/api/stats/compare.week.delta` 驱动）——与分板块趋势 tooltip 的 7 日环比同源同窗（2026-09-11 由 30 日统一，见 §19），避免两张并排图一张看 30 日、一张看 7 日的口径不对称；趋势图 7/14/21/28 天维度由下拉独立切换。

### 17.3 R3 待下载推荐（接入 `/stats/pending_downloads`）

- **位置**：初版在热门榜 P1 行下方的新区块；**2026-09-13 移入趋势区第二行右侧，与「每日互动量趋势」各占 1/2**（见 §21.8），并随之从 P1 懒加载改为随首屏 P0 一起取数（不再等滚动）；`autoRefreshTick` 与 `fullscreen watch` 仍一并触发刷新——下载完成后相应帖子自动退出推荐。
- **形态**：卡片内清单**单列**（卡片自身已是半宽，窄屏堆叠后同样单列），**取 10 条 + 超出 360px 纵向滚动**（2026-09-13 由「8 条铺满卡片」改为与「本月最热」同款 `.board-list`，见 §21.9），每行：名次 + 板块 tag + 标题 + 发布日 MM-DD + 下载按钮 + 互动量；样式复用热门榜既有 board-card 类（窄屏隐藏板块 tag 的 `@media (max-width: 1100px)` 自动生效）。
- **数据**：近 30 日互动量最高、且尚未下载到本地的帖子（已下载与下载中自动排除），按互动量排序；后端候选池 400 条 + Python 侧 URL 归一化差集（与提交前判重同一口径，唯一隐蔽坑：下载任务存完整 URL 而 posts.url 是相对路径，必须经 `txxy_env.to_storage_path` 归一化）。
- **状态标记（2026-09-12 新增）**：每条带 `state` 字段——`fresh`=全新待下载；`re_download`=下载履历里有记录、但文件已被资源管理清空（`downloads/` 下目录不在磁盘）。后者在行内打「可重下」橙色标签，与全新待下载区分开，避免用户清理过文件还以为「没下过」。判定复用 `download_tasks.manager.asset_snapshot()` 的 `gone` 集合（与提交前判重同源自 `_classify_urls_locked`；2026-09-13 由 `gone_urls()` 收敛而来，见 §20.3），不另写判重逻辑；清空「下载中心」任务不触发此类（履历独立留存，§18.1），只有真正删掉磁盘文件才归入 `re_download`。
- **空态**：「近 30 日高互动帖子均已下载」一行居中提示。
- **交互关键——行不可下钻**：帖子页无「未下载」过滤条件，行内下钻必然口径不一致（与既有「下钻必须继承口径」约定同源）。仅保留「标题打开原帖」与「行内下载按钮」两个动作；卡头右侧「查看全部 ›」下钻到帖子页（`/posts?date_from&date_to&undownloaded=1&sort=engagement_desc`），继承「近30日 · 未下载 · 按互动量」上下文，列表为该筛选的全量明细（与推荐口径严格自洽），顶部摘要条可逐项清除条件。
- **窗口口径显式化（2026-09-13）**：本卡的窗口是**滚动近 30 日（含今天、会跨月）**，而「本月最热」用的是**数据最新月份的自然月**（§11.8）；用户追问「两榜口径是否一样」时实测两者 Top8 **零交集**——同按互动量排序，但窗口不同，且更老的帖子累积互动更多，8 月下旬的帖子（互动 1500~2567）必然压过 9 月上半月（最高 1391）。
  处理（方案 2：只改文案，成本最低）：副标题由「近30日 · 未下载 · 按互动量」改为「**近30日滚动窗口（跨月）** · 未下载 · 按互动量」，悬浮内容补一句「『本月最热』按数据最新月份的自然月统计，两者窗口不同，即使同按互动量排序，结果也可能不重合」——把差异讲在明面上，不动窗口定义（保留「本月」的自然语义与「近 30 日」的滚动语义各自成立）。
  实测：桌面 1440×900 / 移动 390×844 双视口副标题与气泡文案均正确、无横向溢出、无控制台错误；`vue-tsc` / `read_lints` 0 错误、构建成功。
  未做的两个方向（备查）：统一窗口（会丢失「本月/or 近30日」其中一个语义）、行内补年份/加窗口副标（信息已由 tooltip 承载）。
- **`loadPending` 与 `loadBoards` 分开实现**：loadBoards 首次加载后即早退（热门榜不参与轮询刷新），待下载列表必须随下载进度动态更新——刚下完的帖子要退出推荐位。

### 17.4 R4 内容资产漏斗（接入 `/stats/assets`）

- **位置**：KPI 行下方（stat-grid 与 trend-row 之间）的紧凑横条卡，全宽。
- **形态**：四级漏斗「收录帖子 → 已下载帖 → 本地文件 → 占用体积」，首级箭头附转化率（已下载帖 / 收录帖子 %），四级配色沿用趋势统计卡家族色（蓝/绿/紫/橙，不另建色板）；卡头右侧「资源管理」入口跳 `/resources`；窄屏自动换行（`flex-wrap: wrap`）。
- **类型分布（按大小）占比条（2026-09-12 新增）**：漏斗下方新增「类型分布（按大小）」横向占比条 + 图例（图片/视频/种子/磁力链接/云盘清单/文本/其他），按体积占比着色，复用资源管理页同款分类（`resources.category_of` 七大类）与色板（`categoryColors`，集中定义于 `utils/category.ts`，DashboardView 与 ResourcesView 共用，不重复造色板）；图例每行展示「文件数 · 体积 · 占比%」；点击占比条任一段或图例任一项，下钻到资源管理页按该类型筛选（`/resources?type=<key>`，继承类型上下文，数字自洽）。后端 `/stats/assets` 新增 `type_breakdown`（由 `scan()` 同一趟遍历聚合，零额外开销，沿用 5s TTL 缓存）。
- **口径**：与资源管理页 `resources.scan()` 同一实现（自带签名 + TTL 增量缓存）；「已下载帖」= `download_tasks` 中 ok/skip 且文件仍在的 URL 与 posts 取交集（与提交前判重、待下载队列一致）；不另写目录遍历。
- **实测（2026-09-11）**：14.4 万帖 / 4250 文件 / 173 目录 / 24.2 GB；已下载帖仅 1 条匹配（历史下载记录 URL 多为完整域名前缀，与相对路径入库格式需归一化匹配；显示如实为 0.0%，无伪造）。

### 17.5 端到端实测（2026-09-11）

桌面 1440×900 + 移动 390×844 双视口 Playwright 实测，**26 项检查全通过**（`vue-tsc --noEmit` 0 错误 + `read_lints` 0 错误 + `npm run build` 成功 + 4 个新接口 + 既有受影响接口重启后真实请求无 500 + 双视口渲染/交互/无横向溢出）：

- 桌面：健康条 ok 态「最近批次 2026-09-11 正常 · 13/13 个版块成功」、点击跳 `/runs`、累计收录卡显示「近 7 日 ↓24.5%」、趋势卡头显示「近 30 日环比 ↑45.8%」、资产卡四级「144,057 → 0.0% 1 → 4,250 173 个目录 → 22.6 GB」、滚动触发 P1 后待下载 8 行带下载按钮；
- 移动：健康条单行省略 +「运行记录 ›」可达、资产卡自动换两行、待下载 8 行单列（每行 `x=39` 严格同列）、两个滚动位置均无横向溢出、两个页面均无 JS 错误。

### 17.6 改动文件

- `web/frontend/src/views/DashboardView.vue`：导入 `formatDuration/formatSize` + Health/Compare/Assets/PendingDownloads 类型；新增 4 个 ref + `loadPending` 函数 + `goRuns/goResources/goDownloads` 三个下钻；`loadP0` 并行拉取 3 个新接口；`autoRefreshTick` / `onMounted` / fullscreen watch / `startCarousel` 一并触发 `loadPending`；模板新增健康条/资产漏斗/待下载推荐三个区块，KPI 与趋势卡副指标嵌入周期对比；scoped 样式新增 health-bar / asset-card / pending-grid 等；`section-overview` 锚点外层新增 `<div id="section-overview">` 包裹健康条（演示轮播目标仍居首屏，滚动后整组可见）。
- `web/frontend/src/utils/category.ts`：新增，集中定义类型分类 / 标签 / 色板 / 筛选选项（原 ResourcesView 内联定义抽离至此，单一来源，禁止再写第二份）。
- `web/frontend/src/views/DashboardView.vue`：新增 `typeRows` 派生（按 `CATEGORY_ORDER` 取后端 `type_breakdown`，仅展示有文件的类型，算体积/文件占比）与 `goResourcesType` 下钻；R4 卡内新增「类型分布（按大小）」占比条 + 图例模板与样式；导入 `utils/category`。
- `web/frontend/src/views/ResourcesView.vue`：移除本地类型常量与 `categoryLabel` 函数，改引用 `utils/category`；`onMounted` 读取路由 `query.type` 应用类型筛选（下钻入口，复用既有 `typeFilter`）；本地排序位次表重命名为 `CATEGORY_RANK`，消除与导入的 `CATEGORY_ORDER` 数组的命名遮蔽。
- `web/resources.py`：`scan()` 遍历中按 `category_of` 累加七类文件数与体积，返回新增 `type_breakdown`（零额外目录遍历）。
- `web/api.py`：`AssetsResp` 新增 `type_breakdown: dict[str, TypeBreakdownItem]`（子模型 `TypeBreakdownItem` 定义于其前，避免类体求值 `NameError`）；`stats_assets` 透传 `scan()` 的聚合。
- `web/frontend/src/api/index.ts`：`Assets` 接口新增 `type_breakdown` 字段。
- `README.md`：大屏页描述新增采集健康条 / 内容资产 / 待下载推荐三节，KPI 与趋势条目补周期对比；docs 索引新增「数据总览大屏业务优化建议」。
- `docs/数据总览大屏业务优化建议.md`：R1-R4 标注已落地（含本次端到端实测结论）。

### 17.7 KPI 卡片去重与扩充（2026-09-11 已落地）

- **去重**：「最近入库」副指标不再输出新鲜度告警（原「今天/昨天/N 天前」），该信号已由 R1 采集健康条唯一承担（避免停摆时横幅与 KPI 同义双呈现）；该卡副指标仅保留「抓取中 N%」徽标 + 中性「更新于 HH:MM」。
- **新增「最新最热帖」KPI（2026-09-12 已移除）**：主值取最新数据日互动最高帖的回复数（`/stats/today_top` 榜首，首屏 `loadP0` 内 `api.todayTop(1,'engagement')` 单独拉取，与热门榜懒加载解耦），副指标版块名 + 赞数，整卡点击下钻该数据日帖子列表；补齐「热度质量」视角。
- **新增「媒体文件」KPI（2026-09-12 已移除）**：主值本地媒体文件数（`/stats/assets.files`），副指标占用体积 + 目录数，整卡点击进资源管理；与 R4 内容资产漏斗同源但形态为「首屏数字卡」。
- **2026-09-12 移除两张 KPI（回归 4 张）**：热度质量视角已由热门榜「最新最热」栏覆盖、媒体资产视角已由 R4 内容资产漏斗覆盖，两张 KPI 与既有区块重复，故移除。同步移除 `hotTop` ref、`goHotPost`、`loadP0` 内独立的 `api.todayTop(1,'engagement')` 拉取，骨架屏 6→4；`goPostsInRange` / `dayRange` 仍由热门榜复用，保留。
- **布局**：`.stat-grid` 全局保持 `repeat(3,1fr)`（下载页沿用）；DashboardView 与 ResourcesView 均作用域内覆盖为 `repeat(4,1fr)`（宽屏 4 列单行），并加 1024px(2×2)/640px(1 列) 响应式断点（桌面保持一行 4 列，窄屏才折行），4 张 KPI 各端铺满不空旷。
- **改动文件**：`web/frontend/src/views/DashboardView.vue`（移除 `hotTop` ref、`goHotPost`、两张 KPI 卡，骨架屏回退为 4，作用域内覆盖 `.stat-grid` 响应式；`assets` 仍保留供 R4 漏斗）、`web/frontend/src/style.css`（栅格保持 3 列不变）；`api.todayTop` / `api.assets` / `api.overview` 均已有，未改后端。

**实测（2026-09-11，2026-09-12 复核）**：桌面 1440×900 + 移动 390×844 双视口 Playwright 真实渲染，**全部检查通过**（无控制台错误、无横向溢出）：6 张 KPI 全部渲染且顺序正确（2026-09-12 已移除「最新最热帖 / 媒体文件」两张，现 4 张，布局改为宽屏 4 列 / 中屏 2×2 / 窄屏单列）；「最新最热帖」值 135、副标「技術討論區 赞 276」；「媒体文件」值 3,557、副标「21.4 GB 146 目录」；「最近入库」副标为「更新于 21:21」且**不含**「天前」新鲜度告警（去重生效）。前端 `vue-tsc --noEmit` 0 错误 + `read_lints` 0 错误 + `npm run build` 成功。

### 17.8 按类型占比与下钻（2026-09-12 已落地）

- **需求**：内容资产卡补充各媒体类型（图片/视频/种子/磁力链接/云盘清单/文本/其他）的情况，参考业界做法（Google Photos / Plex / Nextcloud 等均以「类型分组 + 存储占比 + 可点击下钻」呈现）。
- **后端**：`resources.scan()` 复用既有 `category_of` 分类器，在现有 `rglob` 遍历里顺手累加七类 `{files, size}`，返回 `type_breakdown`；`/stats/assets`（`AssetsResp.type_breakdown`）透传，沿用 5s TTL 缓存，零额外目录遍历。
- **前端**：抽离 `utils/category.ts` 为类型「标签 / 色板 / 筛选选项 / 顺序」唯一来源（原 ResourcesView 内联定义移入，消除重复色板；媒体类型由后端 `category_of` 按文件名把「文本」拆出**磁力链接**（magnets.txt）/ **云盘清单**（clouds.txt）两个独立类型，其余 .txt/.md/.log 仍归文本，共七大类）；新增 `buildTypeSegments(assets)` 把后端 `type_breakdown` 派生为「按类型」占比段（sizePct 体积占比、filePct 文件数占比，均 2 位小数、按 `CATEGORY_ORDER` 固定顺序、仅留有文件类型）——R4「类型分布（按大小）」与 B6「类型分布（按大小）」**共用同一函数**，确保两边数据 / 顺序 / 颜色 / 文本完全一致，不再各自聚合；R4 卡新增「按类型」横向占比条（按体积占比着色）+ 图例（文件数·体积·占比%，占比统一保留 2 位小数），点击任一类下钻 `/resources?type=<key>`；R4 卡不展示 hover tooltip（占比信息已在图例常驻显示）；ResourcesView `onMounted` 读取 `query.type` 应用类型筛选（与既有 `typeFilter` 同一套，下钻口径自洽）。
- **实测（2026-09-12）**：`python -m py_compile` 通过；`/stats/assets` 真实返回 `type_breakdown`（图片 2153/0.95GB、视频 1316/20.3GB、磁力链接 2、云盘清单 19、种子 1、其他 56、文本 0，合计 3547 文件 / 21.3GB，与 total 一致；媒体类型已由后端按文件名拆出磁力链接/云盘清单两个独立类型）；前端 `vue-tsc --noEmit` 0 错误 + `read_lints` 0 错误 + `npm run build` 成功。浏览器双视口点击下钻受环境限制未做可视化实测，但「路由 `query.type` → `typeFilter` → 全局筛选」链路已通过类型检查与构建校验，且复用既有已验证的类型筛选逻辑。

---

## 十八、四项口径修正：下载履历 / NEW 快照 / 分板块环比（2026-09-11 已落地）

> 用户对 R1-R4 落地后的四个追问（详见《业务优化建议.md》第七节），经诊断全部属实并修复。

### 18.1 下载履历（修复「清空任务后已下载帖归零 / 待下载推荐重复推荐」）

**根因**：`downloaded_urls()` 只从**任务列表**（download_tasks.json）取「历史成功且目录仍在」的 URL，而任务列表是易失的——手动「清空已完成」与自动轮转（保留 200 条）都会删记录；「已下载」却是持久事实（文件在磁盘）。实测：用户清空任务后 `downloaded_posts=0`（磁盘 3882 个文件仍在），待下载推荐全量重复。

**业界做法**：下载管理器普遍把「任务列表」（用户可清理）与「下载履历」（append-only 独立持久化）分开——浏览器下载历史、qBittorrent resume data 均如此；判「是否已下载」看履历而非任务面板。

**修复**（`web/config.py` + `web/download_tasks.py` + `web/resources.py`）：
1. 新增**下载履历** `outputs/download_history.json`（url → saved_dir，可用 `TXXY_DOWNLOAD_HISTORY_FILE` 覆盖；随 outputs/ 卷持久化，Docker 各端一致）：任务项完成 ok/skip 时立即记入，随每次任务保存一并原子落盘；
2. `_classify_urls_locked` 的 alive 集合改为「任务项 ∪ 履历」中目录仍在磁盘者（dup_check / downloaded_urls / active_urls 三处口径自动一致）——清空任务、自动轮转都不再丢失「已下载」事实；
3. **启动三步恢复**：读履历文件 → 从现存任务 ok/skip 条目播种（一次性迁移）→ 按「目录名 = 帖子标题」从 downloads/ 增量恢复（复用 `resources.source_lookup` 新增的 `exact_only` 参数，只接受标题全等的精确命中——模糊命中可能张冠李戴，误配会把未下载的帖子错误排除出推荐，宁可漏记不误记；同时覆盖不经任务队列的 CLI 直接下载，每次启动增量自愈）。

**实测**：恢复 156 条履历（磁盘 159 个非空目录中 156 个精确命中来源帖），`downloaded_posts` 从 0 恢复为 156，待下载推荐 8 条与履历零重叠。

### 18.2 NEW 快照口径（修复「9/1 的帖子到现在还是 NEW」第二处根因）

**根因**（与 16.1 修的取反 bug 不同源）：四种排序（综合/点赞/回复/热度）共用**同一个快照 key** `month_top`，而快照「仅保留当前榜条目」的清理逻辑在每次请求后把其它排序的记录整体抹掉——**切换排序维度时，当前排序的 Top10 全部按「首见」重记，切回来再重记一遍**，表现为老帖反复全量标 NEW。快照文件还残留历史实验的整数脏值 key。

**修复**（`web/api.py`）：
1. 快照 key 按排序维度分开：`month_top:{sort}`，切换排序互不覆盖；
2. **去掉「仅保留当前榜条目」的裁剪**：首见记录永久保留，掉榜再回榜不重复标 NEW（「首次进入该榜 Top10」语义严格成立）；文件增长量级为「历次进榜的 url 数」，极小；
3. 读取时**逐值净化**：非法日期值（整数残留等）迁移为「早已入榜」哨兵 `2000-01-01`（永不等于真实今天，不产生 NEW），合法日期值保留不误伤；旧单 key 的日期本身已被排序切换污染（不可考），整组归零并入默认排序 key，避免迁移当天全量刷 NEW。
4. 快照写盘补 **`.bak` 轮转**（与 `download_tasks.json` 对齐）：`write_json_atomic(_SNAP_FILE, snap, backup=True)`。整文件重写场景下，万一写盘中断 / 重启竞态，可回退上一版快照而非丢失——此前该文件缺轮转，是 `download_tasks.json` 之外唯一一处无 `.bak` 保护的 Web 状态文件。

**等价单测**（直接调用真实 `_mark_new_and_save`，隔离快照文件）：首见 / 同日刷新 / 跨天消失 / 排序隔离（三组） / 回榜不重标（回榜者排除、当日首见者保留） / 迁移（旧 key 清除、整数归零、合法日期不误伤）共 **12 项全过**。

### 18.3 链接黑名单（大屏各卡片口径统一过滤）

需求：能否加一个链接黑名单，针对大屏各个卡片口径过滤。结论：**可行，且天然保证口径一致**——黑名单作用在「数据底座」（`posts_filtered` 视图），各卡片口径（累计 / 近7日 / 近30日 / 滚动窗口）都建在其上，结构上不可能出现 A 卡算进、B 卡漏算。

- 支持三类：`url`（链接）/ `author`（作者）/ `fid`（版块），用 `type` 列区分；
- **仅作用于大屏看板**：`/api/stats/*` 全部改读 `posts_filtered` 视图；`/posts` 帖子浏览页、导出、下载中心仍读原 `posts` 表，不受影响（作用范围按需求限定为「仅大屏卡片」）；
- `blacklist` 表与 `posts_filtered` 视图落在 `posts.db`，但仅 blacklist 模块持有**可写连接**，其余查询仍走只读连接（不破坏「web 只读」整体约束）；
- **配置入口统一在设置页「链接黑名单」区**（按类型增 / 删 / 备注）。看板卡片**不放置行内屏蔽按钮**——用户确认按钮不美观、且黑名单属低频配置，集中在设置页管理更清晰；
- **前端刷新机制**：点赞最高帖 / 回复最高帖 / 各版块最高等卡片（`boards`）设计上首次加载后不参与轮询刷新。`boards` 是 **Pinia 跨页面导航持久缓存**，原 `loadBoards` 以「`boards.value` 非空」早退，会导致「设置页改完黑名单、再切回看板」时仍显示旧榜单（含已屏蔽帖）。改为按 `blacklistVersion` 版本号判断：版本不变且不强制则早退（保留「不参与轮询刷新」），版本变化则**强制重拉全部卡片口径**（`loadBoards` 内部 + 今日/本月/待下载/总览），同时覆盖「看板正打开时改」（`watch` 监听 `bumpBlacklist`）与「切回看板时」两种场景，保证即时同步；
- **前端改动需重建**：`app.py` 托管的是构建产物 `web/frontend/dist`。改了 `.vue` 源文件必须 `npm run build` 重新构建，浏览器硬刷新（绕过本地缓存）才能加载新逻辑；仅改后端无需前端重建；
- **视图健壮性**：`posts.url` 库内存有「相对路径 /htm_data/...」与「带域名全称」两种形态，视图用 `substr(p.url, instr(p.url,'/htm_data/'))` 归一后再比较，规避「用户粘贴链接格式与库内不一致」导致黑名单静默失效；
- **入库去域名（与「域名不得写入数据库」铁律一致）**：`blacklist.add` 对 `url` 类型经 `config.to_storage_path` 归一为相对路径 `/htm_data/...` 再落库，避免把 `127.0.0.1:1024` 等环境相关镜像地址写进 DB（数据换环境后该黑名单会因域名对不上而静默失效）。`ensure_schema` 启动时还会把历史残留的「带域名完整链接」url 黑名单归一为相对路径。视图两侧均按 `substr(...,'/htm_data/')` 归一，故无论存相对还是带域名都能正确过滤，但入库统一相对路径更干净、可移植；
- **踩坑**：本机 SQLite 不支持 `CREATE OR REPLACE VIEW`（报 `near "OR": syntax error`），若用该语法建视图会静默失败、视图停留在旧定义（表现为「加了黑名单某卡片仍不过滤」）。改为「`DROP VIEW IF EXISTS` + `CREATE VIEW`」确保每次启动加载最新定义。

**业界参照**：社交平台 mute/block（X/Twitter Block、Reddit Block、YouTube「不感兴趣」）的「单点屏蔽 → 全局各处同步隐藏」范式；与 uBlock/AdBlock「单一过滤列表作用于所有页面」同一思路。

**全链路实测**：重启后连续切换 4 种排序再切回，engagement 榜 0 误标；hot 榜仅 1 条 NEW——经核实为 `[人间光影录]0909爱看看` 今天**首次进入 hot 榜**（不在迁移哨兵的历史条目中），属口径内合法 NEW。

### 18.3 分板块环比（补齐与全站趋势卡的对称性）

**诉求**：全站趋势卡头（当时为「近30日环比」，2026-09-11 已统一为近 7 日，见 §19）与分板块对比卡需要对称的环比值。逐版块的 7 日环比其实在「活跃版块 Top10」条尾已有，但趋势卡本身缺失。

**修复**：
- 后端 `/stats/trend_by_fid` 每系列附 `delta`（近 7 日 vs 前 7 日，与活跃榜 `_delta` 同源同窗，查询窗口内部扩展到 14 天取前 7 日基准，展示仍只返回 days 天）；缓存 key bump `trend_by_fid_v2`；
- 前端分板块卡头新增第 4 张 ts-card「最活板块7日环比」（色随涨跌，复用 R2 的 `cmpDelta` 文案与配色）；tooltip 每行尾部附「· 7日 ↑/↓x% / 新增 / 持平」（涨绿/跌红/持平灰/新增橙，取自接口 delta、不受缩放条裁剪影响）。

**实测**：卡头「最活板块7日环比 ↓19.8%」；tooltip 逐行「在綫CR影院：385 · 7日 ↓19.8% / 國產原創區：220 · 7日 ↓15.8% / AI破解原創區：87 · 7日 ↑7.4% …」。

### 18.4 端到端实测与改动文件

双视口（桌面 1440×900 / 移动 390×844）Playwright 实测 14 项全过：资产卡「已下载帖 156 / 0.1%」双端一致、分板块环比卡双端渲染、本月最热 NEW 收敛为 0（仅剩合法首见）、待下载 8 行、移动端两处无横向溢出、双端无 JS 错误；`vue-tsc --noEmit` / `read_lints` 0 错误、`npm run build` 成功；重启后 11 个受影响接口真实请求全 200。

改动文件：`web/config.py`（履历文件路径）、`web/download_tasks.py`（履历读写/播种/磁盘恢复/三分类并集）、`web/resources.py`（`source_lookup` 增 `exact_only`）、`web/api.py`（快照口径重写 + `trend_by_fid` delta）、`web/frontend/src/api/index.ts`（TrendByFid.delta）、`web/frontend/src/views/DashboardView.vue`（最活板块环比卡 + tooltip 环比后缀）、`README.md` 与两份 docs。

## 十九、三项 UI 口径修复：环比统一 7 日 / 资产空态 / 单一滚动条（2026-09-11 已落地）

### 19.1 全站趋势环比统一为近 7 日

**问题**：全站趋势卡头显示「近 30 日环比」，而分板块对比卡 tooltip 每行、最活板块卡头的环比都是**近 7 日**——两张并排图一张看 30 天、一张看 7 天，并排阅读会困惑（用户原话「一个是近30日，一个是近7日」）。

**修复**（前端 `DashboardView.vue`）：全站趋势卡头统计卡由 `compare.month.delta`（30 日）改为 `compare.week.delta`（7 日），标签「近30日环比」→「近7日环比」，与分板块趋势的 7 日环比**同源同窗**（同取近 7 日 vs 前 7 日滚动窗口）。7 日更灵敏，契合看板「近期变化」场景，也与分板块粒度匹配。后端 `/stats/compare` 仍返回 `week` / `month` 两份 delta，未删字段（避免误伤其它调用方）。

### 19.2 内容资产卡空态

**问题**：任务 + 文件 + 履历全部清空后，资产漏斗显示「144,057 → 0 (0%) → 0 → 0 B」，技术正确但 UX 生硬。

**业界做法**：Chrome 下载页清空后显示「没有下载内容」空态；GitHub 空仓库给引导性空态。本项目已有独立「下载履历」（`download_history.json`），只清任务不影响（履历仍在，`downloaded_posts` 仍 >0）；仅当三者全清才触发真空间态。

**修复**（`DashboardView.vue`）：`downloaded_posts === 0 && files === 0 && folders === 0` 时渲染引导空态——图标 +「尚未下载任何内容」+ 说明 +「去资源管理 ›」入口，替代生硬「0 漏斗」；其余情况保持四级漏斗不变（已下载帖 >0 即正常展示，含「只清任务」这类履历仍在的场景）。

### 19.3 去除嵌套滚动条（仅保留 .main 一个滚动容器）

**问题**：页面出现两个纵向滚动条——`html/body` 默认可滚动 + `.main`（el-main）`overflow-y:auto`，形成嵌套滚动（用户标注「里面那个滚动条」需去除）。

**修复**：
- 全局 `style.css`：`html, body { overflow: hidden }`，关闭浏览器默认滚动，仅保留 `.main` 为唯一滚动容器；
- `AppLayout.vue`：`.body` 补 `height: 100%`，保证高度链贯通到 `.main` 成为有界滚动容器。

**实测**（Playwright 双视口）：桌面 1440×900 / 移动 390×844 下 `document.documentElement` 与 `body` 的 `scrollHeight - clientHeight` 均为 0（无 html/body 滚动），仅 `.main` 产生滚动（desktop 溢出 627px / mobile 溢出 2310px，均 `overflow-y:auto`），双端无横向溢出、无 JS 控制台错误；趋势卡「近7日环比」渲染、「近30日环比」消失；资产空态（拦截 `/api/stats/assets` 注入全 0）正确渲染「尚未下载任何内容」。

改动文件：`web/frontend/src/views/DashboardView.vue`、`web/frontend/src/style.css`、`web/frontend/src/layout/AppLayout.vue`、`README.md` 与本文档。

---

## 二十、内容资产沉淀进度：口径扩展与实施（2026-09-13 已落地）

> 依据《内容资产沉淀进度调研与建议.md》（同日调研）落地的 C1-C11；C12（冷热分层/归档）按该文结论暂不做。
> 目标：让「内容资产」卡从「一张库存快照」升级为「可解释、可下钻、有目标与时间轴的沉淀进度」。

### 20.1 修掉两个真实缺陷（本次最先动的地方）

**（1）长标题帖的目录失配（口径少计）**：目录名 = `extract_torrents.sanitize_title(标题)`（非法字符替换 + 截断 80 字符），而履历的磁盘恢复判据是「目录名 == 库内标题」全等 —— 长标题必失配。实测 2 个目录（库内标题 99 / 90 字，目录名被截到 80 字）长期未被认领。
修复：新增 `resources.match_dirs_to_posts()` 收敛「目录名 ↔ 帖子」判定口径（按 `sanitize_title(标题) == 目录名` 比对，流式全表扫描 + 命中即停，仅在确有未认领目录时调用），`download_tasks._load` 的磁盘恢复改为调用它；`source_lookup` 去掉已无调用方的 `exact_only` 参数并补充「展示用 vs 判定用」的口径说明。
**实测**：`downloaded_posts` 130 → **132**，`reconcile` 未认领 2 → **0**（132 目录 = 132 认领）。

**（2）CSV 导出被截断（举一反三发现的既有缺陷）**：从「资产卡已沉淀帖 → 帖子页 → 导出」链路实测时发现：导出 CSV 行数与列表 total 对不上（全库导出只落 4 万余行、按作者导出只有 161 行）。
根因：`db.iter_query` 在生成器 `finally` 里 `conn.close()`，而 FastAPI 的 `StreamingResponse` 会把同步生成器交给线程池迭代（`anyio.to_thread`），每次 `next()` 可能落在不同 worker 线程 → `sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread`，响应中途断流、CSV 被静默截断（客户端仍收到 200）。
修复：`db.open_conn(cross_thread=True)` 供流式场景使用（`check_same_thread=False`，sqlite3 默认 serialized 模式，同一连接顺序跨线程使用安全），普通查询路径保持默认的线程亲和校验。
同时补齐导出接口缺失的入参：前端本来就在传 `author` / `adv`，但接口未声明这两个参数，**FastAPI 会静默忽略未声明的查询参数**——表现为「列表按作者筛过、导出的却是全库」，无报错无提示；现与列表页逐项对齐（含新增 `downloaded`）。
**实测**：全库导出 146,840 行 = `posts` 表行数；按作者 1,005 行 = 列表 total；`downloaded=1` 132 行 = 卡片数字；`undownloaded=1` 146,708 行；非法 `adv` 正确返回 400；服务日志无 ERROR / Traceback。

### 20.2 卡片重构（`DashboardView.vue`）

原四级漏斗（末级是字节）改为**三级同量纲漏斗** + 5 个信息行（口径依据见调研文档第二节「五条口径原则」）：

| 行 | 内容 | 口径要点 |
|---|---|---|
| 漏斗 | 收录帖子 → 已沉淀帖 → 本地文件（首级箭头带全库转化率，末级带目录数） | 同量纲（计数）；「占用体积」移出漏斗 |
| 目标 | 进度条 + 目标刻度（橙线）+ `目标 N%` 小字 + 主值 + 还差 N 帖 | 设置页可配（默认 互动 Top500 / 50%）；卡面不复述档位名与分母明细（档位交「分层」行高亮、明细进悬浮，见 §20.6）；**不做下钻**（该档按互动量取前 N，帖子页无法表达，强行下钻必然口径不一致） |
| 状态 | 在途 / 失败 / 可重下 / 空壳 / 近 N 日缺口 + 右侧对账 | 只看库存等于只看结果不看过程；「缺口」可点击下钻（口径与帖子页「未下载」筛选严格一致） |
| 分层 | 全库 / 互动≥10 / 互动 Top500 沉淀率 + 「集中在 X 版块」 | 全库百分比被长尾稀释（实测 0.09% vs Top500 3.4%，差 38 倍），必须分档；集中度悬浮给全部版块明细 |
| 存储 | 占用 · 可用/总容量 · 近 N 日沉淀帖数与体积 + 迷你增长柱 | 计数与字节分列两个视图；增长柱为纯 CSS（不新开 ECharts 实例） |
| 类型 | 既有「类型分布（按大小）」占比条 + 图例 | 沿用 `utils/category.buildTypeSegments`（唯一实现） |

下钻：`已沉淀帖` → `/posts?downloaded=1`（新增筛选）；`本地文件` → `/resources`；`缺口` → `/posts?date_from&date_to&undownloaded=1&sort=engagement_desc`（复用 R3 既有入口，不新开第二个推荐列表）；`类型` → `/resources?type=`。

### 20.3 后端口径扩展

- `/api/stats/assets`（缓存 key `assets_v1` → `assets_v2:{days}`）新增：`coverage`（三档分层沉淀率）、`state`（五态 + 缺口，缺口口径与 `/posts?undownloaded=1` 严格一致）、`reconcile`（磁盘目录 = 已认领 + 未认领 + 空壳，三者互斥）、`by_fid`（分版块沉淀率，按沉淀率排序）、`growth`（按首次落盘日聚合帖数/体积）、`goal`（目标档位/目标覆盖率/当前值/剩余缺口）、`disk_total` / `disk_free`（`shutil.disk_usage`，零依赖）。
- 下载履历升维（`download_history.json`）：`url → 目录名` 改为 `url → {dir, first_at, checked_at, fail_at, fail_error}`；成功时只在缺失时写 `first_at`（增长曲线据此判定「哪天沉淀的」）、并清空失败标记；失败时只更新失败标记（不动已记录的目录与首次时间）。启动时把旧字符串形态一次性归一为对象形态（`first_at` 置空 = 时间不可考，不伪造）。**失败清单从此不再随任务面板清理而丢失。**
- `download_tasks` 新增 `asset_snapshot()` 作为资产口径唯一数据源（一次持锁 + 一次目录校验，返回 alive/gone/active + 已认领目录 + 首次落盘时间 + 目录映射 + 失败清单），替换原先 `downloaded_urls/active_urls/gone_urls` 三个 getter（逐个调用会反复持锁并重复校验目录）。
- `/api/stats/health` 新增 `asset_stall_days`：采集正常但连续 `_ASSET_STALL_DAYS`(14) 天无新增本地资产 → 升 `warn`，与采集阈值同处（`_health_verdict`）判定，前端只上色；仅在 `level` 仍为 ok 时升级，不掩盖采集问题。
- `/posts` 新增 `downloaded` 参数（与 `undownloaded` 对称，共用 `_download_path_sets` 归一化与 URL-IN 分块构造 `_apply_url_match`）；`/posts/export` 补齐 `author` / `adv` / `downloaded`。
- 设置页新增「内容资产」分组：`asset_goal_scope`（新增 `enum` 单选类型，非法值回落默认）/ `asset_goal_rate`；默认值定义在 `web/config.py`（`ASSET_GOAL_SCOPE` / `ASSET_GOAL_RATE`），设置页只存覆盖值。

### 20.4 实测（2026-09-13）

- **后端**：重启后 22 个受影响接口真实请求全 200；`/api/posts?downloaded=1` total = **132** = `/api/stats/assets.downloaded_posts`（数字自洽硬校验）；导出 CSV 行数与列表 total 逐项吻合（见 20.1（2））；服务日志无 ERROR / Traceback。
- **前端**：`npx vue-tsc --noEmit` 0 错误 + `read_lints` 0 错误 + `npm run build` 成功；Playwright 双视口（桌面 1440×900 / 移动 390×844）**18 项检查全通过**——卡片五行渲染、体积已不在漏斗、桌面与移动端横向溢出均为 0、无控制台错误；真实点击「已沉淀帖」跳 `/posts?downloaded=1` 且分页显示「共 132 条」（与卡片一致）、摘要条出现「已下载」；设置页「内容资产」分组与枚举控件渲染正常。
- **现状读数**：收录 146,824 / 已沉淀 132（全库 0.09% · 互动≥10 0.32% · Top500 3.4%）/ 3313 文件 / 132 目录 / 20.7 GB；集中度 131/132 落在「技術討論區」；近 30 日缺口 19,061；`growth` 暂为空（历史条目 `first_at` 不可考，新下载会自动生成曲线——如实呈现，不伪造时间）。

### 20.5 改动文件

- `web/download_tasks.py`：履历结构升维与归一、`_record_history_locked`、`asset_snapshot()`（替换三个 getter）、磁盘恢复改用 `match_dirs_to_posts`、`dir_of` 映射。
- `web/resources.py`：新增 `match_dirs_to_posts()`；`source_lookup` 去掉 `exact_only`；`scan()` 返回 `empty_dirs`。
- `web/db.py`：`open_conn(cross_thread=...)` 修复流式查询跨线程关闭；`iter_query` 返回类型标注为 `Generator`。
- `web/api.py`：`AssetsResp` 及 6 个子模型、`stats_assets` 全量重写、`_asset_snapshot` / `_pct` / `_count_posts_by_paths` / `_count_by_fid` / `_url_forms` / `_apply_url_match`、`_apply_downloaded`、`/posts` 与 `/posts/export` 参数、`_health_verdict` 资产停滞分支、`HealthResp.asset_stall_days`。
- `web/config.py`：`ASSET_GOAL_SCOPE` / `ASSET_GOAL_RATE` 默认值。
- `web/settings.py`：`enum` 类型钳制 + 两个白名单项。
- 前端：`api/index.ts`（Assets 子类型 / Health.asset_stall_days / SettingItem enum / posts.downloaded）、`views/DashboardView.vue`（卡片重构 + 5 个派生 + 下钻 + 样式）、`views/PostsView.vue`（已下载筛选：状态 / 请求 / 摘要条 / 路由继承 / 导出）、`views/SettingsView.vue`（内容资产分组 + enum 控件）。
- `README.md`、本文档、《内容资产沉淀进度调研与建议.md》。

### 20.6 文案收敛：卡面与设置页「一处事实一处表达」（2026-09-13 已落地）

> 起因：用户上报「『内容资产』中的『沉淀目标』太多文字描述，请参考业界做法」。排查后确认症结不是「写得不清楚」，而是**同一事实在卡内被复述三遍 + 解释性文案占据常驻位**。

**诊断（改动前的重复点）**：

| 重复项 | 出现位置 1 | 出现位置 2 | 出现位置 3 |
|---|---|---|---|
| 档位名 `互动 Top500` | 目标行标题 `沉淀目标 · 互动 Top500 50%` | 分层行 `互动 Top500 3.4% 17/500` | — |
| 当前值 `3.4%` | 目标行 `当前 3.4%（17 / 500）` | 分层行 `互动 Top500 3.4%` | — |
| 分母明细 `17/500` | 目标行括号内 | 分层行 `17/500` | 设置页 desc |
| 「三档对应 / 长尾稀释」 | 设置页组级说明 | `asset_goal_scope` 项 desc | 卡片分层行提示 |

**业界对照**：Grafana stat-vs-target / Datadog SLO widget 的常驻形态只有「值 + 目标阈值刻度」，达没达用颜色表达；Google SRE 的 error budget 面板常驻只有「SLO / 当前 / 剩余预算」三个数，口径定义在使用方文档；解释性内容按 NN/g 的渐进披露原则放悬浮或设置端一次（Grafana 的 panel description 即为此设）。业内把这类指标统一收敛为三元组「目标 · 当前 · 缺口」。

**落地（四项）**：

1. **卡片目标行**只留「`沉淀目标` 标签 + 紧跟的目标值小字 `50%` + 当前值（15px 加粗主值）+ `还差 N 帖`」，删除档位名与 `（已下载 / 该档总数）`；小字不写「目标」二字（行首标签已含，否则成「沉淀目标 目标 50%」叠字）；达成时进度条整条转绿（`is-reached`），用颜色替代多余标签。可见文字由 36 字降到 19 字且无重复。
2. **`goalTip` 由 42 字三句压成一行结构化**：`目标档 X · 目标 N% · 当前 a/b → 还差 c 帖`。原 tooltip 里的「为何不下钻」属实现约束，移出面向用户文案、只留代码注释与文档；新增 `goalAria` 把完整口径（含分母与档位）挂到进度条的 `aria-label`，文字收敛不损无障碍。
3. **分层沉淀率行高亮当前目标档**（`is-goal`：底色 + 左侧色条 + 悬浮给该档分母明细），承接被移除的档位名——分母仍可解释，项目专属约束第 12 条不被削弱。
4. **设置页去重**：「内容资产」组级说明删除（`GROUPS.desc` 改为可选 + 模板 `v-if`，避免渲染空行）；`asset_goal_scope` 的 3 句 desc 压成 1 句，主观建议「建议选 Top500」改由选项 label「互动 Top500（推荐）」承载；`asset_goal_rate` desc 压成 1 句。取舍理由（长尾稀释）移入 `web/settings.py` 代码注释。该组可见说明文字由 4 段约 90 字降到 2 句约 25 字。

**刻意的未做**：不整卡折叠（「对账」「集中在 X 版块」仍常驻）——本次只处理用户明确指出的「沉淀目标」，避免顺带改动放大回归面；若后续要降整卡密度（当前约 35 段 12px 标签），另立一节评估。

---

## 二十一、「本月最热」互动量图抽离 + 「今日未完整」口径（2026-09-13 已落地）

> 起因：用户提出「本月最热」卡内的互动量 sparkline 应抽成独立图表、卡片只做列表，并要求参考业界做法。
> 结论：**同意抽出**，落点为**趋势区第三张图**（全宽一行），窗口用**近 N 日滚动**（跟随「全站发布趋势」的天数）。

### 21.1 抽出的理由（实测量化，不是审美偏好）

| 项 | 实测 |
|---|---|
| 数据口径 | `month_top.daily` = `SUM(点赞+回复) GROUP BY date`，即**当月全站 14,717 帖**的每日互动量合计——与列表那 10 条帖子无关 |
| 渲染尺寸 | 卡宽 286 / 406px → 图区 **244×26px**（1920 下 364×26）、13 根柱、柱宽 16.6px、**无坐标轴无刻度** |
| 逐日数值 | 09-01 24983 · 09-04 25696 · … · 09-11 17897 · 09-12 12207 · **09-13 1215** |

三个硬伤：

1. **末日塌陷（会误导）**：9-13 是今天，帖子刚发、点赞未累积，1215 = 前一日 12207 的 **10%** → 柱高只剩 **1.2px**，视觉上近乎消失，会被读成「热度断崖」；
2. **不可读数**：26px 高承载 13 个点，相邻日差异（21.8 vs 22.6px）肉眼不可辨，只能逐柱 hover；
3. **同卡混装两个口径**：列表 = 当月 **Top10 帖**（单位：帖，值 1,391/1,306…），图 = 当月**全站所有帖**的每日互动量合计（单位：互动量，值 12k~26k）—— 卡片标题只声明了前者。

补充：全站**没有任何独立图表**承载互动量（趋势双图都是发帖量 = 供给侧），把最有决策价值的「需求侧」指标困在 26px 装饰高度里，等于降级。

### 21.2 业界做法（简）

| 产品 / 规范 | 做法 |
|---|---|
| GA4「Reports snapshot」 | 卡片 = **单指标**（数字 + 该指标自身迷你趋势）；主趋势是独立图 + 指标选择器；明细表独立 |
| YouTube Studio | 「概览」= 指标卡 + 独立「随时间变化」图（可切观看次数/时长）；「内容」页 Top 视频 = **纯列表不带图** |
| Grafana / Datadog | one panel, one question；sparkline 只承载该 stat 自身的时序 |
| Ant Design / Material | sparkline 定位为「紧凑、无坐标轴的单一指标走势」，需读数/对照时用完整图表 |
| 微博热搜 / B站热门 / 知乎热榜 / Steam·App Store 榜单 | 榜 = 纯列表（排名 + 热度值 + 变动）；趋势在独立图表 |

共性：**榜单 = 列表，趋势 = 图表**；卡内迷你图只能承载「卡自身那个指标」。本卡的迷你图是另一个口径（全站每日互动量）→ 不满足前提。

### 21.3 落点决策

| 方案 | 结论 |
|---|---|
| **B. 趋势区新增独立图（采用）** | 供给侧（发帖量）与需求侧（互动量）**同屏可对照**；**投屏/演示轮播下无需交互即可见**（切换器在无人操作的大屏上形同虚设——决定性理由）；可用完整坐标轴、DataZoom、规范标注 |
| A. 现有趋势卡加指标切换 | 像素零新增，但必须切换才能看，投屏场景无意义；备选 |
| C. 并入发布趋势做双 Y 轴 | 不符合「单独展示」的诉求，且双轴有误读风险；不采用 |

窗口：**近 7/14/21/28 日滚动**（跟随「全站发布趋势」的天数选择），与趋势区其它图横轴严格对齐——并排图窗口不一致是本项目踩过的坑（§19.1）。
布局：初版独占一行全宽；**同日按用户要求改为与「待下载推荐」同行各占 1/2**（见 §21.8），窄屏自动上下堆叠。既有的两张半宽图（全站趋势 / 分版块）不受影响。

### 21.4 口径修正：「今日未完整」

实测「今天」是窗口最后一天，数据天然残缺：发布量 **322 / 前日 1007（32%）**、互动量 **1215 / 前日 12207（10%）**。三条处理：

1. 曲线最后一天画**橙色虚线 + 「今日未完整」标签**（markLine，Grafana / GA4 对「未完整周期」的通行标注）；
2. tooltip 对最后一天追加一行：「今日数据未完整（当日抓取尚未覆盖全天），不计入峰值/谷值/日均」；
3. **统计卡（峰值/谷值/日均/窗口合计）排除今天** —— 否则「谷值」永远是今天。实测修复前谷值会显示 1,215 / 09-13，修复后为 **12,207 / 09-12**。发布量图同步应用（共用 option 构造），避免并排两图对「谷值」各算一套口径。

### 21.5 顺带修掉的两处既有缺陷

| # | 缺陷 | 影响 | 修复 |
|---|---|---|---|
| 1 | **`markLine` 组件从未注册**（`use([...])` 里没有 MarkLineComponent） | 分版块图「点总趋势某天 → 同天高亮」的垂直标线**自上线起从未渲染**（徽标会显示、图内无标线）；ECharts 按需引入模式下未注册组件被静默忽略 | 注册 `MarkLineComponent`；标线坐标与分版块横轴（完整日期）本就一致，注册后即可渲染 |
| 2 | **`markArea` 的 data 必须是「坐标对」**（实施中踩到） | 最初用 `markArea: { data: [[{ xAxis: '09-13' }]] }` 打浅色底 → ECharts 读 `undefined.coord` 抛错，异常中断 `loadP0` 后续渲染：**互动量图与活跃作者/活跃版块两张图当场全部空白**（实测到 5 张图只剩 2 张） | 改用 `markLine`（接受单个对象，语义也够表达「此点未完整」） |

> 教训：ECharts 按需引入时，**用到的每个组件都必须显式注册**，未注册不会报错只会静默缺失；mark 系列组件的 `data` 结构各不相同（markArea 要坐标对、markLine 要单对象），写错会直接抛错并中断渲染链。

### 21.6 实测（2026-09-13）

- **后端**：`/stats/trend?metric=posts|engagement` 双口径返回；`month_top` 响应体去掉 `daily`。**注意**：只删 `_calc` 里的 `daily` 不够——`TodayTopResp.daily` + `BoardDailyResp` 必须一并删，否则响应模型会用默认值把 `daily: []` 补回来（第一次实测即因此仍是 4 个字段）。21 个受影响接口全 200。
- **前端**：`npx vue-tsc --noEmit` 0 错误 + `read_lints` 0 错误 + `npm run build` 成功；Playwright 双视口全过——5 张图实例齐全（发布 / 分版块 / 互动量 / 活跃作者 / 活跃版块）、新卡 4 张统计卡、天数切换两口径**同时重取**（`days=14&metric=posts` + `days=14&metric=engagement`）、sparkline 彻底移除（`.board-spark` 计数 0）、本月最热仍为 10 行纯列表、双端无横向溢出、无控制台错误；截图确认橙色虚线 + 「今日未完整」标签渲染；卡片全屏（新增 `'eng'` 分支）进出后仍 5 张图（`document.fullscreenElement` = 该卡）。
- **移动端多修一处**：390px 下统计卡行溢出 7px（互动量是 6 位数，如窗口合计 109,191，4 张卡一行放不下）→ `.trend-stats` 加 `flex-wrap: wrap`（统计卡自身仍 `nowrap` 防数值被拆），溢出归零。
- **未实测部分**：分版块图那条联动标线的**可视化**未单独确认（自动点击难以命中折线，未触发「联动聚焦日」）；已确认其坐标值与横轴数据格式一致（均为完整日期），且渲染机制与本图 markLine 相同（后者已截图确认）。headless 下 Esc 不退出全屏（浏览器行为限制，与代码无关）。

### 21.7 改动文件

- `web/api.py`：`stats_trend` 加 `metric` 参数（`posts` / `engagement`）、`TrendPointResp.count` → `value`、缓存 key `trend_v2:{metric}:{days}`；`stats_month_top` 去掉 `daily`（缓存 key `month_top_v4`）；删 `TodayTopResp.daily` 与 `BoardDailyResp`。
- `web/frontend/src/api/index.ts`：`TrendPoint.value` + `TrendMetric`、`trend(days, metric)`；删 `BoardDaily` 与 `TodayTop.daily`。
- `web/frontend/src/views/DashboardView.vue`：注册 `MarkLineComponent`；抽出共用 `buildDayLineOption()`（两图唯一实现，含「今日未完整」标注）；`daySeriesStats()` 共用统计（排除今天）；新增 `trendEng` 系列状态 / `renderTrendEngChart` / `trendEngCache` / 趋势区第三张卡（初版全宽、4 统计卡、卡片全屏 'eng'）/ `.trend-stats` 换行样式；**删除** 本月最热 sparkline（模板 + `sparkHeight`/`sparkText` + `.board-spark`/`.spark-bar` 样式）。
- `README.md`、本文档。

### 21.8 排版调整：互动量趋势 + 待下载推荐 同行各占 1/2（2026-09-13 同日追加）

**起因**：用户要求「待下载推荐」移到「每日互动量趋势」右侧、各占 1/2 屏，且移动端自适应。

**改动**：

1. **位置**：待下载推荐卡由「热门榜下方全宽区」整体移入 `.trend-row`，紧随互动量卡（同属趋势区第二行）；互动量卡的 `.trend-full`（`grid-column: 1 / -1`）改回 `.trend-half`，两卡在同一 `1fr 1fr` 网格里各占一半。取数方式随之从 P1 懒加载改为 **P0 随首屏加载**（`onMounted` 调 `loadPending()`，从 p1Observer 回调中移除），否则卡片在首屏可见却要等滚到热门榜才有数据。
2. **卡片内部**：待下载清单由「桌面双列」改为**单列**（卡片自身已半宽；条数与滚动样式于同日再改为「10 条 + 滚动」，见 §21.9）；`autoRefreshTick` 改为无条件刷新该卡（它已是首屏卡，不再依赖「热门榜已加载」这一无关条件）。
3. **等高与留白**：两卡同行时网格默认拉伸等高，右卡 8 行清单决定行高 → 左卡若保持 300px 固定图表高度，底部会留 46px 空白（大屏态因 `--chart-h` 变矮会更多）。故给互动量卡加 `.trend-eng`：卡片 flex 纵向布局 + 图表区 `flex: 1`（`min-height: var(--chart-h, 300px)` 只作下限），并在 `loadPending()` 拿到新清单后 `await nextTick(); trendEngChart.value?.resize()`——ECharts 不监听容器尺寸，行高变化后不 resize 会让画布停在旧高度。
4. **移动端**：沿用 `.trend-row` 在 `≤1100px` 的 1 列规则自动上下堆叠，两卡恢复全宽、清单仍单列，无需额外规则。

**实测（Playwright，1440×900 / 1920×1080 / 390×844）**：

| 视口 | 结果 |
|---|---|
| 1440 | 两卡同行同宽（588 = 1192/2）、高度差 **0**、图表区吃满（底部空白仅 19px 卡片内边距，画布 327 = 容器 327）、待下载 **8 条且未滚动即已加载** |
| 1920 | 同上（828 = 1672/2，画布 360 = 容器 360） |
| 390 | 上下堆叠（互动量在上、待下载在下，均全宽 354）、清单单列、8 条、无横向溢出 |

另回归确认：滚动到底仍能正常触发 P1 热门榜懒加载（本月最热 10 行），待下载保持 8 条；三视口均无控制台错误。`vue-tsc --noEmit` 0 错误、`build` 成功。

（表中「8 条」为该次改动时的口径，条数随后改为 10，见 §21.9。）

### 21.9 卡片头部模式统一 + 待下载推荐 10 条滚动（2026-09-13 同日追加）

**起因**：用户要求「参考「每日互动量趋势」卡，其它卡片按同一模式改：标题加口径说明，然后换行显示其它图表、列表等」；并把「待下载推荐」条数 8 → 10、加滚动条（参考「本月最热」）。

**改动一：卡片头部统一模式（11 张卡全覆盖）**

- **规则**：第一行只放「标题 + 口径说明副标题（悬浮给完整口径）」+ 右侧操作/筛选；**第二行起才是内容**（统计卡行 → 图表 / 列表）。
- **三张趋势卡**：统计卡行从 `.chart-head-right` 移到 head 之下，改用新的 `.chart-stats`（左对齐、`margin-bottom: 8px`，替代原 `.trend-stats` 的右对齐——它原先挂在标题行右侧，半宽卡靠 `flex-wrap` 折行兜底、全宽卡与标题同行，同一套卡两种观感）；「全站发布趋势」卡头补口径副标题「近 N 日 · 按发布日统计」（含「统计卡与曲线均排除今天」的完整口径 tooltip），「分版块发布对比」补「近 N 日 · 按版块发帖量」（含窗口跟随、联动聚焦、DataZoom 说明）。
- **活跃作者 / 活跃版块**：原先把口径说明单独占一行（「按X发帖量 · 点击查看…」），现移入标题行（`chart-head-left`），并补悬浮完整口径（窗口切换来源、发帖量定义、同源同窗关系）。
- **点赞最高帖 / 回复最高帖**：补口径副标题「全站累计 · 按点赞数 / 按回复数」（明确「不受任何时间筛选影响」）+ 交互说明。
- **最新最热 / 本月最热**：日期角标（`.board-date`，其 tooltip 已含窗口口径）**合并进副标题**——`09-13 当日 · 按点赞+回复` / `2026-09 当月 · 按点赞+回复`，卡头右侧只剩「排序切换 + 查看更多」，少一个元素、口径更近标题；`.board-date` 样式随之删除（无引用）。「本月最热」的 tooltip 另补一句「自然月窗口 vs 待下载推荐的近 30 日滚动窗口」的口径差异。
- **内容资产 / 每日互动量趋势 / 待下载推荐**：本就是这个结构（标题 + `chart-sub`），未改动，作为样板。

**改动二：待下载推荐 8 → 10 条 + 滚动**

- 前端 `api.pendingDownloads(10, 30)`（默认值同步抬到 10）、后端 `limit` 默认 8 → 10（仍 `le=30`），骨架屏 4 → 8 行。
- 列表**直接复用 `.board-list`**（热门榜四卡用的类）：单列 + `max-height: 360px` + `overflow-y: auto`，与「本月最热」完全同款；`.pending-grid` 与 `.pending-empty` 里的 `grid-column` 随之删除（容器已是 flex 列表）。
- **滚动条常显**：给 `.board-list` 加 `scrollbar-width: thin` + `scrollbar-color`（Firefox/Chromium）与 `::-webkit-scrollbar` 6px 细窄样式（旧 WebKit 兜底）。实测样式生效（`getComputedStyle(list,'::-webkit-scrollbar').width = 6px`、滑块 `rgb(200,206,216)`）；Playwright 无头默认带 `--hide-scrollbars`（故占位测得 0），去掉该默认参数后实测**滚动条占位 10px、可滚动**（`scrollHeight 492 > clientHeight 360`）。该样式同时作用于「本月最热 / 点赞最高帖 / 回复最高帖 / 最新最热」，四卡与待下载推荐滚动条观感一致。

**实测（Playwright，1440×900 / 1920×1080 / 390×844）**

| 断言 | 结果 |
|---|---|
| 11 张卡片均有口径副标题 | 全部命中（内容资产/全站/分版块/互动量/待下载/活跃作者/活跃版块/点赞/回复/最新最热/本月最热） |
| 头部行内不混入内容 | 每张卡 `.chart-head` 直接子元素恒为 `chart-head-left + chart-head-right`，统计卡/列表/图表容器混入数 **0** |
| 统计卡行位于标题行下方 | 命中（y 大于 head 的 y） |
| 待下载 10 条 + 可滚动 | `rows=10`、`overflow-y=auto`、`scrollHeight 492 > clientHeight 360` |
| 本月最热仍 10 条 | 命中（未被共用类改坏） |
| 两卡同行等高 | 1440：439 = 439；1920：439 = 439 |
| 窄屏堆叠 / 无横向溢出 / 无控制台错误 | 三视口全部通过 |

`vue-tsc --noEmit` 0 错误、`read_lints` 0 错误、`npm run build` 成功、`python -m py_compile web/api.py` 通过，后端默认条数改动已重启生效。

### 21.10 缩放选区联动补齐：全站发布趋势 ⟷ 每日互动量趋势（2026-09-13 同日追加）

**用户报告**：趋势天数输入 365 后出现横向滑动条；拖动全站趋势的滑动条，「分版块发布对比」有联动，但「每日互动量趋势」纹丝不动。

**诊断结论：不是 bug，而是这条联动边从未接线。**

- `buildDayLineOption()` 对 `points.length > 31` 的两张单指标图都注入 `dataZoom: [inside, slider]` —— 所以 **365 天下「全站」与「互动量」两张图都有滑动条**（用户只看到全站那条，因为互动量在下排右半宽卡里）。
- `trendChart.on('datazoom', () => applyTrendZoom())` 的监听**只挂在全站图上**；`applyTrendZoom()` 只做「写 `trendZoomRange` + `renderFidTrendChart()`」，而 `renderFidTrendChart()` 是**唯一**消费 `trendZoomRange` 的地方。
- `renderTrendEngChart()` 只读 `trendEng.value` 全量数据，既无 datazoom 监听、也不读 `trendZoomRange`；其函数头注释只声明「刻意不参与 **Tooltip 双向联动与自动轮播**」，**未提缩放** —— 即新图接入时漏接了这条边，而非有意排除。
- 实测（1920×1080，输入 365 天）：三条请求齐发（`trend?days=365&metric=posts` / `metric=engagement` / `trend_by_fid?days=365`）；在全站图上滚轮缩放后，分版块卡头出现「跟随趋势区间：09-30 ~ 08-27」，而互动量图 x 轴（09-14…09-06）与滑动条宽度前后**逐像素一致**；反向在互动量图上缩放则三图毫无变化 —— 该图是完全孤立的视窗。

**顺带发现并修复的第二个问题**：跨年徽标读起来像「起止倒序」。徽标只取 `trendZoomRange.start.slice(5)`（MM-DD），而 365 天窗口必然跨年（2025-09-30 ~ 2026-08-27），显示为「09-30 ~ 08-27」。

**改动（`web/frontend/src/views/DashboardView.vue`）**

1. `applyTrendZoom(source: 'trend' | 'eng' = 'trend')` 参数化：新增 `dayChart(key)` 统一取「图表实例 + 日期序列」，读哪张图的 `dataZoom[0]` 由参数决定；算出日期区间后，把选区 **`dispatchAction('dataZoom', {startValue, endValue})` 同步给另一张单指标图**（不带 `dataZoomIndex` = 同时作用于它的 inside 与 slider），**只移动视窗、不改数据** —— 比照分版块那样裁剪数据更干净：滑动条语义完整，用户可在任一张图上继续调。
2. 新增 `zoomSyncing` 防回声标志（与既有 `tipSyncing` 同思路）：`dispatchAction` 会**同步**触发目标图自己的 `datazoom` 事件，不挡住两张图会互相触发；`trendEngChart` 的监听由新标志 `trendEngZoomBound` 保证只注册一次，并在 `rebuildCharts()`（大屏/卡片全屏切换会 dispose 重建实例）里复位。
3. `clearTrendZoom()` 复位**两张**单指标图（原先只复位全站，会留下「徽标已清、互动量仍在旧区间」的不一致）；`onTrendDaysChange()` 改为直接复用 `clearTrendZoom()`。
4. 徽标文案抽成 `zoomRangeLabel` computed：同年 `MM-DD`，**跨年显示完整 `YYYY-MM-DD`**。
5. 显式记录**单向边界**：分版块图的缩放不回传（其选区语义是「在已裁剪数据里再看一段」，后端 `trend_by_fid` 只支持「最近 N 天」，回传会引入循环裁剪语义）——写进 `applyTrendZoom` 注释。

**实测（Playwright 1920×1400，判据 = 两张同窗图在同一横向比例处必须读出同一天）**

> 判据设计说明：首版用「同坐标日期是否变化」+ 截图比对，被两件事污染 —— ① 互动量卡在 1080 高视口下位于折叠线以下（`mouse.move` 落在窗口外，滚轮实际滚了页面）；② tooltip 轮播会随时弹出别的图的 tooltip。改为 **1920×1400 视口 + 悬停即暂停该图轮播 + 取离光标最近的 tooltip**，并用「两图同比例同日期」这一联动充分判据。

| 断言 | 结果 |
|---|---|
| 方法自检：缩放前两图 @35% 同一天 | 01-08 = 01-08 ✓ |
| 全站滚轮缩放 → 互动量跟随 | 两图 @35% 均为 01-14（且 ≠ 01-08）✓ |
| 跨年徽标 | `跟随趋势区间：2025-09-30 ~ 2026-08-27` ✓ |
| 互动量滚轮缩放 → 全站跟随（反向） | 两图 @35% 均为 01-23（且 ≠ 01-14）✓ |
| 徽标 ✕ 清除选区 | 徽标消失，两图都回到 01-08（= 缩放前基准）✓ |
| 切换天数（365 → 30） | 无残留徽标 ✓ |
| 卡片全屏往返（dispose + 重建实例）后 | 基线两图 01-08 = 01-08；再缩放后两图 01-14 = 01-14 ✓ |
| 无横向溢出 / 无控制台错误 | 通过 ✓ |

`vue-tsc --noEmit` 0 错误、`read_lints` 0 错误、`npm run build` 成功。

### 21.11 联动「反向」补齐与全量审计：三图双向（2026-09-13 同日追加）

**用户要求**：不止单向有联动，反向也要有联动；并检查大屏涉及的反向联动内容（同时要求把该原则写入 `CODEBUDDY.md` 第 19 条）。

**审计结果（趋势区三图全部联动边）**

| # | 联动边 | 正向 | 反向（改前） | 处置 |
|---|---|---|---|---|
| ① | 窗口天数跟随 | 天数下拉 → 三图 | 无（图表无对应控件） | 天然单向（控制项 → 视图），非缺口 |
| ② | Tooltip 联动 | 全站 → 分版块 | 分版块 → 全站 | 原本即双向（`syncTipTo` 两处注册），未动 |
| ③ | 聚焦某天 | 全站 → 分版块标线 | 无 | **补齐为三图双向**（下方详述） |
| ④ | 缩放选区 | 全站 → 分版块裁剪 | 无（上一轮还以「单向边界（刻意）」自证） | **补齐为三图双向** |
| ⑤ | 版块聚焦配色 | 分版块图例 → 全站线色 | 无（全站无图例/无版块入口） | **不可达**：该图例 `show: false`，待用户决策 |
| ⑥ | Tooltip 轮播 | 各图独立计时 | — | 非跨图联动 |

**改动一：缩放联动从「两张单指标图双向」扩到「三图双向」**

- 新增 `fidRenderedDates`（分版块**本次实际渲染**的日期，可能已被共享区间裁剪）+ `linkedChart(key)` 统一取三图的「实例 + 日期序列」。
- 分版块注册 `datazoom` 监听（与另两张同款），其选区索引经 `fidRenderedDates` 映射回真实日期后回传为共享区间，再分发给两张单指标图。
- **分版块作为来源时的「满区间」特判**：它的数据本身已被裁剪，滑动条只表示「在裁剪区间内再看一段」，故「选区覆盖其全部数据」不视为区间变更（否则每次裁剪后复位滑动条都会自我触发）。
- 每次裁剪重绘后把分版块自身滑动条复位为「裁剪区间的满宽」（索引区间在重绘后已失效），该 `dispatchAction` 同样被 `zoomSyncing` 挡回声。

**改动二：聚焦日联动补齐为三图双向（并修掉「接线了但点不中」）**

- 状态仍只有 `linkedDay` 一处，新增 `focusDay(date)` 统一入口（三图任一发起 → 三图都重绘），`clearDayLink` 同步重绘三图。
- 单指标图的标线由 `buildDayLineOption` 新增 `markDate` 支撑：`markLine.data` 变为数组（① 今日未完整橙虚线 ② 聚焦日蓝实线，逐项 `lineStyle`/`label` 覆盖；与末端同日时不重复画），互动量图据此次获得「参与联动」的能力。
- **修掉两个「接线了却点不中」的隐患**（这才是本次真正的坑）：
  1. 折线 markLine/series 的点击命中区只有 `lineStyle.width`（2px）——固定列上扫 14 次点击**仅命中 1 次**；
  2. 即便命中，`triggerLineEvent` 给出的 series 事件 **`dataIndex` 为 undefined**（`dataIndex` 是「点中符号」的语义，而本图 `showSymbol: false` 不渲染符号）→ 旧代码 `trend.value[undefined]` 必然落空（实测 15/15 全落空），**该功能自上线起从未可用**。
  → 改为 `bindPlotClick()`：在 `chart.getZr()` 层监听，用 `containPixel({ gridIndex: 0 })` 判定「点是否落在绘图区内」（天然排除底部缩放条与图例），再用 `convertFromPixel({ xAxisIndex: 0 }, offsetX)` 把像素换算成日期（分类轴返回索引，四舍五入）。命中区从 2px 扩到整列，三图一致。

**实测（Playwright 1920×1400，12 项断言全通过）**

| 断言 | 结果 |
|---|---|
| 方法自检：同窗两图同比例同日期 | 01-08 = 01-08 ✓ |
| 正向：全站缩放 → 互动量跟随 / 分版块裁剪徽标 | 01-14 = 01-14 ✓ / 徽标出现 ✓ |
| 反向：互动量缩放 → 全站跟随 | 01-23 = 01-23 ✓ |
| **反向：分版块缩放 → 两张单指标图同步** | 01-08 → 01-12 = 01-12 ✓ |
| 联动稳定（1.5s 内徽标不振荡，无环形触发） | ✓ |
| 正向：全站点某天 → 三图同标 | 「联动聚焦日：02-17」徽标 ✓ + 三图蓝线截图确认 ✓ |
| **反向：分版块点某天** / **第三向：互动量图点某天** | 均出现聚焦徽标 ✓ |
| 清除聚焦日 / 清除选区 / 切天数 / 全屏往返后 | 均正确复位、联动仍生效 ✓ |
| 无横向溢出 / 无控制台错误 | ✓ |

**遗留（需用户决策）**：⑤ 版块聚焦配色（分版块图例点击 → 全站趋势线换为该版块色）当前**不可达**——该图例 `legend.show: false`。三个选项：① 开启图例（需同时调 `grid.top` 让位，8 个版块名会占一行）；② 改为「点分版块某条折线 = 聚焦该版块」（与「点某天」共用同一手势，需要区分命中目标）；③ 删除该联动与相关徽标/代码（承认不需要）。未自行改动，等确认。
